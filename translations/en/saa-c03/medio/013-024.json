{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "saa-c03",
    "level": "medio",
    "sourcePath": "data/exams/saa-c03/medio.json",
    "range": "013-024",
    "generator": "offline-marian-faithful-v2"
  },
  "questions": {
    "saa-med-013": {
      "text": "A platform allows customers to download private reports stored in a pocket S3 The application needs to provide each customer with a temporary link, valid for 15 minutes, to download only your report, without changing the Bucket permissions. S3 meets this requirement?",
      "options": [
        "Enable bucket versioning",
        "Pre-signed URLs (presigned URLs) generated by the application with defined validity",
        "Make the bucket public during the download window",
        "Create a user IAM for each customer with access to the pocket"
      ],
      "explanation": "Presigned URLs carry a temporary signature based on the credentials of the person who generates them, granting punctual access to a specific object for a limited time, without touching the policies of the pocket.",
      "hint": "Think of the link that loads an 'authorization with validity term' embedded in itself URL.",
      "optionRationales": [
        "Presigned URLs carry a temporary signature based on the credentials of the person who generates them, granting punctual access to a specific object for a limited time, without touching the policies of the pocket.",
        "Exposing the entire newsletter publicly, even if for minutes, would deliver all the customer reports to anyone on the internet.",
        "Create identities IAM for external customers is unstoppable and contrary to the purpose of IAM, focused on internal identities and workloads.",
        "Version preserves versions of objects against accidental changes — does not grant temporary access to private objects."
      ]
    },
    "saa-med-014": {
      "text": "A static site hosted in S3 is distributed via CloudFront. The security team has discovered that users can bypass the CloudFront accessing the objects directly through the URL What is the recommended way to ensure that the content is served only through the distribution of CloudFront?",
      "options": [
        "Use names of long and random objects as protection",
        "Move objects to class S3 Glacier",
        "Configure Origin Access Control (OAC) in the distribution and restrict the bucket policy to accept only requests from the CloudFront",
        "Enable bucket versioning to hide URLs"
      ],
      "explanation": "The OAC does the CloudFront sign the original requests; combined with a bucket policy that only authorizes this principal, direct access to the bucket is blocked and all traffic passes through the distribution.",
      "hint": "Origin must rely only on a 'carimbo' that only the distribution has — what modern feature makes this signature?",
      "optionRationales": [
        "The OAC does the CloudFront sign the original requests; combined with a bucket policy that only authorizes this principal, direct access to the bucket is blocked and all traffic passes through the distribution.",
        "Versioning does not hide or restrict URLs — objects would remain accessible directly.",
        "Changing the storage class would affect the availability of the site, without any relation to restricting the access path.",
        "Security by obscurity is not access control: URLs leak in logs and history, and direct access would continue technically allowed."
      ]
    },
    "saa-med-015": {
      "text": "A platform team allows developers to create their own IAM Roles for applications, but wants to ensure that no role created by them exceeds a defined permission ceiling (for example, never access the billing service), even if the attached policy is broad. IAM imposes that roof?",
      "options": [
        "Bondary permissions (limit of permissions) attached to the roles created by developers",
        "Groups IAM containing all the roles",
        "Inline Policy with Large Allow on each Role",
        "Cost tags on roles created"
      ],
      "explanation": "The boundary permissions define the maximum effective permissions of an identity: the final permission is the intersection between the annexed policy and the boundary, preventing climbing beyond the established ceiling.",
      "hint": "Think of the 'frame' that delimits the extent to which the permissions of an identity can reach, regardless of the policies attached.",
      "optionRationales": [
        "The boundary permissions define the maximum effective permissions of an identity: the final permission is the intersection between the annexed policy and the boundary, preventing climbing beyond the established ceiling.",
        "A broad policy of Allow does the opposite: increases permissions instead of limiting them to a ceiling.",
        "Groups IAM group only users (roles do not enter groups) and serve to grant batch permissions, not to impose limits.",
        "Tags categorize resources for organization and costs — do not influence permission assessment."
      ]
    },
    "saa-med-016": {
      "text": "A three-layer architecture has public web instances and private application instances. The requirement is that the application layer accepts traffic at the door 8080 coming exclusively from web forums — even if new web forums are created or replaced by Auto Scaling with different IPs. What configuration of Security Group responds to this dynamically?",
      "options": [
        "No. Security Group application layer, create a rule of entry on the door 8080 having as origin the ID of the Security Group of the web layer",
        "Use one Network ACL allowing the door 8080 of all VPC",
        "Clear the door 8080 for 0.0.0.0/0 and trust the private subnet",
        "List the current private IPs of web instances in the entry rule"
      ],
      "explanation": "Security Groups may refer to other Security Groups as origin: any instance associated with the web SG is automatically authorized, without relying on fixed IPs — the pattern for dynamic layers.",
      "hint": "Instead of relying on changing addresses, the rule can rely on the 'group crack' of the origin instances.",
      "optionRationales": [
        "Security Groups may refer to other Security Groups as origin: any instance associated with the web SG is automatically authorized, without relying on fixed IPs — the pattern for dynamic layers.",
        "Fixed IPs break when the Auto Scaling replaces instances — the rule would be out of date constantly.",
        "Open to any origin violates the least privilege: any resource reaching the network could access the application layer.",
        "NACLs operate by IP bands in the sub-network and fail to restrict the origin to a dynamic group of specific instances."
      ]
    },
    "saa-med-017": {
      "text": "After adding entry rules in a Network ACL to allow traffic HTTPS (door 443) to a sub-network, the responses of the instances for the customers stopped leaving. Security Group is correct. What characteristic of NACLs explains the problem and what is the correction?",
      "options": [
        "NACLs are stateful and the entry rule should already release the answer; the problem is Security Group",
        "NACLs block HTTPS by default and this cannot be changed",
        "The door 443 can only be released in Security Groups, never in NACLs",
        "NACLs are statess: it is also necessary to create rules of SAIDA allowing the ephemeral doors (1024-65535) used in responses"
      ],
      "explanation": "For being statesss, NACLs evaluate each direction independently: the response traffic exits through ephemeral doors of the customer and needs an explicit exit rule — unlike the Security Groups, which are stateful.",
      "hint": "Those who don't keep memory of the connection need permission for the E-way back — and the back uses high doors.",
      "optionRationales": [
        "For being statesss, NACLs evaluate each direction independently: the response traffic exits through ephemeral doors of the customer and needs an explicit exit rule — unlike the Security Groups, which are stateful.",
        "It's the reverse: stateful is the Security Group; a NACL does not 'remember' connections and requires explicit rules in both directions.",
        "NACLs are fully configurable — there is no unchangeable blockage of HTTPS.",
        "Any door can be released in NACLs; the question is to cover both directions of the flow."
      ]
    },
    "saa-med-018": {
      "text": "The network team needs to investigate why connections between two sub-networks are being rejected, identifying origin, destination, door and action (ACCEPT/REJECT) of each traffic flow in the VPC. What resource does these records provide??",
      "options": [
        "AWS CloudTrail",
        "AWS X-Ray",
        "Amazon Macie",
        "VPC Flow Logs"
      ],
      "explanation": "Flow Logs capture network flow metadata (IPs, ports, protocol, bytes, and acceptance/rejection action) at the level of VPC, subnet or ENI — the standard tool for diagnostic connectivity and traffic audit.",
      "hint": "To see the extract of the network flows of the VPC, with accepted or rejected verdict, which log you enable?",
      "optionRationales": [
        "Flow Logs capture network flow metadata (IPs, ports, protocol, bytes, and acceptance/rejection action) at the level of VPC, subnet or ENI — the standard tool for diagnostic connectivity and traffic audit.",
        "The CloudTrail records calls from API (administrative actions), not network packet flows between resources.",
        "The Macie discover sensitive data in buckets S3 — does not monitor network traffic.",
        "X-Ray tracks requests within distributed applications for performance analysis, not accepted/rejected network flows in VPC."
      ]
    },
    "saa-med-019": {
      "text": "The security team wants to eliminate the bastion host and the use of keys SSH: the objective is to get shell in the instances EC2 private without opening the door 22, with registered sessions and permission-controlled access IAM. Which service meets these requirements?",
      "options": [
        "Increase the safety of the bastion with MFA in the operating system",
        "Enable access via RDP instead of SSH",
        "Distribute the same key SSH for the whole team by a safe",
        "AWS Systems Manager Session Manager"
      ],
      "explanation": "Session Manager provides shell via console or CLI using the agent SSM, without open entrance doors, without keys SSH and no bastion, with control by IAM and registration of sessions in the CloudWatch/S3.",
      "hint": "The 'Session Manager' Systems Manager Door dispensation 22, intermediate keys and servers.",
      "optionRationales": [
        "Session Manager provides shell via console or CLI using the agent SSM, without open entrance doors, without keys SSH and no bastion, with control by IAM and registration of sessions in the CloudWatch/S3.",
        "Hardening the bastion keeps the door SSH exposed and key management — the requirement is to delete this model.",
        "Changing the protocol only changes the exposed door — does not remove the attack surface or add control by IAM.",
        "Shared key eliminates individual traceability and keeps the open door model that you want to abolish."
      ]
    },
    "saa-med-020": {
      "text": "A company needs to choose the right service for every need for security: (1) detect malicious activities and compromised credentials by analyzing account logs; (2) find software vulnerabilities (CVEs) in container instances and images; (3) discover sensitive personal data stored in buckets S3. Which trio corresponds, in order, to these needs?",
      "options": [
        "AWS WAF, AWS Shield and AWS Firewall Manager",
        "AWS Configure, AWS CloudTrail and Amazon CloudWatch",
        "Amazon GuardDuty, Amazon Inspector and Amazon Macie",
        "Amazon Macie, Amazon GuardDuty and Amazon Inspector"
      ],
      "explanation": "GuardDuty is threat detection based on logs and intelligence; Inspector is a vulnerability scan in workloads (EC2, ECR, Lambda); Macie it is discovered and classification of sensitive data in S3 — each covers a distinct layer.",
      "hint": "Memorize division: malicious behavior → guard; software vulnerability → inspector; personal data → detective of PII.",
      "optionRationales": [
        "GuardDuty is threat detection based on logs and intelligence; Inspector is a vulnerability scan in workloads (EC2, ECR, Lambda); Macie it is discovered and classification of sensitive data in S3 — each covers a distinct layer.",
        "The order is changed: Macie does not detect log threats, GuardDuty does not sweep CVEs and Inspector does not classify personal data.",
        "This trio protects web applications against attacks and centralizes firewall rules — none of them covers log detection, CVEs or PII no S3.",
        "These services deal with configuration compliance, API auditing and observability — they are not the specialized detection tools mentioned above."
      ]
    },
    "saa-med-021": {
      "text": "A web and mobile application needs two distinct features of Amazon Cognito: (1) register and authenticate end-users, issuing tokens after login; and (2) exchange these tokens for credentials AWS temporary for the app to send photos directly to a pocket S3. What components of Cognito fulfil each paper, respectively?",
      "options": [
        "User Pools for user authentication and Identity Pools for issuing credentials AWS temporary",
        "IAM Identity Center for both roles",
        "AWS STS directly in the application with built-in keys",
        "Identity Pools for login and User Pools for credentials AWS"
      ],
      "explanation": "User Pool is the user directory that manages registration and login (issuing tokens); Identity Pool federates these identities and exchanges tokens for credentials AWS temporary to access services such as the S3.",
      "hint": "A pool takes care of 'who you are' (login); the other takes care of 'what you can touch' AWS' (temporary credentials).",
      "optionRationales": [
        "User Pool is the user directory that manages registration and login (issuing tokens); Identity Pool federates these identities and exchanges tokens for credentials AWS temporary to access services such as the S3.",
        "The roles are reversed: the login directory is the User Pool; who delivers credentials AWS is the Identity Pool.",
        "The Identity Center assists the workforce (staff accessing accounts) AWS), no end-users of applications.",
        "Embedting access keys into the application is a serious failure: the correct flow uses Cognito to authenticate and only then obtain temporary credentials."
      ]
    },
    "saa-med-022": {
      "text": "A key KMS managed by the client in account A needs to be used by account B to decipher shared data. What combination of settings enables this use between accounts?",
      "options": [
        "Making the key public for the whole organization",
        "Export the private key to KMS and send it to account B",
        "Allow key policy B account in A account and grant permissions KMS corresponding to account B identities via IAM",
        "Just create a role in the B account with permissions KMS, without changing the key policy"
      ],
      "explanation": "The use of cross-account keys KMS requires both tips: key policy key must allow the external main, and identity in the other account needs permissions IAM for cryptographic operations.",
      "hint": "Access between accounts in KMS is a handshake: DA policy CHAVE authorises on one side, IAM other authorization.",
      "optionRationales": [
        "The use of cross-account keys KMS requires both tips: key policy key must allow the external main, and identity in the other account needs permissions IAM for cryptographic operations.",
        "Keys KMS cannot be exported — cryptographic material remains protected within the service, and sharing is done by permissions.",
        "There is no 'make public' key KMS; the authorization is granular by main in the policies, and open too much would violate the least privilege.",
        "Permissions IAM in account B alone are not enough: key policy key in account A also needs to authorize external access."
      ]
    },
    "saa-med-023": {
      "text": "After an incident with a volume created without encryption, the company wants to ensure that TODO new volume EBS created in a region be automatically encrypted, without depending on each developer remembering to mark the option.?",
      "options": [
        "Convert volumes to Forum Store, which is encrypted by default",
        "Enable encryption by default of EBS (EBS encryption by default) in account configuration for that region",
        "Send a statement asking everyone to encrypt manually",
        "Encrypt data only in the application layer"
      ],
      "explanation": "The default encryption configuration forces that all new volumes and snapshot copies in the Region be automatically encrypted, eliminating the human forget factor.",
      "hint": "There is a 'regional switch' that makes the encryption of EBS obligatory for everything that is created from there on.",
      "optionRationales": [
        "The default encryption configuration forces that all new volumes and snapshot copies in the Region be automatically encrypted, eliminating the human forget factor.",
        "Controls based only on human discipline fail — the requirement calls for automatic platform warranty.",
        "Application encryption is complementary, but does not prevent volume creation EBS without storage encryption.",
        "Forum Store is ephemeral storage with its own characteristics — is not a substitute for EBS does not meet the standard cryptography requirement of persistent volumes."
      ]
    },
    "saa-med-024": {
      "text": "The compliance team wants that, whenever the AWS Configure detect one Security Group outside the standard (ex: door) 22 open for 0.0.0.0/0), an automatic correction is applied without human intervention. AWS Config supports this flow?",
      "options": [
        "Generating a monthly report on PDF for the administrator to act",
        "By associating with the config rule an automatic remediation action that performs an automation document of the Systems Manager",
        "Config fixes any feature on its own, without additional configuration",
        "Blocking the entire account until manual correction"
      ],
      "explanation": "Config allows you to link automatic fixes to the rules: when you detect the non-compliant feature, it triggers a runbook of the Systems Manager Automation that fixes the configuration — closing the detect-correct cycle.",
      "hint": "Detection (Config) + correction runbook (Systems Manager Automation) = conformity that is fixed alone.",
      "optionRationales": [
        "Config allows you to link automatic fixes to the rules: when you detect the non-compliant feature, it triggers a runbook of the Systems Manager Automation that fixes the configuration — closing the detect-correct cycle.",
        "By default config only assesses and reports compliance; automatic correction needs to be explicitly configured with remedial actions.",
        "Periodic reports would leave the resource vulnerable until reading — the requirement is immediate and automatic correction.",
        "Config does not suspend accounts; besides disproportionate, this would interrupt all workloads by a rule of SG."
      ]
    }
  }
}
