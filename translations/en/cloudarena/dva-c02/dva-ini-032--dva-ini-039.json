{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "dva-c02",
    "range": "dva-ini-032..dva-ini-039",
    "generator": "offline-marian-faithful"
  },
  "questions": {
    "dva-ini-032": {
      "optionReasons": {
        "dva-ini-032:option:0": "It's the closest because the Trusted Advisor is the dev tool of AWS — but it gives recommendations for good account practice, does not define/test/implant serverless applications; the framework to declare Lambda/API/DynamoDB in simplified template, local testing and deployment is the AWS SAM.",
        "dva-ini-032:option:1": "Snow Family is for physical data transfer — nothing to do with serverless development.",
        "dva-ini-032:option:2": "WorkSpaces is managed virtual desktop — no framework serverless.",
        "dva-ini-032:option:3": ""
      },
      "finalJustifications": [
        "Because AWS SAM define Lambda, APIs and DynamoDB in a simplified template, tests functions locally (sam local) and deploys with few commands — the serverless framework of AWS.",
        "Because AWS Trusted Advisor define Lambda, APIs and DynamoDB in a simplified template, tests the functions locally and deploys with few commands — the serverless framework of AWS.",
        "Because AWS Snow Family define Lambda, APIs and DynamoDB in a simplified template, tests the functions locally and deploys with few commands — the serverless framework of AWS.",
        "Because Amazon WorkSpaces defines Lambda, APIs and DynamoDB in a simplified template, tests the functions locally and deploys with few commands — the serverless framework of AWS."
      ]
    },
    "dva-ini-033": {
      "optionReasons": {
        "dva-ini-033:option:0": "",
        "dva-ini-033:option:1": "It's the closest because the Scan REALMENTE can read in parallel (parallel scan) — but it still READ A TABELA INTEIRA and filters later (caro and slow), while Query only reads the target partition by the key; so Query is more efficient, not Scan.",
        "dva-ini-033:option:2": "Query and Scan are NOT identical — they differ in what they read (target partition vs full table)).",
        "dva-ini-033:option:3": "Query is not limited to tables <1 GB — works in any size, reading by partition key."
      },
      "finalJustifications": [
        "Because Query looks for the partition key by reading only the target partition, and Scan reads the entire table by filtering it later — so Query is much more efficient and cheaper.",
        "Because Scan is faster by reading parallel by default, while Query only reads target partition — so Scan is generally preferred for efficiency and cost.",
        "Because Query and Scan are identical, changing only the name in SDK — both read by partition key, so the recommendation to prefer Query is just convention.",
        "Because Query only works on tables with less than 1 GB reading the target partition, and the Scan reads the whole table — so Query is preferred when it fits the limit."
      ]
    },
    "dva-ini-034": {
      "optionReasons": {
        "dva-ini-034:option:0": "Elasticity is scaling capacity — has no relation to avoiding double collection by retry.",
        "dva-ini-034:option:1": "",
        "dva-ini-034:option:2": "Pagination is dividing results into pages — nothing to do with the duplicate requisition problem.",
        "dva-ini-034:option:3": "It is the closest because atomicity is the concept of related transaction — but it deals with the 'everything-or-nothing' of grouped operations, not to make a request REPETIDA harmless; ensuring that reprocessing the same request does not cover again is IDEMPOTENCE."
      },
      "finalJustifications": [
        "Because Idempotence causes the repeated operation to produce the same result — with a unique requisition identifier verified before effects, the same recovery is not applied twice.",
        "Because atomicity makes the repeated operation produce the same result — with a unique requisition identifier verified before effects, the same charge is not applied twice.",
        "Because elasticity makes the repeated operation produce the same result — with a unique requisition identifier verified before the effects, the same charge is not applied twice.",
        "Because the pagination makes the repeated operation produce the same result — with a unique requisition identifier verified before the effects, the same charge is not applied twice."
      ]
    },
    "dva-ini-035": {
      "optionReasons": {
        "dva-ini-035:option:0": "There is no 'e-mail subscription' between SQS and Lambda — integration is the event source mapping.",
        "dva-ini-035:option:1": "It's the closest because a cron in one EC2 it would also invoke the function periodically — but demands MANTER a server and write the polling/lot, the opposite of 'without writing the loop'; connecting the row to the function with polling and managed batches is the event source mapping.",
        "dva-ini-035:option:2": "",
        "dva-ini-035:option:3": "Making the line public doesn't do the Lambda 'find it' — and it's insecure; the link is made by event source mapping."
      },
      "finalJustifications": [
        "Because the event source mapping does the Lambda do row polling SQS and invoke the function with batches of messages — without the developer writing the polling loop.",
        "Because the cron in one EC2 does the Lambda do row polling SQS and invoke the function with batches of messages — without the developer writing the polling loop in the application.",
        "Because e-mail signature does the Lambda do row polling SQS and invoke the function with batches of messages — without the developer writing the polling loop in the application.",
        "Because public access in the line does the Lambda do row polling SQS and invoke the function with batches of messages — without the developer writing the polling loop in the application."
      ]
    },
    "dva-ini-036": {
      "optionReasons": {
        "dva-ini-036:option:0": "",
        "dva-ini-036:option:1": "It's the closest because the API Gateway REST it's also API managed — but it exposes APIs REST, no GraphQL, and does not aggregate sources in a single scheme nor does it offer native real-time subscriptions; API GraphQL managed with subscriptions is the AWS AppSync.",
        "dva-ini-036:option:2": "CloudFront Functions light logic wheel on edge — not a API GraphQL.",
        "dva-ini-036:option:3": "AWS Glue It's ETL Data — not service API for customers."
      },
      "finalJustifications": [
        "Because AWS AppSync is the API Managed GraphQL: aggregates data from DynamoDB/Lambda in a schema and offers real-time subscriptions to customers — exactly the order.",
        "Because API Gateway REST is the API Managed GraphQL: aggregates data from DynamoDB/Lambda in a schema and offers real-time subscriptions to customers — exactly the order.",
        "Because CloudFront Functions is the API Managed GraphQL: aggregates data from DynamoDB/Lambda in a schema and offers real-time subscriptions to customers — exactly the order.",
        "Because AWS Glue is the API Managed GraphQL: aggregates data from DynamoDB/Lambda in a schema and offers real-time subscriptions to customers — exactly the order."
      ]
    },
    "dva-ini-037": {
      "optionReasons": {
        "dva-ini-037:option:0": "",
        "dva-ini-037:option:1": "It's the closest because it mentions credentials, present in every call — but SDKs DO NOT 'change keys' when they receive transitory error; what they do by default is RETENTAR automatically with exponential backoff and jitter (configurable in number and mode).",
        "dva-ini-037:option:2": "The SDKs do not forward the request to another account — that makes no sense in the treatment of error.",
        "dva-ini-037:option:3": "SDKs do not open call in support for each error — they retain locally with backoff."
      },
      "finalJustifications": [
        "Because the official SDKs already implement automatic retentatives with exponential backoff and jitter for transient errors (such as throttling), configurable in number and retry mode.",
        "Because official SDKs already exchange credentials for new keys for transient errors (such as throttling), configurable in number and mode — standard retry behavior.",
        "Because the official SDKs already send the request to another account AWS in transitory errors (such as throttling), configurable in number and mode — standard retry behavior.",
        "Because official SDKs already open call in support for each transitory error (such as throttling), configurable in number and mode — the standard retry behavior of calls."
      ]
    },
    "dva-ini-038": {
      "optionReasons": {
        "dva-ini-038:option:0": "",
        "dva-ini-038:option:1": "The two are NOT identical in terms of resources and price — they differ exactly from that.",
        "dva-ini-038:option:2": "It is the closest because it touches the axis of PRICE, which is part of the comparison — but it is INVERTIDO: the HTTP APIs are cheaper and less latency for common cases; a REST offers resources EXTRAS (api keys, caching, request validation) for more cost, is not always cheaper'.",
        "dva-ini-038:option:3": "HTTP APIs invoke Lambda normally — including proxy and JWT of the Cognito."
      },
      "finalJustifications": [
        "Because the HTTP APIs are simpler, cheaper and less latency for common cases (proxy Lambda, JWT of the Cognito), while the REST APIs bring extras like api keys, caching and request validation.",
        "Because both are identical in resources and price for common cases (proxy Lambda, JWT of the Cognito), with REST and HTTP offering api keys, caching and request validation in the same way.",
        "Because the REST APIs are always cheaper for common cases (proxy Lambda, JWT of the Cognito), while the HTTP bring extras like api keys, caching and request validation for more cost.",
        "Because the HTTP APIs do not invoke Lambda for common cases (proxy, JWT of the Cognito), while the REST Bring it in. api keys, caching and request validation and cover serverless integration."
      ]
    },
    "dva-ini-039": {
      "optionReasons": {
        "dva-ini-039:option:0": "It is the closest because it associates 'more load' with 'greatest instance' — but the Lambda NOT 'Change to one EC2 major': it scales HORIZONTALMENTE creating executions CONCORRENTES additional for simultaneous requests, within the limit of account competition.",
        "dva-ini-039:option:1": "There's no clusters of the Lambda' with servers to be added by hand — the scaling is automatic and managed.",
        "dva-ini-039:option:2": "Extra requests are not discarded 'silently' — if it exceeds competition, there is throttling (429) reported, not silence.",
        "dva-ini-039:option:3": ""
      },
      "finalJustifications": [
        "Because Lambda Automatically scale creating additional competing executions to meet simultaneous requests — within account competition limits.",
        "Because Lambda changes to one EC2 increased automatically to meet simultaneous requests — within the account's competition limits when doubling traffic.",
        "Because the developer adds servers to the cluster of Lambda to meet simultaneous requests — within the account's competition limits when doubling traffic.",
        "Because the extra requests are silently discarded until traffic falls, taking into account the simultaneous possible — within the limits of the account competition."
      ]
    }
  }
}
