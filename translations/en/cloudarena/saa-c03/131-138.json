{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "saa-c03",
    "range": "131-138",
    "generator": "offline-marian-faithful"
  },
  "questions": {
    "saa-avc-131": {
      "optionReasons": {
        "saa-avc-131:option:0": "It's the closest because latency routing Route 53 also send the player to NLB great regional — but resolves by DNS (without static IP) and traffic still crosses the INTERNET public until NLB, keeping the route unforeseeable; entering the network of AWS on the edge, with static IPs, is the Global Accelerator.",
        "saa-avc-131:option:1": "CloudFront speed up content HTTP caching — is not the ideal front for real-time gaming traffic.",
        "saa-avc-131:option:2": "",
        "saa-avc-131:option:3": "Direct Connect from each player's home is unfeasible — it's for corporate data centers."
      },
      "finalJustifications": [
        "Because Global Accelerator delivery static anycast IPs and takes traffic to the global network of AWS on the nearest edge: stable latency, predictable route and IPs do not change if the backend changes.",
        "Because latency routing Route 53 delivery static anycast IPs and takes traffic to the global network of AWS on the edge: stable latency, predictable route and IPs do not change if the backend changes.",
        "Because CloudFront in front of the NLBs deliver static IPs anycast and takes traffic to the global network of AWS on the edge: stable latency, predictable route and IPs do not change if the backend changes.",
        "Because Direct Connect delivery static anycast IPs and takes traffic to the global network of AWS on the edge: stable latency, predictable route and IPs do not change if the backend changes."
      ]
    },
    "saa-avc-132": {
      "optionReasons": {
        "saa-avc-132:option:0": "It's the closest because Reserved Instances in fact reduce cost — but only by COMPROMISSO price no MESMO x86; does not improve performance per dollar by changing the ARQUITETURA of CPU, which is what the scenario asks and the load (ARM64-ready) allows; this is migrating to Graviton.",
        "saa-avc-132:option:1": "",
        "saa-avc-132:option:2": "Enhanced Networking improves network throughput — does not change the price-performance of CPU Computational Load.",
        "saa-avc-132:option:3": "Migrate everything to Lambda rewrites the execution model — it's not just 'change the architecture of CPU' maintaining the application."
      },
      "finalJustifications": [
        "Because migrating to Graviton instances (e.g. m7g.xlarge) changes to CPU for ARM64, that the application already supports, improving performance per dollar without changing the architecture and the amount of instances.",
        "Why migrate to Reserved Instances x86 change to CPU for ARM64, that the application already supports, improving performance per dollar without changing the architecture and the amount of instances.",
        "Why enable Enhanced Networking to exchange CPU for ARM64, that the application already supports, improving performance per dollar without changing the architecture and the amount of instances.",
        "Why move the load to Lambda change to CPU for ARM64, that the application already supports, improving performance per dollar without changing the architecture and the amount of instances."
      ]
    },
    "saa-avc-133": {
      "optionReasons": {
        "saa-avc-133:option:0": "Spark self-managed with local instance store loses data in closing and throws out management of EMR.",
        "saa-avc-133:option:1": "It's the closest because instance groups with HDFS is a configuration REAL of EMR — but uses a ONLY type of instance (without the flexibility of types/purchase requested) and saves the data in HDFS LOCAL, which disappears when the cluster is completed; persistence independent of the cluster + flexible fleet is EMRFS no S3 + Fleets Forum.",
        "saa-avc-133:option:2": "",
        "saa-avc-133:option:3": "Single cluster 24/7 with HDFS in EBS does not scale dynamically and pays idle time — the opposite of the target."
      },
      "finalJustifications": [
        "Because EMRFS direct recording in S3 (data persist after finishing the cluster) and the Fleets Forum mix types and On-demand/Spot with dynamic scale — availability, performance and persistence together.",
        "Because the instance groups record directly in S3 (data persist after finishing the cluster) and mix types and On-demand/Spot with dynamic scale — availability, performance and persistence together.",
        "Because the Spark self-managed directly records on S3 (data persist after finishing the cluster) and mix types and On-demand/Spot with dynamic scale — availability, performance and persistence together.",
        "Because the unique cluster 24/7 direct recording in S3 (data persist after finishing the cluster) and mix types and On-demand/Spot with dynamic scale — availability, performance and persistence together."
      ]
    },
    "saa-avc-134": {
      "optionReasons": {
        "saa-avc-134:option:0": "It's the closest because Partition Placement Group also groups instances and serves distributed loads — but it separates the fleet into PARTITIONS into distinct hardware for ISOLAR faults (HDFS, Kafka), which increases latency between partitions; lower latency with physically close instances in an AZ is the Cluster Placement Group.",
        "saa-avc-134:option:1": "Spreading multiple AZs adds latency between zones — the opposite of the requirement of HPC strongly coupled.",
        "saa-avc-134:option:2": "Spread Placement Group pushes separate hardware instances away from availability — worsening latency between us.",
        "saa-avc-134:option:3": ""
      },
      "finalJustifications": [
        "Because the Cluster Placement Group groups the physically close instances into a single AZ, delivering the lowest latency and the largest network throughput between them — ideal for the MPI strongly coupled.",
        "Because the Partition Placement Group groups the physically close instances into a single AZ, delivering the lowest latency and the largest network throughput between them — ideal for the MPI strongly coupled.",
        "Because the Spread Placement Group groups the physically close instances into a single AZ, delivering the lowest latency and the largest network throughput between them — ideal for the MPI strongly coupled.",
        "Because Auto Scaling Group multi-AZ groups the physically close instances into a single AZ, delivering the lowest latency and the largest network throughput between them — ideal for the MPI strongly coupled."
      ]
    },
    "saa-avc-135": {
      "optionReasons": {
        "saa-avc-135:option:0": "Switching GPUs by C family takes the accelerator needed for training — solves the wrong problem.",
        "saa-avc-135:option:1": "It's the closest because the ENA standard is already the high network performance of AWS — But he goes through the pile TCP/IP of the operating system; the described gargal is precisely this overhead, and the requirement asks for OS-BYPASS for NCCL/MPI: This is the Elastic Fabric Adapter (EFA), not ENA alone.",
        "saa-avc-135:option:2": "",
        "saa-avc-135:option:3": "Jumbo Frames reduces overhead per package, but continues in stack TCP/SO IP — does not deliver the OS-bypass of EFA."
      },
      "finalJustifications": [
        "Because EFA (with Cluster Placement Group) OS-bypass no NCCL/MPI: surrounds the pile TCP/IP and delivers the lowest latency between us and the largest throughput for strongly coupled communication.",
        "Because ENA pattern does OS-bypass no NCCL/MPI: surrounds the pile TCP/IP and delivers the lowest latency between us and the largest throughput for strongly coupled communication of training.",
        "Because the C family does OS-bypass no NCCL/MPI: surrounds the pile TCP/IP and delivers the lowest latency between us and the largest throughput for strongly coupled communication of training.",
        "Because the Jumbo Frames make OS-bypass no NCCL/MPI: surround the pile TCP/IP and deliver the lowest latency between us and the largest throughput for strongly coupled communication."
      ]
    },
    "saa-avc-136": {
      "optionReasons": {
        "saa-avc-136:option:0": "Switching language reduces the cold start a bit — does not guarantee environments always ready in low traffic.",
        "saa-avc-136:option:1": "More memory speeds up execution and a little init — does not eliminate cold start or preheat environments.",
        "saa-avc-136:option:2": "",
        "saa-avc-136:option:3": "It's the closest because Reserved Competition looks like the competition mechanism of the function — but it's just RESERVA (and limits) a ceiling of simultaneous executions; DO NOT maintain pre-initialized environments: cold starts continue; maintain a number-base already heated and ready is the Provided Competition."
      },
      "finalJustifications": [
        "Because Provided Competition maintains a number-base of pre-initiated and ready-to-respond environments immediately, including low traffic — and the Application Auto Scaling adjusts that level.",
        "Because Reserved Competition maintains a base number of pre-initiated and ready to respond immediately, including low traffic — and the Application Auto Scaling adjusts that level.",
        "Because switching to a compiled language keeps a number-base of pre-initiated environments ready to respond immediately, including low traffic, eliminating the cold start of p99.",
        "Because maximizing memory keeps a base number of pre-initiated environments ready to respond immediately, including low traffic, eliminating the cold start of p99."
      ]
    },
    "saa-avc-137": {
      "optionReasons": {
        "saa-avc-137:option:0": "",
        "saa-avc-137:option:1": "Lambda@Edge no 'origin request' only rotates in cache MISS, after evaluation of the behaviors — too late to rewrite the URI before cache.",
        "saa-avc-137:option:2": "It's the closest because Lambda@Edge in the viewer request also runs on the edge before cache and can rewrite the URI — but it is heavier and more expensive, with latency of milliseconds and limits of scale; for light operation, sub-milisegund and very high volume (without body, net or disk), the right is CloudFront Functions not see request.",
        "saa-avc-137:option:3": "Doing at source discards the goal of running in BORDA in each request — the origin is not reached in cache hit."
      },
      "finalJustifications": [
        "Because the CloudFront Functions run in the viewer request with sub-millimeter overhead and minimal cost in very high volume: inspect the header and rewrite the URI Before the Behavior caches.",
        "Because Lambda@Edge in viewer request wheel with sub-millimeter head and minimum cost in very high volume: inspect the header and rewrite the URI Before the Behavior caches.",
        "Because Lambda@Edge in the original request wheel with sub-millimeter head and minimum cost in very high volume: inspect the header and rewrite the URI Before the Behavior caches.",
        "Because the logic in origin runs with sub-millimeter head and minimal cost in very high volume: inspects the header and rewrites the URI before the behavior caches of CloudFront."
      ]
    },
    "saa-avc-138": {
      "optionReasons": {
        "saa-avc-138:option:0": "It's the closest because the Redis cluster mode disabled also serves key-value cache — but the Redis is single-thread per node (does not use CPU multi-core as requested) and brings persistence, replication and complex structures that the scenario says NOT to need; simple key-value cache, multi-thread and horizontally scalable is Memcached.",
        "saa-avc-138:option:1": "",
        "saa-avc-138:option:2": "Redis cluster mode enabled with Multi-AZ adds sharding, replication and failover — explicitly unnecessary features.",
        "saa-avc-138:option:3": "DAX is specific cache of DynamoDB — does not serve as a layer in front of a bank RELACIONAL."
      },
      "finalJustifications": [
        "Because Memcached is the simplest key-value cache: multi-thread (uses all the colors of the node) and scales horizontally with many small knots, without persistence, replication or complex structures.",
        "Because the Redis cluster mode disabled is the simplest key-value cache: multi-thread (uses all the colors of the node) and scales horizontally with many small knots, without persistence or replication.",
        "Because the Redis cluster mode enabled is the simplest key-value cache: multi-thread (uses all the colors of the node) and scales horizontally with many small knots, without persistence or replication.",
        "Because DAX is the simplest key-value cache: multi-thread (uses all colors of the node) and scales horizontally with many small knots, without persistence, replication or complex structures."
      ]
    }
  }
}
