{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "saa-c03",
    "range": "123-130",
    "generator": "offline-marian-faithful"
  },
  "questions": {
    "saa-avc-123": {
      "optionReasons": {
        "saa-avc-123:option:0": "",
        "saa-avc-123:option:1": "Three federated clusters is to rebuild in hand what the EKS already managed delivery — multi-AZ plan control is native.",
        "saa-avc-123:option:2": "Control plane self-managed in EC2 contrary to the requirement not to manage the infrastructure of the plan control.",
        "saa-avc-123:option:3": "It's the closest because the Fargate distributes pods for several AZs without managing us — but responds only to part (b) and still leaves the (a) unaddressed; moreover, 'exclusively Fargate for everything' is strong restriction; the requested design is the multi-AZ (automatic) managed plan control + Managed Node Group in several AZs."
      },
      "finalJustifications": [
        "Because the management plan control of EKS already running multi-AZ by default (a), and a Managed Node Group in several sub-networks/AZs distributes workers nodes and replaces unhealthy ones via Auto Scaling (b).",
        "Because control plans on 3 federated clusters already run multi-AZ by default (a), and the Managed Node Group in several AZs distributes workers nodes and replaces unhealthy ones via Auto Scaling (b).",
        "Because the self-managed control plane in EC2 already running multi-AZ by default (a), and the Managed Node Group in several AZs distributes workers nodes and replaces unhealthy ones via Auto Scaling (b).",
        "Because control plans with Fargate exclusive multi-AZ wheel by default (a), and profiles Fargate in several AZs distribute workers nodes and replace unhealthy ones via Auto Scaling (b)."
      ]
    },
    "saa-avc-124": {
      "optionReasons": {
        "saa-avc-124:option:0": "It's the closest because the CRR also replicates objects automatically between pockets/accounts — but CRR presupposes regions DIFERENTES; 'setting destination equal to origin' is not how to set replication in MESMA region: the own resource for this is the Same-Region Replication (SRR), which supports destination in another account.",
        "saa-avc-124:option:1": "DataSync synchronizes under schedule/execution — is not the continuous and native replication of new objects between the accounts of the S3.",
        "saa-avc-124:option:2": "",
        "saa-avc-124:option:3": "Read the Account A newsletter directly does not create COPY in Account B — violates the required account/property segregation."
      },
      "finalJustifications": [
        "Because SRR replicates each new Account A object to the Account B pocket in the same region (with versioning both), and can change ownership and use RTC — without latency or cost between regions.",
        "Because the CRR with destination equal to origin replicates each new Account A object to Account B in the same region (with versioning both), and can change ownership — without cost between regions.",
        "Because DataSync replicates each new Account A object to the Account B pocket in the same region (with versioning both), and can change ownership and use RTC — without latency between regions.",
        "Because cross-account reading replicates each new Account A object to Account B's pocket in the same region (with versioning in both), and can change ownership — without cost between regions."
      ]
    },
    "saa-avc-125": {
      "optionReasons": {
        "saa-avc-125:option:0": "Change ALB by NLB changes the balancer — does not reduce the amount of Miss cache requests at source.",
        "saa-avc-125:option:1": "Multiple distributions per region multiply origins and complexity — does not consolidate the missions on one origin.",
        "saa-avc-125:option:2": "",
        "saa-avc-125:option:3": "It's the closest because TTL higher REALMENTE reduces departures to origin — but it ALTERA the behavior of cache per object (what the statement vetoes) and does not solve the problem described: several edges still make miss cache simultaneously for the MESMO object; consolidating these requests into an intermediate layer is Origin Shield."
      },
      "finalJustifications": [
        "Because the Origin Shield adds a central cache layer near the source: the miss caches of the edges converge on it, and the origin receives a single request per object — without changing the cache per object.",
        "Why increase the TTL the maximum adds a layer of central cache near the origin: the miss caches of the edges converge on it, and the origin receives a single request per object.",
        "Why change the ALB by NLB adds a central cache layer near the source: the miss caches of the edges converge on it, and the origin receives a single request per popular object.",
        "Because multiple distributions per region add a layer of central cache near the origin: the miss caches of the edges converge on it, and the origin receives a single request per object."
      ]
    },
    "saa-avc-126": {
      "optionReasons": {
        "saa-avc-126:option:0": "More scale reading replicas LEITURA — does not distribute written or increases the total memory of the dataset.",
        "saa-avc-126:option:1": "Memcached does not persist session in the same way or replica — migration changes model and loses Redis resources.",
        "saa-avc-126:option:2": "It's the closest because a larger node relieves memory and throughput for a while — but it's scaling VERTICAL: there is a ceiling of size and all writings continue in a single primary; solving dataset E throughput writing horizontally requires sharding with Cluster Mode Enabled.",
        "saa-avc-126:option:3": ""
      },
      "finalJustifications": [
        "Because Cluster Mode Enabled distributes data by multiple shards: each shard has its primary, adding memory to the dataset and parallelizing the writings — horizontal scaling of the two gargalos.",
        "Because the larger node distributes the data by multiple shards: each shard has its primary, adding memory to the dataset and parallelizing the writings — horizontal scaling of the two gargalos.",
        "Because more read replicas distribute data by multiple shards: each shard has its primary, adding memory to the dataset and parallelizing the writings — horizontal stratification of the gargalos.",
        "Because migrating to Memcached distributes the data by multiple shards: each shard has its primary, adding memory to the dataset and parallelizing the writings — horizontal stratification of the gargalos."
      ]
    },
    "saa-avc-127": {
      "optionReasons": {
        "saa-avc-127:option:0": "Forum Store is ephemeral — unacceptable for transactional bank (lose data on stop/failure).",
        "saa-avc-127:option:1": "",
        "saa-avc-127:option:2": "It's the closest because it provides more IOPS in yo2 is the right instinct — but the ceiling above 64.000 IOPS only reached with i2 BLOCK EXPRESS in instance that support; in the current instance SEM Support for Block Express, increase the number provided is no more than the limit; Io2 Block Express is required in an eligible Nitro type (e.g. R5b)).",
        "saa-avc-127:option:3": "RAID 0 of gp3 add IOPS, but without consistent sub-ms latency and low io2 Block Express error rate — and the RAID 0 has no redundancy."
      },
      "finalJustifications": [
        "Because the Io2 Block Express in an eligible Nitro type (e.g.: R5b) breaks the roof of 64.000 IOPS with consistent sub-millimeter latency and very low error rate — exactly what the transactional bank requires.",
        "Why increase the IOPS provided from the current yo2 breaks the roof of 64.000 with consistent sub-millimeter latency and very low error rate — exactly what the transactional bank requires.",
        "Because the NVMe Forum Store breaks the roof of 64.000 IOPS with consistent sub-millimeter latency and very low error rate — exactly what the transactional bank requires, without losing data.",
        "Because RAID 0 of volumes gp3 breaks or roof of 64.000 IOPS with consistent sub-millimeter latency and very low error rate — exactly what the transactional bank requires, redundancy."
      ]
    },
    "saa-avc-128": {
      "optionReasons": {
        "saa-avc-128:option:0": "",
        "saa-avc-128:option:1": "It's the closest because regional bucks REALMENTE they bring the upload of users from Asia/Australia closer — but the statement explicitly vetoes creating buckets by region and making the client choose the endpoint; speed up uploading to MESMO network-based AWS, transparent to the user, is the Transfer Acceleration.",
        "saa-avc-128:option:2": "Direct Connect from each end user is unfeasible — it is for data centers, not for uploaders scattered around the world.",
        "saa-avc-128:option:3": "Larger parts in the multipart adjust the upload mechanics — do not change the slow path through the internet until us-east-1."
      },
      "finalJustifications": [
        "Because the Transfer Acceleration sends the upload to the nearest edge and follows the network of the AWS until us-east-1 — faster and more reliable for remote users, in the same pocket and without manual choice.",
        "Because separate regional buckets route the upload to the nearest edge and follow through the network of AWS until us-east-1 — faster for remote users, in the same pocket and without manual choice.",
        "Because Direct Connect forwards the upload to the nearest edge and follows the network of AWS until us-east-1 — faster and more reliable for remote users, in the same pocket and without manual choice.",
        "Because larger parts in the multipart route upload to the nearest edge and then to the network of the AWS until us-east-1 — faster for remote users, in the same pocket and without manual choice."
      ]
    },
    "saa-avc-129": {
      "optionReasons": {
        "saa-avc-129:option:0": "",
        "saa-avc-129:option:1": "Route 53 weighted by replica requires updating weights by hand to each change — the problem you want to eliminate.",
        "saa-avc-129:option:2": "It's the closest because a NLB in front of the replicas would also distribute connections — but it is extra infrastructure to manage and NLB does not know the cluster topology (addition/reply removal, reader role); Aurora already exposes the READER ENDPOINT, which automatically balances between the current replicas.",
        "saa-avc-129:option:3": "The cluster/writer endpoint points to the PRIMARY — sending reading there does not use any replica."
      },
      "finalJustifications": [
        "Because the endpoint reader of Aurora balances the consultations only reading between TODAS the current replicas automatically — add or remove replica does not require change in application.",
        "Because NLB in front of the replicas balances the consultations only reading between the TODAS the current replicas automatically — add or remove replica does not require change in application.",
        "Because Route 53 weighted balances queries only reading between TODAS the current replicas automatically — add or remove replica does not require change in application.",
        "Because the cluster/writer endpoint balances the queries only reading between TODAS the current replicas automatically — add or remove replica does not require change in application."
      ]
    },
    "saa-avc-130": {
      "optionReasons": {
        "saa-avc-130:option:0": "GSI creates index for OUTRO query pattern — does not speed up key reading of hot items or lower latency to microseconds.",
        "saa-avc-130:option:1": "Cache in a second table DynamoDB still delivers millisecond latency — not enough microseconds.",
        "saa-avc-130:option:2": "It is the closest because more RCUs/On-Demand responds to the reading volume — but does not change the LATENCIA: the DynamoDB delivers a few milliseconds, and the requirement is MICROSSEGUNDOS for hot items; and the cost continues high in repeated readings; memory cache with microsecond latency is the DAX.",
        "saa-avc-130:option:3": ""
      },
      "finalJustifications": [
        "Because DAX it is cached in memory in front of the table: repeated readings of hot items come out in microseconds and do not touch the table, cutting latency and cost — and tolerates the eventual consistency requested.",
        "Why increase the RCUs puts a cache in memory in front of the table: the readings of hot items come out in microseconds and do not touch the table, cutting latency and cost — tolerating the eventual consistency.",
        "Because GSI put a cache in memory in front of the table: the readings of hot items come out in microseconds and do not touch the table, cutting latency and cost — tolerating the eventual consistency requested.",
        "Because the cache table with Streams puts a cache in memory in front of the table: the readings of hot items come out in microseconds and do not touch the original, cutting latency and cost."
      ]
    }
  }
}
