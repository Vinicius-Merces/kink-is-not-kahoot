{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "clf-c02",
    "range": "clf-ini-079..clf-ini-086",
    "generator": "offline-marian-faithful"
  },
  "questions": {
    "clf-ini-079": {
      "optionReasons": {
        "clf-ini-079:option:0": "",
        "clf-ini-079:option:1": "AWS Lambda performs serverless functions per event — is not the long-term Docker container orchestrator.",
        "clf-ini-079:option:2": "EC2 Auto Scaling adjusts the amount of instances — does not orchestrate the execution of Docker containers.",
        "clf-ini-079:option:3": "It's the closest because the EKS also orchestrates scaled containers in the AWS — but he runs the Kubernetes (market open-source standard), not the orchestrator NATIVO and owner of AWS; the solution of the AWS to orchestrate containers is the Amazon ECS."
      },
      "finalJustifications": [
        "Because Amazon ECS is the native container orchestrator and owner of AWS — managing the execution of Docker containers on a corporate scale, exactly the specification requested.",
        "Because AWS Lambda is the native container orchestrator and owner of AWS — managing the execution of Docker containers on a corporate scale, exactly the specification requested.",
        "Because Amazon EC2 Auto Scaling is the native container orchestrator and owner of AWS — managing the execution of Docker containers on a corporate scale, exactly the specification requested.",
        "Because Amazon EKS is the native container orchestrator and owner of AWS — managing the execution of Docker containers on a corporate scale, exactly the specification requested."
      ]
    },
    "clf-ini-080": {
      "optionReasons": {
        "clf-ini-080:option:0": "Elastic Beanstalk implements and manages the application (and can use scaling below) — but the service that performs the dynamic capacity adjustment is the Auto Scaling.",
        "clf-ini-080:option:1": "It's the closest because the ELB acts along with the fleet under high load — but he DISTRIBUI traffic between existing instances, not ADICIONA nor REMOVE machines as follows: CPU; dynamically adjust the quantity of the fleet is the AWS Auto Scaling.",
        "clf-ini-080:option:2": "",
        "clf-ini-080:option:3": "CloudWatch collection of metrics (ex.: CPU) and trigger the alarms — but who adds/removes the instances in response is the Auto Scaling."
      },
      "finalJustifications": [
        "Because AWS Auto Scaling add instances EC2 when the CPU reaches critical levels and removes them when traffic falls — the dynamic capacity adjustment for the unpredictable Black Friday peaks.",
        "Because AWS Elastic Beanstalk add instances EC2 when the CPU reaches critical levels and removes them when traffic falls — the dynamic capacity adjustment for the unpredictable Black Friday peaks.",
        "Because Elastic Load Balancing adds instances EC2 when the CPU reaches critical levels and removes them when traffic falls — the dynamic capacity adjustment for the unpredictable Black Friday peaks.",
        "Because Amazon CloudWatch add instances EC2 when the CPU reaches critical levels and removes them when traffic falls — the dynamic capacity adjustment for the unpredictable Black Friday peaks."
      ]
    },
    "clf-ini-081": {
      "optionReasons": {
        "clf-ini-081:option:0": "Route 53 Traffic Flow manages routing DNS — is not a row of messages for asynchronous processing.",
        "clf-ini-081:option:1": "S3 is storage of objects — it is not the managed message queue that guarantees individual and consumer-ordered processing.",
        "clf-ini-081:option:2": "",
        "clf-ini-081:option:3": "It's the closest because the SNS it is also managed service of uncoupled messages — but it does PUSH immediate to several subscribers (pub/sub), without holding the message for the consumer to look for later; FILA durable in which each message is processed in the backend rhythm is the SQS."
      },
      "finalJustifications": [
        "Because Amazon SQS is the queue of durable and persistent messages: the order is lined up and the backend consumes each message at its own pace, without loss in case of bread — the asynchronous processing requested.",
        "Because Route 53 Traffic Flow is the durable and persistent message queue: the order is lined up and the backend consumes each message at its own pace, without loss in case of bread — asynchronous processing.",
        "Because Amazon S3 is the queue of durable and persistent messages: the order is lined up and the backend consumes each message at its own pace, without loss in case of bread — the asynchronous processing requested.",
        "Because Amazon SNS is the queue of durable and persistent messages: the order is lined up and the backend consumes each message at its own pace, without loss in case of bread — the asynchronous processing requested."
      ]
    },
    "clf-ini-082": {
      "optionReasons": {
        "clf-ini-082:option:0": "Target Group groups destinations for the load balancer forwarding traffic — does not define the setting parameters of the instance.",
        "clf-ini-082:option:1": "",
        "clf-ini-082:option:2": "'Virtual Network Fleets of the VPC' is not a component of Auto Scaling — does not define how the instance is created.",
        "clf-ini-082:option:3": "It is the closest because Target Tracking policy is also configuration of Auto Scaling — But she defines QUANDO scalar (the target metric), not COMO create the instance; the document with AMI, type of instance and keys to launch the new machine is Launch Template."
      },
      "finalJustifications": [
        "Because Launch Template is the central configuration that defines how to create a new instance (ID of AMI, type of instance, safety keys) — the model that Auto Scaling uses when launching machines.",
        "Because Target Group is the central configuration that defines how to create a new instance (ID of AMI, type of instance, safety keys) — the model that Auto Scaling uses when launching machines.",
        "Because the Virtual Network Fleets of VPC are the central configuration that defines how to create a new instance (ID of AMI, type, keys) — the model that Auto Scaling uses when launching machines.",
        "Because Target Tracking Policy is the central configuration that defines how to create a new instance (ID of AMI, type, keys) — the model that Auto Scaling uses when launching machines."
      ]
    },
    "clf-ini-083": {
      "optionReasons": {
        "clf-ini-083:option:0": "",
        "clf-ini-083:option:1": "Step Functions orchestrate workflows in sequential steps — does not make the broadcast simultaneous to multiple subscribers.",
        "clf-ini-083:option:2": "EventBridge Scheduler triggers tasks in scheduled times — it is not the push publication service to multiple destinations at the same time.",
        "clf-ini-083:option:3": "It's the closest because the SQS also uncouples components with messages — but it is a FILA from where ONE consumer pulls each message; spread the MESMA notification simultaneously to several destinations (SMS, email, Lambda) in fan-out is the Amazon SNS."
      },
      "finalJustifications": [
        "Because Amazon SNS publish the same notification simultaneously to multiple independent destinations (SMS, email, Lambda) in fan-out — mass distribution at the time of publication, as projected.",
        "Because AWS Step Functions publish the same notification simultaneously to multiple independent destinations (SMS, email, Lambda) in fan-out — mass distribution at the time of publication.",
        "Because Amazon EventBridge Scheduler publishes the same notification simultaneously to multiple independent destinations (SMS, email, Lambda) in fan-out — mass distribution at the time of publication.",
        "Because Amazon SQS publish the same notification simultaneously to multiple independent destinations (SMS, email, Lambda) in fan-out — mass distribution at the time of publication, as projected."
      ]
    },
    "clf-ini-084": {
      "optionReasons": {
        "clf-ini-084:option:0": "It's the closest because you're fleeting. EC2 On-Demand are also a way to rotate containers in ECS — but require to provide, scale and apply patches in instances; completely eliminate this server management is the serverless engine Fargate.",
        "clf-ini-084:option:1": "OpsWorks Stacks manages configuration with Chef/Puppet — is not the container serverless engine of ECS.",
        "clf-ini-084:option:2": "",
        "clf-ini-084:option:3": "Lambda rotate serverless functions per event — it's not the engine that runs the Docker containers of ECS without running servers."
      },
      "finalJustifications": [
        "Because AWS Fargate is the serverless engine of ECS: rotates containers without providing, scaling or applying patches in instances EC2 — completely eliminating server management, as a request.",
        "Because the Fleets of Entities EC2 On-Demand are the serverless engine of ECS: run containers without providing, scaling or applying patches in instances — completely eliminating server management.",
        "Because AWS OpsWorks Stacks is the serverless engine of ECS: rotates containers without providing, scaling or applying patches in instances EC2 — completely eliminating server management, as a request.",
        "Because AWS Lambda is the serverless engine of ECS: rotates containers without providing, scaling or applying patches in instances EC2 — completely eliminating server management, as a request."
      ]
    },
    "clf-ini-085": {
      "optionReasons": {
        "clf-ini-085:option:0": "",
        "clf-ini-085:option:1": "The Classic Load Balancer is the legacy generation, with basic routing — does not have advanced routing by layer content 7 of the ALB.",
        "clf-ini-085:option:2": "It's the closest because the NLB is also a high performance balancer of the ELB — But he operates on the Layer 4 (TCP/UDP), routing by IP/port without reading the content HTTP; decide by headers, cookies and paths of URL in the Layer 7 is the Application Load Balancer.",
        "clf-ini-085:option:3": "The Gateway Load Balancer directs traffic to virtual security applications (chamber 3) — does not route by road from URL Application."
      },
      "finalJustifications": [
        "Because Application Load Balancer Route in the Layer 7 reading headlines HTTP, cookies and paths of URL — forwarding '/payments/*' to specific servers, exactly the requirement of microservices.",
        "Because the Classic Load Balancer Route in the Layer 7 reading headlines HTTP, cookies and paths of URL — forwarding '/payments/*' to specific servers, exactly the requirement of microservices.",
        "Because the Network Load Balancer Route in the Layer 7 reading headlines HTTP, cookies and paths of URL — forwarding '/payments/*' to specific servers, exactly the requirement of microservices.",
        "Because the Gateway Load Balancer Route in the Layer 7 reading headlines HTTP, cookies and paths of URL — forwarding '/payments/*' to specific servers, exactly the requirement of microservices."
      ]
    },
    "clf-ini-086": {
      "optionReasons": {
        "clf-ini-086:option:0": "",
        "clf-ini-086:option:1": "It's the closest because weighted routing also distributes traffic between servers based on configuration — but it distributes by PERCENTUAIS defined (ex.: 70/30), do not switch to a backup when the main one falls; send to the reservation as Health Checks is Failover Routing.",
        "clf-ini-086:option:2": "Latency routing chooses the fastest endpoint for the user — it is not the switch mechanism for a backup server in the fall.",
        "clf-ini-086:option:3": "Geolocation router addresses the country/region of the user — does not do the automatic failover for a backup server."
      },
      "finalJustifications": [
        "Because Failover Routing sends users to the main server and automatically switches to backup when Health Checks indicate that the main fell — exactly the behavior requested.",
        "Because Weighted Routing sends users to the main server and automatically switches to backup when Health Checks indicate that the main one fell — exactly the behavior requested in Route 53.",
        "Because Latency-based Routing sends users to the main server and automatically switches to backup when Health Checks indicate that the main fell — exactly the behavior requested.",
        "Because Geolocation Routing sends users to the main server and automatically switches to backup when Health Checks indicate that the main one fell — exactly the behavior requested in Route 53."
      ]
    }
  }
}
