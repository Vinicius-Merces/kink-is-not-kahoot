{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "dva-c02",
    "level": "avancado",
    "sourcePath": "data/exams/dva-c02/avancado.json",
    "range": "133-144",
    "generator": "offline-marian-faithful-v2"
  },
  "questions": {
    "dva-avc-133": {
      "text": "An IC/CD pipeline in AWS CodePipeline builds and automatically implements an application in a STAGING. Before implementing in PRODUCTION, the team requires that a HUMANO review and explicitly approve the results of the staging deployment (e.g. after manual QA testing) — the pipeline must PAUSAR after the placement stage in staging and AGUARDAR indefinitely (or even a configured timeout) that a designated approval or rejection via Console of AWS (or API/notification SNS) before proceeding to the Deployment stage in production. How to configure this?",
      "options": [
        "Add a STAGIO (or an ACTION within an stage) to the pipeline configured with the 'Manual Approval' action provider (category 'Approval')') ENTRE the staging deployment stage and the production deployment stage — when the pipeline reaches this action, it PAUSA the execution; optionally, set up a topic SNS for NOTIFICAR designated approvals when the pipeline reaches this point; an authorized user then reviews and click on 'Approve' or 'Reject' on the console of CodePipeline (or via API 'PutApprovalResult') — 'Approves' allows the pipeline to PROSSIGA for the next stage (deployment in production), while 'Reject' (or a set timeout) interrupts the pipeline.",
        "Configure a policy of the IAM that NEGA to the service roll of the CodePipeline permission to deploy in the DeploymentGroup of CodeDeploy of production, requiring that an administrator CONCEDA TEMPORARIAMENTE permission (see separate change in IAM) whenever a production department is to continue.",
        "Use AWS Step Functions with the task token standard 'Wait for Callback' integrated into the buildspec of CodeBuild, pause the build until a call from API external or resume.",
        "Configure DeploymentGroup CodeDeploy of the production stage with a 'Canary' deployment configuration and an alarm of CloudWatch which requires manual recognition before proceeding to 100% traffic."
      ],
      "explanation": "The native 'Manual Approval' action of the CodePipeline pause the pipeline execution at a defined point, optionally notifying approvals via SNS, and awaits an explicit decision ('Approv'/'Reject') via console or API 'PutApprovalResult' before allowing progression to the next stage — exactly the human gate described.",
      "hint": "Think about what kind of native action the CodePipeline pauses the pipeline and awaits an explicit decision of approval or rejection before continuing.",
      "optionRationales": [
        "The native 'Manual Approval' action of the CodePipeline pause the pipeline execution at a defined point, optionally notifying approvals via SNS, and awaits an explicit decision ('Approv'/'Reject') via console or API 'PutApprovalResult' before allowing progression to the next stage — exactly the human gate described.",
        "Incorrect. CloudWatch in deployment configurations of the CodeDeploy shoot ROLLBACK AUTOMATICAL based on LIMIARES OF METRICS — they do not provide a mechanism for a HUMANO review and explicitly approve/reject ANTES of the production staff not even START; a Manual Approval action in the pipeline blocks the progression to the production stage as a whole.",
        "Incorrect. Step Functions POSSA implementing human-in-the-loop standards, this would require building an integration PERSONALIZADA between CodeBuild/buildspec and Step Functions, more than 1 IU/API Customised approval — or CodePipeline already has a type of action NATIVO 'Approval Manual' designed exactly for this case to pause the pipeline for approval, without custom integration.",
        "Incorrect. Use permission changes from IAM as a manual gate mechanism is operationally fragile and is not the intended standard — requires modifications of IAM Off-band to CADA deploys and does not integrate into the pipeline's visual flow/notifications as a native action Manual Approval does."
      ]
    },
    "dva-avc-134": {
      "text": "A stack of CloudFormation stock a pocket S3 used to store important files sent by users, and a table DynamoDB storing application data. The team is concerned that an operation 'DeleteStack' ACIDENTAL (or an update of the stack that requires SUBSTITUIR these resources) may result in PERDA PERMANENTE OF DADOS, since the standard behavior of CloudFormation It's EXCLUIR resources when the stack (or the resource within it) is deleted/replaced. How the team can ensure that the bucket S3 and the table DynamoDB or PRESERVADOS (not excluded) even if the stack is deleted or these resources are replaced during an update?",
      "options": [
        "Move Bucket Resource Settings S3 and Table DynamoDB for FORA of the template of CloudFormation completely, creating/managing them manually outside the CloudFormation, and reference your RNAs as Parameters in stack.",
        "Enable 'Termination Protection' in stack.",
        "Add 'DeletionPolicy: Retain' to the Bucket features S3 and Table DynamoDB in the template of the CloudFormation — with 'DeletionPolicy: Retain', when the STACK is EXCLUDED, CloudFormation IGNORA the exclusion of these resources (they remain in the account, now not managed by the CloudFormation); ADICIONALMENTE, add 'UpdateReplacePolicy: Retain' to ensure that if an update of the stack causes the SUBSTITUTE of these resources (e.g. due to the change of an unchangeable property), the resource ANTIGO (with your data) be RETIDO (not excluded) instead of being excluded after the creation of the new.",
        "Set the 'DeletionPolicy' of the pocket S3 and Table DynamoDB like 'Snapshot''."
      ],
      "explanation": "Correct. 'DeletionPolicy: Retain' does the CloudFormation skip the exclusion of the resource when STACK is excluded; 'UpdateReplacePolicy: Retain' does the same for the resource ANTIGO when an UPDATE causes replacement. Together, these policies protect the bucket S3 and the table DynamoDB against accidental exclusion in both 'DeleteStack' and 'UpdateStack' with replacement.",
      "hint": "Think of what resource attributes (not stack) of CloudFormation control what happens to a specific resource when it is deleted or replaced.",
      "optionRationales": [
        "Correct. 'DeletionPolicy: Retain' does the CloudFormation skip the exclusion of the resource when STACK is excluded; 'UpdateReplacePolicy: Retain' does the same for the resource ANTIGO when an UPDATE causes replacement. Together, these policies protect the bucket S3 and the table DynamoDB against accidental exclusion in both 'DeleteStack' and 'UpdateStack' with replacement.",
        "Incorrect. 'Termination Protection' prevents STACK INTEIRA is deleted via 'DeleteStack' (the API is rejected) — although it offers ALGUMA protection against accidental exclusion from the complete stack, DO NOT protect against SUBSTITUTE OF RECURSOS during updates (where Termination Protection does not apply), and an administrator could still DESABILITAR the protection and delete the stack — 'DeletionPolicy'/'UpdateReplacePolicy: Retain' provide protection at the LEVEL OF RECURSO, independent of stack level protection settings.",
        "Incorrect. 'DeletionPolicy: Snapshot' is only supported for features that support SNAPSHOTS NATIVOS as part of the exclusion treatment of CloudFormation, as 'AWS::RDS::DBAssistance', 'AWS::EC2::Volume 'AWS::ElastiCache::CacheCluster' (Redis) — NOT a valid/supported option for 'AWS::S3::Bucket' or 'AWS::DynamoDB::Table'; 'Retain' is the appropriate policy for these types of resources.",
        "Incorrect. Remove these resources from management of CloudFormation completely is a much larger architectural change (losing benefits of IAC as consistent provisioning/tracking of configuration for these resources) than simply adding 'DeletionPolicy'/'UpdateReplacePolicy: Retain', which preserves management by CloudFormation while protecting against data loss by accidental deletion/substitution."
      ]
    },
    "dva-avc-135": {
      "text": "A function Lambda written in Node.js processes thousands of invocations per minute. The function code creates a NOVA instance of 'DynamoDBClient' (AWS SDK v3) and a new connection TCP DENTRO of the handler function, in TODA invocation. The team perceives high latency by invocation and wants to reduce the overhead to repeatedly create clients of SDK/connections TCP, taking advantage of the fact that the Lambda often REUTILIZA the environment of execution between invocations. What change must be made?",
      "options": [
        "Use AWS Lambda Layers to pack the SDK of AWS as a layer shared between functions, which enables the pool of connections to be shared between function execution environments Lambda DIFERENTES.",
        "Increase function memory allocation Lambda, since larger memory allocations increase proportionally the power of CPU, which accelerates the client's demand for SDK and establishing the connection TCP in each invocation.",
        "Configure the function with Provided Competition, which automatically stores in cache and reuses client instances from SDK between invocations without code changes.",
        "Move the 'DynamoDBClient' instance (and any other client startup of the SDK) for the ESCOPO GLOBAL/OF the function code module, FORA handler function — code in the global scope executes UMA VEZ by execution environment (during INIT), and the initialized client (together with their pool of connections TCP underlying, kept active via HTTP Keep-alive) is RETIDO in memory, and REUTILIZADO in invocations SUBSEQUENTES which re-use MESMO execution environment ('warm startups') — avoiding overhead to recreate the client and re-establish connections TCP/TLS in each invocation."
      ],
      "explanation": "Correct. Code in the global scope/module of a function Lambda is executed only during the phase of INIT of the execution environment, and variables/objects created there (as a client of SDK with your pool of connections HTTP) remain in memory and are reused by subsequent invocations in the same environment ('warm start'), eliminating the overhead of recreation at each invocation.",
      "hint": "Think about what part of a function code Lambda is executed only once per running environment (in the startup phase), and remains available for subsequent invocations that reuse this environment.",
      "optionRationales": [
        "Correct. Code in the global scope/module of a function Lambda is executed only during the phase of INIT of the execution environment, and variables/objects created there (as a client of SDK with your pool of connections HTTP) remain in memory and are reused by subsequent invocations in the same environment ('warm start'), eliminating the overhead of recreation at each invocation.",
        "Incorrect. Although increasing memory AUMENTE a CPU (which could marginally accelerate the TRABALHO of creating a customer), this does NOT solve the underlying inefficiency of RECRIAR clients/connections in CADA Invocation — Move the initialization to the global scope ELIMINA this work repeated for warm invocations, being a more direct and economical correction than simply allocating more memory..",
        "Incorrect. Provided Competition maintains pre-initiated and heated running environments (reducing COLD STARTS), But it doesn't change automatically COMO THE FIRST CODE behaves — if the code creates a new customer SDK DENTRO of the handler in every invocation, this recreation AINDA OCORRE in every invocation independently of Provided Competition; the code change (moving the initialization to the global scope) is necessary to in fact achieve customer reuse/connection.",
        "Incorrect. Lambda Layers are a mechanism for COMPARTILHAR CODE/DEPENDENCES (reducing the size of the deployment package and allowing the reuse of common libraries between functions) — they DO NOT create shared connection pools ENTRE implementing environments SEPARADOS (each running environment, even for the same function, has its own isolated memory/connections) — the correction is on ONDE in the code (global scope vs handler) the customer is asked, not about packaging via Layers."
      ]
    },
    "dva-avc-136": {
      "text": "An application has a table DynamoDB which meets a workload with FORTE PREDOMINATION OF LEITURA (reading ratio: 1000:1), with many requests reading the MESMOS frequently accessed items (e.g.: popular product details). The team wants to reduce reading latency from milliseconds to MICROSSEGUNDOS for these items frequently accessed and significantly reduce the reading capacity consumed in the underlying table, SEM require the application code to manage a separate cache invalidation strategy or use a different data access pattern (want to continue using Cache calls). API of the DynamoDB as 'GetItem'/ 'Query'). What they should implement?",
      "options": [
        "Implement o Amazon DynamoDB Accelerator (DAX) — o DAX is a layer of CACHE IN MEMORY, WRITE-THROUGH, specific to the DynamoDB, positioned ENTRE the application and the table DynamoDB; the application uses the CLIENTE DAX (a customer's direct replacement (drop-in) SDK of the DynamoDB, compatible with API of 'GetItem'/'Query'/etc.), requiring MINIMUM CODE CHANGES; or DAX automatically caches the results of 'GetItem'/ 'Query'/ 'Scan' (item cache and query cache) and responds to repeated readings of MESMOS items/queries from your memory cache with latency MICROSSEGUNDOS, while writing passes through the DAX for the DynamoDB (write-through), reducing the reading load in the underlying table without custom logic of cache invalidation.",
        "Implement o Amazon ElastiCache for Redis as cache in front of the DynamoDB, with the application code implementing a Lazy Loading (cache-aside) pattern: check the Redis first, and in case of miss cache consult the DynamoDB and popular the Redis with a TTL.",
        "Enable DynamoDB Streams in the table and have a function Lambda consuming the stream for popular a separate memory cache (e.g.: a global variable of the running environment of the Lambda) which subsequent invocations consult before consulting the DynamoDB.",
        "Enable 'accelerated readings' of DynamoDB increasing the reading capacity units (RCUs) provided in the table and changing to readings FORTEMENTE CONSISTENTES, that the AWS automatically caches in the storage layer for repeated access patterns."
      ],
      "explanation": "Correct. DAX is the native cache layer, compatible with the API of the DynamoDB (drop-in customer replacement SDK), which provides cache of items and queries with microsecond latency and automatic write-through — meeting exactly the requirement to reduce latency/load without changing the access pattern or implement custom invalidation.",
      "hint": "Think of what service the AWS is specifically a cache in memory, compatible with the API of the DynamoDB, which can be adopted as a direct customer substitute SDK without changing the call pattern.",
      "optionRationales": [
        "Correct. DAX is the native cache layer, compatible with the API of the DynamoDB (drop-in customer replacement SDK), which provides cache of items and queries with microsecond latency and automatic write-through — meeting exactly the requirement to reduce latency/load without changing the access pattern or implement custom invalidation.",
        "Incorrect. Although ElastiCache is Redis PUDESSE reduce the reading load in DynamoDB, requires the application to use a API DIFERENTE (Redis commands, not calls from API of the DynamoDB) and IMPLEMENTE custom cache-aside/validation logic in the application code — the scenario specifically wants EVITAR custom logic of cache invalidation and MANTER the use of calls from API of the DynamoDB; o DAX is compatible with the API and manage the cache transparently.",
        "Incorrect. Increasing RCUs increases to CAPACIDADE OF THROUGHPUT and the CUSTO of the table, but does not provide CACHE with latency MICROSSEGUNDOS — o DynamoDB does not have a native resource of 'accelerated readings via RCU' which works as cache; this confuses raw capacity provisioning with a cache layer. DAX is the cache layer dedicated to this requirement.",
        "Incorrect. Depending on a global variable in memory of an execution environment of Lambda how shared cache is POUCO TRUST (execution environments are NOT shared between competing invocations, and can be recycled at any time) — this does not provide a cache CONSISTENTE, CENTRALIZADO, with microsecond latency between TODAS the instances of application in the form that the DAX provides, and adds, significant complexity built-to-measure."
      ]
    },
    "dva-avc-137": {
      "text": "A project of the CodeBuild for an application Node.js takes ~8 minutes per build, being that 'npm install' (lowering and installing package.json dependencies, ~600MB of node_modules) consumes most of that time, even though dependencies rarely change between builds. The team wants to reduce SIGNIFICATIVAMENTE the build time avoiding downloading/reinstalling unchanged dependencies in each build. What configuration of the CodeBuild You'll have to work it out.?",
      "options": [
        "Use AWS CodeArtifact as a private npm record, and point the 'npm install' to the CodeArtifact repository instead of the npm public record, since CodeArtifact is faster than the public record.",
        "Configure the CACHE of the project CodeBuild using the mode 'LOCAL' (ex.: 'LOCAL_CUSTOM_CACHE') or the cache type 'S3', and specify the directory 'node_modules' (and/or the npm cache directory) in the 'cache:paths' section of the buildspec — this PERSISTE the specified directories ENTRE builds (cache LOCAL not persist MESMO build host is reused; cache S3 persists in a pocket S3 and is restored at the beginning of subsequent builds) — in builds SUBSEQUENTES, if 'package.json'/ 'package-lock.json' have not changed, 'npm install' can run much faster (or be effectively skipped), because 'node_modules' in cache is restored, avoiding lowering dependencies.",
        "Increase the project's 'computerType' CodeBuild for a larger instance (e.g.: 'BUILD_GENERAL1_SMALL' for 'BUILD_GENERAL1_2XLARGE') to speed up the 'npm install' with more CPU/Network bandwidth.",
        "Divide buildspec into projects CodeBuild separate for the phases of 'install', 'build' and 'test', running them in PARALELO in projects CodeBuild within the same stage of CodePipeline."
      ],
      "explanation": "Correct. The cache configuration of the CodeBuild (LOCAL or S3) with 'cache:paths' including 'node_modules' (or npm cache) these dependencies persist between build executions — when 'package.json'/ 'package-lock.json' do not change, subsequent builds restore the cache and avoid the cost of lowering/reinstalling all dependencies, drastically reducing the total build time.",
      "hint": "Think about which configuration of the CodeBuild allows to persist directories of dependencies between build executions, avoiding repeated work when nothing has changed.",
      "optionRationales": [
        "Correct. The cache configuration of the CodeBuild (LOCAL or S3) with 'cache:paths' including 'node_modules' (or npm cache) these dependencies persist between build executions — when 'package.json'/ 'package-lock.json' do not change, subsequent builds restore the cache and avoid the cost of lowering/reinstalling all dependencies, drastically reducing the total build time.",
        "Incorrect. Although a larger type computer PUDESSE marginally accelerate parts of the 'npm install' limited by CPU, this DOES NOT eliminate the fundamental problem of REBAIXAR/REINSTALAR the MESMAS unchanged dependencies in CADA build — cache the directory 'node_modules'/cache of dependencies between builds resolves root cause directly, usually with a much larger time saving, without increasing the cost per minute of computing.",
        "Incorrect. CodeArtifact can be used as cache/proxy pass-through for public registration of npm and can offer ALGUNS performance/reliability benefits, but DO NOT eliminate the need for BAIXAR And INSTALAR packages in each build (the CodeArtifact still serves the packages over the network in each request) — persistent 'node_modules' via cache configuration CodeBuild completely avoids the download/installation step when dependencies do not change, being a more direct optimization for this problem.",
        "Incorrect. Running 'install' in parallel with 'build'/ 'test' doesn't make sense, since 'build'/'test' typically DEPENDEM of installed dependencies ('node_modules') — this would not reduce the real time of the 'npm install' and would add orchestration complexity; cache storage of the dependencies directory directly reduces the time that the 'npm install itself takes in subsequent builds."
      ]
    },
    "dva-avc-138": {
      "text": "A team uses the CodeDeploy (via 'DeploymentPreference' SAM) for canary deployments of a function Lambda. They want to execute a VALIDATION AUTOMATIZADA (ex: invoke the new version of the Lambda with a synthetic test event and check the response) DEPOIS that the new version begins to receive a part of the traffic, but ANTES to receive 100% traffic — if validation fails, the staff member must make ROLLBACK AUTOMATICAMENTE. How this is implemented?",
      "options": [
        "Set a hook 'BeforeInstall'.",
        "Add an alarm to the CloudWatch in the configuration 'DeploymentPreference.Alarms' by monitoring the function 'Errors' metric Lambda, without defining any Hook.",
        "Define a HOOK 'AfterAllowTraffic in AppSpec CodeDeploy (referenced via 'DeploymentPreference.Hooks' SAM, or directly in 'appsp.yml' for deploys Lambda) — this hook specifies a function Lambda SEPARADA (a 'validation function') that the CodeDeploy INVOCA AUTOMATICAMENTE at the corresponding point of the deployment life cycle (after the traffic started to be diverted to the new version); the validation function should call 'PutLifecycleEventHookExecutionStatus' with 'Succeded' or 'Failed' based on the test results — a 'Failed' status does the CodeDeploy do ROLLBACK AUTOMATICAMENTE (reversing traffic to the previous version).",
        "Configure the 'DeadLetterConfig' function Lambda to send invocations failures during the canary period to a row SQS, and do the CodeDeploy consult this row to decide whether to roll back."
      ],
      "explanation": "Correct. The hook 'AfterAllowTraffic' allows you to CodeDeploy automatically invokes a validation function after the start of the traffic diversion to the new version; the result reported via 'PutLifecycleEventHookExecutionStatus' determines whether the deployer continues or suffers automatic rollback — exactly the active rollback validation behavior described.",
      "hint": "Think of which hook of the life cycle of CodeDeploy for Lambda execution DEPOIS that the traffic diversion to the new version has already begun, allowing an active validation before proceeding to 100%.",
      "optionRationales": [
        "Correct. The hook 'AfterAllowTraffic' allows you to CodeDeploy automatically invokes a validation function after the start of the traffic diversion to the new version; the result reported via 'PutLifecycleEventHookExecutionStatus' determines whether the deployer continues or suffers automatic rollback — exactly the active rollback validation behavior described.",
        "Incorrect. For deployments Lambda, life cycle hooks 'BeforeInstall' and 'AfterInstall' NOT ONLY SUPORTADOS (these apply to staff EC2/On-premises) — Deployees Lambda specifically support the 'BeforeAllowTraffic' and 'AfterAllowTraffic' hooks, which run in relation to the DESVIO OF TRAFFIC, not to installation steps.",
        "Incorrect. CloudWatch in the metric 'Errors' provides rollback PASSIVO, based on LIMIAR METRIC (monitoring actual production traffic errors over time) — does not perform a SYNTETICAL E validation test ATIVO immediately after the start of the traffic diversion; hooks 'AfterAllowTraffic' allow active validation (e.g. invoke function with a payload test) at a specific point of the deployment, complementing (not replacing) alarm-based monitoring.",
        "Incorrect. 'DeadLetterConfig' captures ASSIGNMENTS faults of its own function — the CodeDeploy do not consult rows DLQ as part of your deployment/rollback decision life cycle; hooks 'AfterAllowTraffic' with 'PutLifecycleEventHookExecutionStatus' are the documented mechanism for validation-based rollback decisions."
      ]
    },
    "dva-avc-139": {
      "text": "The buildspec of a project CodeBuild needs a database password and a token of API of third parties to perform integration tests during the build. A developer initially encodes these values as environment variables in the TEXTO CLARO directly in the buildspec.yml file (versioned into source control). A security review classifies this as a CRITICAL problem. What is the correct way to provide these values sensitive to build SEM expose them in the version control or in the project clear text configuration CodeBuild?",
      "options": [
        "Store values as environment variables in TEXTO CLARO in the configuration of the PROJETO CodeBuild (not in buildspec file), since environment variables at project level are automatically encrypted at rest by CodeBuild.",
        "Passing the secrets as Build PARAMETERS via the UserParameters field of the CodePipeline when taking action CodeBuild, encoded as a string JSON.",
        "Store the database password and token of API no AWS Secrets Manager (or as SecureString parameters in Systems Manager Parameter Store), and refer them in the section 'env.secrets-manager' (or 'env.parameter-store') of the buildspec (or the project) CodeBuild) using the ARN/secret name (ex.: 'DB_PASSWORD: \"arn:aws:secretsmanager:...:secret:mydb-password\"') — the service role of CodeBuild (with permission 'secretsmanager:GetSecretValue' or 'ssm:GetParameters') recovers these values NO MOMENTO DO BUILD and injects them as environment variables into the build environment — the VALORES real secrets never appear in the builtspect file versioned or in the setting of environment variables in clear text of the console CodeBuild.",
        "Encrypting secrets with a key to KMS and version the values of CIPHERTEXT encrypted directly in buildspec.yml file, with a build command that describes them using the AWS CLI ('aws kms decrypt') at the start of the build."
      ],
      "explanation": "Correct. Sections 'env.secrets-manager'/ 'env.parameter-store' of the buildspec/project CodeBuild cause sensitive values to be searched in build time from the Secrets Manager/Parameter Store (with access control via IAM and audit via CloudTrail), without ever appearing in clear text in the source code or project configuration.",
      "hint": "Think about which section of the buildspec allows you to reference secrets stored in a dedicated service, making the values search only in build time, without appearing in clear text in the configuration.",
      "optionRationales": [
        "Correct. Sections 'env.secrets-manager'/ 'env.parameter-store' of the buildspec/project CodeBuild cause sensitive values to be searched in build time from the Secrets Manager/Parameter Store (with access control via IAM and audit via CloudTrail), without ever appearing in clear text in the source code or project configuration.",
        "Incorrect. Although the design configuration CodeBuild SEJA encrypted at rest, environment variables in TEXTO CLARO configured at the project level are still visible to anyone with reading access to the project configuration (e.g. via console or API 'BatchGetProjects') — using environment variables of the type 'secrets-manager'/'parameter-store' ensures that VALORES are searched in build time and not stored/visible as clear text in the project configuration itself.",
        "Incorrect. Although this avoids storing secrets in TEXTO CLARO in version control, requires custom description commands in buildspec, manual management of updates of ciphertext when secrets are rotated, and exposes the values of the data. DESCRIPTOGRAFADOS in the build/environment logs of the process, unless treated with care — 'env.secrets-manager'/ 'env.parameter-store' is the native mechanism of CodeBuild designed for this, without custom description logic.",
        "Incorrect. 'UserParameters' no CodePipeline is intended to pass CONFIGURATION values to an action (often visible in pipeline/console execution details), not to pass CREDENCIAIS sensitive form SEGURA — does not provide recovery with access control and audit that integration with Secrets Manager/Parameter Store offers."
      ]
    },
    "dva-avc-140": {
      "text": "A pipeline from CodePipeline uses a repository AWS CodeCommit as a source. The team realizes that after sending a commit, there is a ATRASO perceptible (up to several minutes) before the pipeline starts — the pipeline was configured using the mechanism MAIS ANTIGO of polling, which checks the repository for changes every few minutes. The team wants the pipeline to start QUASE INSTANTANEAMENTE after a push to the main branch of the repository. What you should set up?",
      "options": [
        "Configure the source action of the CodePipeline for use in the detection of changes via AMAZON EVENTBRIDGE (the recommended modern approach, instead of 'Poll for source changes') — CodeCommit automatically issues a EVENTO NO EVENTBRIDGE when a push occurs (e.g. 'CodeCommit Repository State Change' with 'referenceType: branch' and the branch name); CodePipeline creates (or the team creates) a REGRA of the EventBridge corresponding to this event pattern for the specific repository/branch, with the PIPELINE as ALVO — this triggers the pipeline execution immediately after push, eliminating the polling delay.",
        "Configure a TRIGGER CodeCommit (the legacy notification resource 'PutRepositoryTriggers') to send a notification SNS in push, and do the CodePipeline sign up for this topic SNS as its mechanism of change of origin.",
        "Add a function Lambda as the first action of the pipeline that calls 'GetBranch' in the CodeCommit repository to each 10 seconds in loop, and manually call 'StartPipelineExecution' when a new commit is detected.",
        "Reduce the polling interval of the action of origin of the CodePipeline configuring 'PollForSourceChanges: true' with a custom 'pollingInterval' parameter set to a lower value (ex.: 30 seconds)."
      ],
      "explanation": "Correct. Detection of changes based on EventBridge is event-oriented: CodeCommit issues an event in push, a rule of EventBridge correspondent immediately triggers pipeline execution as target — eliminating the delay inherent to the periodic polling.",
      "hint": "Think of what service the AWS receives native CodeCommit events at the time of push, and can be configured with a rule that triggers the pipeline immediately as target, instead of the pipeline periodically checking for changes.",
      "optionRationales": [
        "Correct. Detection of changes based on EventBridge is event-oriented: CodeCommit issues an event in push, a rule of EventBridge correspondent immediately triggers pipeline execution as target — eliminating the delay inherent to the periodic polling.",
        "Incorrect. CodePipeline for CodeCommit does not expose a configurable pollingInterval parameter for users to reduce below standard behavior — polling inherently introduces ALGUM delay; change to change detection based on EVENTBRIDGE (event-oriented, non-polling) is the mechanism for almost instantaneous pipeline start-ups, as recommended by AWS instead of polling.",
        "Incorrect. This is a custom built polling mechanism performing DENTRO of a pipeline action (which alone would already need to be running/activated to perform this polling) — is circular and much more complex than simply setting up a rule of EventBridge which reacts to the CodeCommit native push event.",
        "Incorrect. The action of origin of CodePipeline does not support topics SNS CodeCommit triggers as MECANISMO OF ACIONAMENTO to start pipeline executions — CodeCommit triggers/SNS are used for NOTIFICATIONS (e.g. send email to developers), not to trigger the CodePipeline; the detection of changes based on EventBridge is the supported mechanism for event-oriented pipeline starters."
      ]
    },
    "dva-avc-141": {
      "text": "A team wants to define its infrastructure (functions Lambda, tables DynamoDB, API Gateway) using a LINGUAGEM PROPOSED PROGRAMME GERAL (TypeScript) with reusable and object-oriented abstractions, instead of writing templates YAML/JSON of the CloudFormation They write code manually. CDK defining a 'Stack' containing a 'aws_lambda.Function' and a 'aws_dynamodb.Table', using high-level constructs of the CDK which automatically connect permissions from IAM saw a call to the 'grantReadWriteData()' method. Before that code CDK may in fact provide resources in an account/region of AWS for PRIMEIRA VEZ, what DEVE to be made?",
      "options": [
        "Grant to the user of IAM of CLI of the CDK permissions 'AdministratorAccess', since the CDK requires unlimited permissions throughout the account to create any type of resource defined in the code.",
        "Run 'cdk synth' and upload template manually CloudFormation resulting via the console of the CloudFormation, since 'cdk deploy' only serves to update stacks EXISTENTES, not to create new.",
        "Manually write the template CloudFormation equivalent in YAML first, and then use 'cdk import' to associate the code CDK to existing template before implanting.",
        "Run 'cdk bootstrap' against account/region AWS of destination — Bootstrap provides a small set of resources FUNDACIONAIS of which the staff of CDK depend (a pocket S3 to store assets from CDK as deployment packages of Lambda, a repository ECR for container image assets, and IAM used by CDK during deployment via CloudFormation) — 'cdk bootstrap' is a unique configuration POR account/region; after that, 'cdk deploy' can synthesize the code CDK in a template of the CloudFormation ('cdk synth') and implement it via CloudFormation, using the resources provided by bootstrap."
      ],
      "explanation": "Correct. 'cdk bootstrap' is the single-by-account/region step that provides support infrastructure (bucket S3 of assets, repository ECR, Roles of IAM/CloudFormation) necessary for 'cdk deploy' to work, especially for buildings that pack assets (as code Lambda) and upload them during the deployment.",
      "hint": "Think of which command of CDK provides, once per account/region, the support resources (bucket assets, roles of the IAM) necessary for deployments that pack code/assets to work.",
      "optionRationales": [
        "Correct. 'cdk bootstrap' is the single-by-account/region step that provides support infrastructure (bucket S3 of assets, repository ECR, Roles of IAM/CloudFormation) necessary for 'cdk deploy' to work, especially for buildings that pack assets (as code Lambda) and upload them during the deployment.",
        "Incorrect. 'cdk import' is used to bring resources EXISTENTES (have already been implemented) AWS for the management of CDK/CloudFormation — is not a pre-requisite step to implement resources NOVOS coded CDK for the first time; the code CDK is synthesized directly into a new template CloudFormation via 'cdk synth'/'cdk deploy', without the need for a template written manually before.",
        "Incorrect. 'cdk deploy' treats TANTO the creation of stacks NOVAS as for updating existing ones (he calls 'CreateStack' or 'UpdateStack' as appropriate) — there is no need to manually upload a synthesized template via console; however, even with 'cdk deploy', the target account/region must first have been BOOTSTRAPPED for asset-based deployments (e.g. code assets) Lambda) function.",
        "Incorrect. Although the principal who does the deployment needs permissions SUFICIENTES (and the bootstrap of CDK creates roles that the CloudFormation during the deployment), 'AdministratorAccess' is not a REQUISITO specific CDK nor the PRE-REQUISITO missing key in this scenario — the actual pre-requirement for an account/region NOVA it is run 'cdk bootstrap' to provide the asset/roll support pocket of which the employees of the CDK depend."
      ]
    },
    "dva-avc-142": {
      "text": "The template of CloudFormation of a team grew to more than 400 resources and is difficult to maintain — changes in network configuration (VPC, subnets, security groups) require careful review of a template that also contains application-specific resources (Lambda, API Gateway, DynamoDB), and the same network configuration needs to be REUTILIZADA in several application stacks. How the team can modulate this MANTENDO the components as part of A SINGLE logical deployment (a single 'aws cloudformation deploy' for the whole application, with resources can refer the outputs of each other)?",
      "options": [
        "Refocus network configuration in a template of CloudFormation SEPARADO, and refer it from the main template ('father') as a NESTED STACK using the resource type 'AWS::CloudFormation::Stack', with the nested template stored in S3 — the appeal 'AWS::CloudFormation::Stack's Dad stack can pass 'Parameters' to the nested stack and consume your 'Outputs' (ex.: VPC ID, subnet IDs) via 'GetAtt' (e.g. 'NetworkingStack.Outputs.VpcId') to connect them to the application resources defined in the parent stack (or other nested stacks) — implement the stack PAI implant TODAS stacks nested together like UMA coordinated operation, while the network template can be REUTILIZADO as stack nested in multiple stacks father.",
        "Combine all 400+ features in a single template, but use 'Conditions' (Fn::If) of CloudFormation for AGRUPAR logically network vs. application features, making it easier to navigate the template without sharing files.",
        "Use cross-stack references CloudFormation: implement network resources in a stack SEPARADA and INDEPENDENTE that 'Export' your Outputs (ex.: VPC ID) via the 'Export' field, and make the application stack use 'Fn::ImportValue' to reference these exported values.",
        "Use AWS CloudFormation StackSets to deploy the network template in MESMA application stack account/region, treating the application account/region as a 'target' of the StackSet."
      ],
      "explanation": "Right. ('AWS::CloudFormation::Stack') allow you to divide a large template into smaller and reusable templates, keeping them as part of A SINGLE Coordinated Stack Father — nested stack outputs can be referenced via 'GetAtt' by the parent stack (or other nested stacks), taking into account both modularity and reuse and the single deployment requirement..",
      "hint": "Think of what resource the CloudFormation allows a template to refer to another complete template as part of ONE ONLY coordinated deployment, with accessible outputs via GetAtt.",
      "optionRationales": [
        "Right. ('AWS::CloudFormation::Stack') allow you to divide a large template into smaller and reusable templates, keeping them as part of A SINGLE Coordinated Stack Father — nested stack outputs can be referenced via 'GetAtt' by the parent stack (or other nested stacks), taking into account both modularity and reuse and the single deployment requirement..",
        "Incorrect. Although cross-stack references (Export/ImportValue) PERMITAM that stacks SEPARADAS share values, they create stacks INDEPENDENTES implanted/updated SEPARADAMENTE (not as a single coordinated staff), and the CloudFormation IMPEDE delete/modify an exported output as QUALQUER stack still import (creating rigid coupling in the exclusion order) — in this stacks keep the components as part of a coordinated parent stack deployer, better meeting the requirement of 'a single logical deployer'.",
        "Incorrect. StackSets are designed to implement the MESMO template CONSISTENTEMENTE in MULTIPLES accounts/regions (e.g., guardians across the organization) — use a StackSet to deploy network resources DENTRO DA MESMA account/region of an application stack (as an alternative solution for modularization) is not the intended use case; in this stacks are the standard mechanism for modularizing templates of a single deployment.",
        "Incorrect. 'Conditions' control SE a resource is created on the basis of a condition (e.g. differences by environment) — they do not provide MODULARIDADE/Reutilization between multiple stacks, nor reduce the TAMANHO/complexity of a single template file; in this stacks solve both modularity (separated files/templates) and reuse (the same nested template used by multiple stacks parent) directly."
      ]
    },
    "dva-avc-143": {
      "text": "A Template SAM define 12 resources 'AWS::Serverless::function', TODOS requiring the MESMO 'Runtime' (nodejs18.x), or MESMO 'Timeout' (10 second) and the MESMO set of 3 environment variables (e.g..: 'LOG_LEVEL', 'TABLE_NAME', 'REGION_NAME'). At present, each of the 12 function definitions repeat these same properties, making the template extensive and prone to errors when these shared values need to change (requiring 12 editions). How the team can define these shared properties UMA VEZ, applying them to TODAS the functions, but still able SOBRESCREVER specific properties for individual functions when necessary?",
      "options": [
        "Define shared properties as 'Parameters' of CloudFormation with standard values, and reference '!Ref' for each property in 12 function definitions.",
        "Set a 'Globals' section at the Level SUPERIOR template SAM (sister of 'Resources'), with a subsection 'Function' specifying the common 'Runtime', 'Timeout' and 'Environment.Variables' — the SAM apply these values GLOBAIS a TODO appeal 'AWS::Serverless::Template Function, A MENOS QUE a specific function resource SOBRESCREVA EXPLICITAMENTE a property in its own definition (function level properties have PRECEDENCE on 'Globals' for this specific property) — allowing you to define shared configuration UMA VEZ, maintaining the possibility of overwriting by function when necessary.",
        "Create a MACRO of the CloudFormation (a custom 'Transform') that pre-processes the template, injecting the shared properties of 'Runtime', 'Timeout' and 'Environment' into each resource 'AWS::Serverless::Function' before deployment.",
        "Use the 'Aspects' feature of AWS CDK to apply common properties to all function constructs Lambda after they are defined, and then run 'cdk synth' to generate the template SAM with applied properties."
      ],
      "explanation": "Right. The Globals section of the SAM allows you to declare standard properties (Runtime, Timeout, Environment Variables, etc.) that apply to all resources 'AWS::Serverless::Function' (and other supported types) of the template, with properties individually defined in each function having precedence over global values — exactly the mechanism of 'define once, overwrite when necessary'.",
      "hint": "Think of which top level section of a template SAM allows you to declare standard properties for all functions, which can be individually overwritten by function when necessary.",
      "optionRationales": [
        "Right. The Globals section of the SAM allows you to declare standard properties (Runtime, Timeout, Environment Variables, etc.) that apply to all resources 'AWS::Serverless::Function' (and other supported types) of the template, with properties individually defined in each function having precedence over global values — exactly the mechanism of 'define once, overwrite when necessary'.",
        "Incorrect. Use Parameters with '!Ref' in each of the 12 function settings still require repeating the reference '!Ref ParameterName' in TODOS the 12 places — although it centralizes the VALOR in a definition of Parameter, this does not reduce the REPETITION by function of the ownership assignments of the way 'Globals' does (which applies the properties of the SEM require no reference in the definition of each function).",
        "Incorrect. Implement a custom Macro (a Transform of CloudFormation based on Lambda) for this it is significantly more complex (requires implanting and maintaining a Lambda separate processing templates) than using the 'Globals section' NATIVA of the SAM, designed exactly for this case of common properties, without additional infrastructure.",
        "Incorrect. This introduces a FERRAMENTA INTEIRAMENTE DIFERENTE (CDK) and a workflow (Aspects + synth) to solve a problem that the 'Globals' section native of SAM solves directly within the template itself SAM, without changing IAC tool."
      ]
    },
    "dva-avc-144": {
      "text": "A team has a function Lambda based on Java (using a large dependency framework, such as Spring) that suffers COLD STARTS of 6 a 10 second, causing timeouts in synchronous integrations with the API Gateway (even within the limit of 29 second, the latency perceived by the user is unacceptable). DRASTICAMENTE the latency of cold start of this Java function SEM pay for Provided Competition to keep environments warm 24/7 (the function has sporadic traffic, making Provisioned Competition constant costly). Lambda You'll have to work it out.?",
      "options": [
        "Enable AWS LAMBDA SNAPSTART for function — SnapStart works INICIALIZANDO the function UMA VEZ (running the initialization code, including the initialization of the JVM and the framework) DEPOIS that one VERSION of the function is published, and taking a SNAPSHOT of the memory state and disk of the initialized running environment — in cold start SUBSEQUENTES, instead of running the initialization COMPLETA again, the Lambda RETOMA the implementation from that SNAPSHOT in cache, reducing the latency of cold start from seconds to frequently MENOS OF 1 SEGUNDO in Java functions — SEM require Provisioned Competition (without continuously charged idle heated environments)).",
        "Rewrite the function using a custom runtime 'provided.al2' with a native GrailVM image compiled statistically instead of the standard Java runtime, at which the AWS automatically applies SnapStart without configuration.",
        "Configure the function with 'ReservedConcurrentExecutions': 0' during periods of low traffic, and trust that the Lambda automatically caches the snapshot of the LAST running environment for instant reuse at the next invocation, regardless of the time elapsed.",
        "Increase the function's assigned MEMORY to maximum (10.240 MB), since the Lambda allocated CPU proportionally to memory, and more CPU accelerates the initialization of the JVM and loading classes during initialization."
      ],
      "explanation": "Correct. SnapStart takes a snapshot of the running environment already initialized (after the release of a version) and reuses it to resume subsequent cold startups almost instantly, drastically reducing the latency of initializing Java functions without requiring Provisioned Competition and its cost of idle environments.",
      "hint": "Think of what resource the Lambda allows an already initialized running environment to be captured as snapshots and reused to resume subsequent cold start almost instantly, without keeping environments continuously heated.",
      "optionRationales": [
        "Correct. SnapStart takes a snapshot of the running environment already initialized (after the release of a version) and reuses it to resume subsequent cold startups almost instantly, drastically reducing the latency of initializing Java functions without requiring Provisioned Competition and its cost of idle environments.",
        "Incorrect. Increase memory PODE reduce the initialization time a little bit JVM (more CPU help), but it does not achieve the 'drug' reduction (from seconds to sub-second) that SnapStart provides, and AUMENTA the cost proportionally for TODA Invocation (not only cold start) — SnapStart specifically targets the initialization phase via snapshot/restoration, without a general cost increase by invocation.",
        "Incorrect. Although native GrailVM images POSSAM reduce cold startups of Java (avoiding the startup of JVM completely), this requires an effort SIGNIFICATIVO rewriting/recompilation (native image compilation has its own complexities/limitations with reflection and dynamic loading of common classes in Spring) — and SnapStart is a resource SEPARADO, OPT-IN (a configuration in function/version) that works with Java FATHER runtimes, not something 'automatically applied' to custom runtimes.",
        "Incorrect. Set Reserved Competition as 0 IMPEDE that the function be invoked (it would be THROTTLED for all invocations) — this is not related to snapshot cache, and execution environments are NOT retained indefinitely regardless of the time elapsed (are recycled after periods of inactivity); the snapshot mechanism of SnapStart is fundamentally different from the reuse/freezing of execution environments."
      ]
    }
  }
}
