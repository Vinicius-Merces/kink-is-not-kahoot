{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "dva-c02",
    "level": "avancado",
    "sourcePath": "data/exams/dva-c02/avancado.json",
    "range": "157-168",
    "generator": "offline-marian-faithful-v2"
  },
  "questions": {
    "dva-avc-157": {
      "text": "A developer is building a serverless application (function Lambda + API Gateway) using the AWS SAM And you want to TESTAR the function LOCALMENTE — invoking it with example events and making requests HTTP real for the API — ANTES to implement any resource in the AWS, to accelerate the development and purification cycle. The local environment must EMULAR the runtime of Lambda (using a Docker container that reproduces the running environment). AWS SAM CLI meet that requirement?",
      "options": [
        "Use 'sam place invoke <LogicalNameDaFuncao> -and event.json' to invoke the function locally with an example event (executed within a Docker container that emulates the runtime of the Lambda), and 'sam start location-api' to start a server HTTP place emulating the API Gateway, routing requests HTTP (via curl, browser or Postman) for the function Lambda the same way as the API Gateway would do in production — allowing testing all integration API Gateway + Lambda locally, without implanting anything in the AWS.",
        "Run 'sam package' to pack and upload artifacts to a pocket S3, and then use 'aws lambda invoke' pointing to the ARN function to test your behavior.",
        "Implant function in AWS and use the step-through debugger of AWS Toolkit (for VS Code/IDEs) directly connected to the function Lambda IMPLANTADA in the cloud, defining breakpoints in the remote code.",
        "Run 'sam validation to verify the template syntax, followed by 'sam deploy --not-execute-changeset' to create a change set without running it, allowing you to inspect the proposed changes as a 'test' form before the actual deploy."
      ],
      "explanation": "Correct.. 'sam place invoke' performs the function in a Docker container that emulates the running environment of Lambda using a test event provided, and 'sam start location-api' emulates the API Gateway locally, exposing an endpoint HTTP Rotating to the function Lambda local — together, allow you to test the full application locally before any deployment.",
      "hint": "Think of what subcomandations of SAM CLI use a Docker container to emulate the runtime of Lambda and the API Gateway directly on the developer machine, no need to deploy.",
      "optionRationales": [
        "Correct.. 'sam place invoke' performs the function in a Docker container that emulates the running environment of Lambda using a test event provided, and 'sam start location-api' emulates the API Gateway locally, exposing an endpoint HTTP Rotating to the function Lambda local — together, allow you to test the full application locally before any deployment.",
        "Incorrect. 'sam validation' checks only the SINTAXE/template structure (does not perform function), and 'sam deploy --not-execute-changeset' creates a change set of CloudFormation for REVIEW of the changes in INFRAESTRUTURA proposals, SEM execute it — neither INVOCA the function Lambda or simulates requests HTTP locally; do not meet the requirement to test function behavior/API local.",
        "Incorrect. This approach requires IMPLANTAR the function in the AWS first (against the requirement) 'ANTES to implement any resource in the AWS') and performs debugging REMOTA of a function already implanted — 'sam local invoke'/'sam start location-api' allow to test the function and API completely in environment LOCAL, without any previous staff.",
        "Incorrect. 'sam package' uploads the deployment artifacts to the S3 (preparing for a real deployment), and 'aws lambda invoke' invokes a function already IMPLANTADA the AWS for your ARN — none of these commands perform the function LOCALMENTE; the requirement is to test 'ANTES to implement any resource in the AWS', what 'sam local invoke'/'sam start location-api' directly respond."
      ]
    },
    "dva-avc-158": {
      "text": "One stage of the CodePipeline currently executed DUAS actions of the CodeBuild SEQUENCIALMENTE: First Unitary Tests (project) CodeBuild A, ~5 minutes), then 'Integration Tests' (project CodeBuild B, ~8 minutes) — totaling ~13 The two sets of tests are COMPLETAMENTE INDEPENDENTES between themselves (no data dependence or order between them) and could be executed SIMULTANEAMENTE, reducing the total time of the internship to approximately 8 minutes (the longest time). How the team should reconfigure these two actions DENTRO DO MESMO STAFF OF CodePipeline for them to execute in PARALELO?",
      "options": [
        "Divide the current stage into DOIS Separate stages — one for 'Unit Tests' and another for 'Integration Tests' — allowing the CodePipeline run them in parallel automatically, since different stages always run in a competitive way.",
        "Add Stage Level Property 'ParallelExecution: true' to the stage definition in CodePipeline, instructing the service to perform all the actions of the internship simultaneously, regardless of 'RunOrder''.",
        "Increase the 'computerType' of both projects CodeBuild for a higher type of instance (ex.: 'BUILD_GENERAL1_LARGE'), which allows the CodePipeline execute multiple actions simultaneously in the same shared build instance.",
        "Assign MESMO value of 'RunOrder' (e.g. 'RunOrder'): 1') for AMBAS the actions ('Unit Tests' and 'Integration Tests') within the definition of the stage — actions that share the same value of 'RunOrder' within an stage are executed in PARALELO for CodePipeline; actions with 'RunOrder' values' DIFERENTES are executed SEQUENCIALMENTE in ascending order, and the stage only advances to the next RunOrder when TODAS the actions of the current RunOrder conclude."
      ],
      "explanation": "Correct. CodePipeline executes in parallel all the actions of an internship that share the same value of 'RunOrder' — assigning the same 'RunOrder' to the two independent test actions makes them run simultaneously, reducing the total time of the internship to the time of the longest action (~8 minutes).",
      "hint": "Think of what property of an action of CodePipeline determines whether it performs simultaneously with other actions of the same stage or only after they conclude.",
      "optionRationales": [
        "Correct. CodePipeline executes in parallel all the actions of an internship that share the same value of 'RunOrder' — assigning the same 'RunOrder' to the two independent test actions makes them run simultaneously, reducing the total time of the internship to the time of the longest action (~8 minutes).",
        "Incorrect. CodePipeline are, POR DEFINITION, SEQUENCIAIS — one stage only starts after TODAS the actions of the stage ANTERIOR to be completed; dividing into two stages would NOT result in parallel execution (in fact, it would still be sequential, and the separation does not have the intended parallelism effect). 'RunOrder' within ONE stage is the correct mechanism for parallelism.",
        "Incorrect. There is no ParallelExecution property in stages of the CodePipeline — the actual and documented mechanism to control parallel vs. sequential execution of actions within an stage is the value of 'RunOrder' assigned to each action (actions with the same RunOrder run in parallel).",
        "Incorrect. ComputerType affects only RECURSOS (CPU/memory) available DENTRO of an individual execution of the CodeBuild, accelerating potentially CADA individual build — does not control whether MULTIPLE ACTIONS of the pipeline perform competitively; each execution of the CodeBuild occurs in its own isolated environment, and the parallel execution of the pipeline ACTIONS is controlled by 'RunOrder', not by the type of computing."
      ]
    },
    "dva-avc-159": {
      "text": "A team uses a repository at GitHub as the source of a pipeline from CodePipeline. Currently, the action of origin uses the action provider 'GitHub (Version) 1)', which depends on a TOKEN OF ACESSO PESSOAL (Personal Access Token) of GitHub stored as a secret of CodePipeline — this provider is marked as DESCONTINUADO for AWS. Team wants to migrate to approach MODERNA and RECOMENDADA integration between CodePipeline and GitHub, with refined permissions (not a wide user access token) and event-based change detection. What is this approach??",
      "options": [
        "Configure a function Lambda scheduled (via EventBridge Scheduler) which periodically clones the GitHub repository and pushes content to a mirror repository in AWS CodeCommit, using CodeCommit as the source of CodePipeline.",
        "Use a pocket of the Amazon S3 as the origin of CodePipeline, configuring a GitHub Action in the repository that compacts the code and uploads it to this pocket S3 each push.",
        "Create a connection from AWS CODESTAR CONNECTIONS (also called 'Connections' on the Developer Tools console) for GitHub — this installs a GITHUB APP in GitHub account/organization with permissions REFINADAS specific to the authorized repositories, requiring only one manual authorization on the console (the connection goes from status 'PENDING' for 'AVAILABLE'); o ARN this connection is then referenced in the action of origin of the CodePipeline using the 'CodeStarSourceConnection' action provider, with 'FullRepositoryId' and 'BranchName' — supports event-based change detection automatically without requiring a personal access token.",
        "Continue using the action provider 'GitHub (Version) 1)', but generate one NOVO GitHub's Personal Access Token with full 'repo' scope and store it in AWS Secrets Manager, referring to it as the secret of CodePipeline."
      ],
      "explanation": "Correct.. AWS CodeStar Connections uses a GitHub App with refined and granular permissions per repository, requires only a single authorization (without managing personal access tokens with wide scopes), and supports the 'CodeStarSourceConnection' action provider with event-based change detection — exactly the recommended modern approach that replaces the 'GitHub' provider (Version) 1)' discontinued.",
      "hint": "Think of what resource the AWS replaces the use of GitHub's personal access tokens with a GitHub App-based integration with repository-refined permissions, once authorized.",
      "optionRationales": [
        "Correct.. AWS CodeStar Connections uses a GitHub App with refined and granular permissions per repository, requires only a single authorization (without managing personal access tokens with wide scopes), and supports the 'CodeStarSourceConnection' action provider with event-based change detection — exactly the recommended modern approach that replaces the 'GitHub' provider (Version) 1)' discontinued.",
        "Incorrect. This continues to use the action provider DESCONTINUADO 'GitHub 1)' — just change ONDE the Personal Access Token is stored (Secrets Manager) does not solve the provider's discontinuance or permission problem AMPLAS associated with personal access tokens (which grant access on behalf of USER, not scope refined by repository like the GitHub App of CodeStar Connections).",
        "Incorrect. This approach introduces a layer of SYNCRONIZATION CUSTOMIZADA complex and ATRASO (dependent on the frequency of scheduling) — AWS CodeStar Connections provides integration NATIVA and DIRETA with GitHub (without mirroring), with almost instantaneous change detection via events, being much simpler and robust.",
        "Incorrect. This approach introduces a DEPENDENCE EXTERNA of CI (GitHub Actions) only to transfer code to S3, adding a layer of indirection and an additional CI system to manage — the AWS CodeStar Connections offers integration NATIVA and direct from CodePipeline with GitHub repository, without requiring any intermediate IC system."
      ]
    },
    "dva-avc-160": {
      "text": "A pipeline from CodePipeline is configured in the us-east region-1 and implements the infrastructure of an application via CloudFormation. The company needs that, as part of MESMO pipeline, an additional stack of CloudFormation be implanted in the eu-west region-1 (to serve European customers). By adding a deployment action directed to the eu-west region-1, the action fails because the pipeline artifacts (stored in the bucket) S3 of artifacts in us-east-1) are not directly accessible by actions executed in OUTRA region. What configuration of the CodePipeline solves this problem of actions CROSS-REGION within the same pipeline?",
      "options": [
        "Divide the implantation into DOIS separate and independent pipelines — one for us-east-1 And another one for me-west-1 — each independently driven by the same change of origin.",
        "Configure the section 'ArtifactStores' (in plural) of the pipeline, defining A 'ArtifactStore' (bucket) S3 + key KMS) PARA CADA REGION where there are stocks — including a bulk of artifacts in eu-west-1 in addition to us-east-1; o CodePipeline REPLICA AUTOMATICAMENTE the necessary artifacts for the corresponding regional bucket, allowing deployment actions directed to eu-west-1 Access the artifacts LOCALMENTE in this region, without additional manual replication configuration.",
        "Configure a Multi-Region Access Point S3 for the artifact pocket, pointing to the regional endpoints of us-east-1 and me-west-1, and refer to this access point as the only 'ArtifactStore' of the pipeline.",
        "Enable Replication Between Regions (Cross-Region Replication), CRR) of the S3 in the existing artifact pocket in us-east-1, configuring it to automatically replicate all objects to an equivalent pocket in eu-west-1."
      ],
      "explanation": "Correct. The 'ArtifactStores' configuration (a artifact store by region used by some action) is the mechanism NATIVO of the CodePipeline for cross-region support — the service automatically replicates the necessary artifacts for the destination region's bucket, allowing cross-region actions to access them locally, within the MESMO pipelines.",
      "hint": "Think of which section of the definition of CodePipeline allows to declare a bucket of artifacts by region, allowing actions in different regions to access the necessary artifacts locally.",
      "optionRationales": [
        "Correct. The 'ArtifactStores' configuration (a artifact store by region used by some action) is the mechanism NATIVO of the CodePipeline for cross-region support — the service automatically replicates the necessary artifacts for the destination region's bucket, allowing cross-region actions to access them locally, within the MESMO pipelines.",
        "Incorrect. CRR of the S3 PUDESSE copy objects between buckets, or CodePipeline Needs SABER on the artifact regional store (configured via 'ArtifactStores') for cross-region actions to use correctly — configure CRR manually in the pocket is not the mechanism INTEGRADO/supported by CodePipeline for this scenario; 'ArtifactStores' already deals with the necessary replication natively.",
        "Incorrect. Multi-Region Access Points S3 are not part of the configuration model of artifact stores CodePipeline for cross-region shares — the CodePipeline has its own native mechanism ('ArtifactStores', one per region with actions), which is the supported and documented way to deal with this scenario.",
        "Incorrect. The requirement is that the additional stack be implemented 'as part of the MESMO pipeline' — split into two separate pipelines contrary to this requirement (unified view/coordination of the deployment is lost); the 'ArtifactStores' configuration allows cross-region actions DENTRO OF ONE Pipeline, meeting the requirement without dividing the orchestration."
      ]
    },
    "dva-avc-161": {
      "text": "A service from the Amazon ECS rolling in Fargate needs a deployment strategy BLUE/GREEN: by publishing a new revision of the Task Definition, a set of tasks (TASK SET) 'the separate green' traffic must be created, Application Load Balancer must be DESVIADO gradually (or totally) from the blue (current) to the green (new), with ROLLBACK AUTOMATICAL for the set 'blue' if alarms of CloudWatch What configuration implements this deployment pattern?",
      "options": [
        "Manually Create One SEGUNDO service ECS ('') with its own listener rule in the ALB, and update MANUALMENTE records of Route 53 with weighted routing (weighted routing) to gradually divert traffic from the blue service to the green'.",
        "Use the deployment type NATIVO and FATHER ECS ('ECS' deployment controller, rolling update), configuring 'minimumHealthyPercent' and 'maximumPercent' to gradually replace existing set tasks with tasks with the new Task Definition.",
        "Configure the service ECS with the type 'deployment controller' 'CODE_DEPLOY' and use the AWS CodeDeploy with the type of deployment 'BLUE_GREEN' for ECS — an AppSpec file ('appsp.yaml') refers to the ARN the new Task Definition and the container name/port; and CodeDeploy creates a NOVO TASK SET (''), records it in a SEGUNDO target group of ALB, and diverts traffic from target group 'blue' to 'green' according to deployment configuration (e.g. 'CodeDeployDefault.ECSAllAtOnce', or Canary/Linear) — with ROLLBACK AUTOMATICAL for the blue set based on alarms of CloudWatch configured in 'DeploymentPreference''.",
        "Use AWS App Mesh, configuring virtual nodes and virtual routers to implement the traffic diversion between the old revision and the new Task Definition revision."
      ],
      "explanation": "Correct. CodeDeploy with typical deployment 'BLUE_GREEN' for ECS (demanding 'deployment controller': CODE_DEPLOY' in the service) creates a new task set, diverts traffic from ALB between two target groups according to the chosen deployment configuration, and supports automatic rollback based on alarms of CloudWatch — exactly the blue/green pattern with gradual deviation and automatic rollback described.",
      "hint": "Think of which deployment service, integrated into the ECS via a specific deployment controller, creates a separate set of tasks and diverts traffic from ALB between two target groups, with automatic rollback based on alarms.",
      "optionRationales": [
        "Correct. CodeDeploy with typical deployment 'BLUE_GREEN' for ECS (demanding 'deployment controller': CODE_DEPLOY' in the service) creates a new task set, diverts traffic from ALB between two target groups according to the chosen deployment configuration, and supports automatic rollback based on alarms of CloudWatch — exactly the blue/green pattern with gradual deviation and automatic rollback described.",
        "Incorrect. The default deployment controller of ECS (rolling update) replaces tasks GRADUALMENTE DENTRO DO MESMO service/target group (does not create a separate 'green' set or a SEGUNDO target group), and does not offer controlled traffic diversion between two environments nor automatic rollback based on alarms from CloudWatch that the CodeDeploy Blue/Green provides — does not meet the blue/green pattern described.",
        "Incorrect. MANUAL is operationally complex, prone to errors, and does not offer ROLLBACK AUTOMATICAL based on alarms of CloudWatch — o CodeDeploy with typical deployment 'BLUE_GREEN' for ECS automates precisely this creation of green set, traffic deviation via target groups of ALB (no Route 53) and automatic rollback, without manual orchestration.",
        "Incorrect. AWS App Mesh is turned to GERENCIAMENTO OF TRANSFER IN SERVICE MESH (routing between microservices, east-west communication observability) — not the mechanism PROJETADO for blue/green deployments of a Task Definition with automatic rollback based on alarms; CodeDeploy with type 'BLUE_GREEN' for ECS is the specific deployment mechanism for this standard."
      ]
    },
    "dva-avc-162": {
      "text": "A company uses AWS Organizations with 50 Member accounts distributed in 3 Organizational Units (OUs). The platform team needs to implement a set BASE resources (a track of CloudTrail, a roll of the IAM for centralized logging, and a rule of AWS config) shape CONSISTENTE in TODAS the 50 Member accounts, in MULTIPLE regions, and wants that, when NOVAS accounts are added to these OUs in the future, these same resources are implemented AUTOMATICAMENTE in the new accounts, without manual intervention. CloudFormation meets this requirement?",
      "options": [
        "Use AWS CLOUDFORMATION STACKSETS with 'service-managed permissions' integrated into AWS Organizations — direct the StackSet to the desired OOs and enable 'automatic deployment'; the StackSet implements the base template (CloudTrail, role of IAM, config rule) as 'stack instances' in TODAS the accounts of the target OYEs in the specified regions; and AUTOMATICAMENTE creates new stack instances when NOVAS accounts are added to target OOs, without manual action.",
        "Write a script that iterates about each of the 50 accounts, take a role in each account and execute 'aws cloudformation individually deploy' for each account/region combination, scheduled periodically via Amazon EventBridge to check new accounts.",
        "Use NESTED STACKS inside a single template, creating a nested stack ('AWS::CloudFormation::Stack') for each of the 50 Member accounts.",
        "Create the core features in a SINGLE stack in the management account of the Organization, and set up roles of IAM cross-account for the other 49 accounts 'assume' access to these shared resources."
      ],
      "explanation": "Correct.. CloudFormation StackSets with service-managed permissions (integration with AWS Organizations) and enabled automatic deployment implement the same template in all specified OO accounts/areas, and automatically extend this deployment to new accounts added to these OOs — precisely meeting the requirement of consistency and automation for new accounts.",
      "hint": "Think of what resource the CloudFormation was designed to implement the MESMO template in multiple accounts and regions, with integration AWS Organizations to automatically extend the deployment to new accounts.",
      "optionRationales": [
        "Correct.. CloudFormation StackSets with service-managed permissions (integration with AWS Organizations) and enabled automatic deployment implement the same template in all specified OO accounts/areas, and automatically extend this deployment to new accounts added to these OOs — precisely meeting the requirement of consistency and automation for new accounts.",
        "Incorrect. The requirement is that resources (track of CloudTrail, role of IAM, config rule) be CRIADOS EM CADA member account — create resources only in the management account and share access via cross-account roles NO REPLICA resources in the 50 accounts; StackSets is the mechanism designed to implement the MESMO EM resource set CADA target account.",
        "Incorrect. Nested Stacks operate DENTRO of a SINGLE stack account/hierarchy (a parent stack and its nested stacks are implanted in the MESMA parent stack account/region) — there is no mechanism for one in this stack to be implemented into a parent stack account. AWS DIFERENTE; StackSets is the mechanism specifically designed for deployments MULTI-CONTA.",
        "Incorrect. This approach is a RE-IMPLEMENTATION MANUAL and complex of a functionality that the CloudFormation StackSets already offers NATIVAMENTE, including AUTOMATIC IMPLEMENTING IN NOVAS CONTAS added to the OUs (which this script would not properly treat without significant additional logic of detection of new accounts) — StackSets with integration with Organizations and automatic implementation is the managed and robust solution for this requirement."
      ]
    },
    "dva-avc-163": {
      "text": "A team needs to expose a SINGLE endpoint HTTPS for a third party SaaS service to send events via webhook directly to a function Lambda. They DO NOT need advanced resources from API Gateway (road-based route for multiple functions, usage/throttling plans per client, requisition validation, custom domains with multiple routes) — only one endpoint HTTPS dedicated to this single function, with the configuration MAIS SIMPLES and MENOR overhead possible. What resource of the AWS Lambda meets this requirement?",
      "options": [
        "Use AWS AppSync to expose an endpoint GraphQL that invokes function Lambda as a solution for the mutations received via webhook.",
        "Enable one FUNCTION URL ('AWS::Lambda::Url', or via 'aws lambda create-function-url-config') directly in the function Lambda — that provides an endpoint HTTPS DEDICADO for this specific function (in the format 'https://<url-id>.lambda-url.<região>.on.aws/'), with 'AuthType' configurable ('AWS_IAM' or 'NONE') and optional configurations of CORS, SEM require the provisioning or management of a separate resource from API Gateway.",
        "Create a HTTP API minimum not API Gateway with a single route 'ANY /{proxy+}' integrated to function Lambda, using the default configuration.",
        "Configure a Application Load Balancer (ALB) with the function Lambda registered as TARGET of a target group, exposing a listener HTTPS no ALB."
      ],
      "explanation": "Correct. Function URLs provide an endpoint HTTPS dedicated and native directly in a function Lambda, with minimum configuration (AuthType), CORS optional), without requiring any additional resource of API Gateway — Exactly the simplest solution to expose a single function via HTTPS, as described in the webhook scenario.",
      "hint": "Think about which feature allows you to expose a function Lambda directly via an endpoint HTTPS dedicated, without providing any other service of API.",
      "optionRationales": [
        "Correct. Function URLs provide an endpoint HTTPS dedicated and native directly in a function Lambda, with minimum configuration (AuthType), CORS optional), without requiring any additional resource of API Gateway — Exactly the simplest solution to expose a single function via HTTPS, as described in the webhook scenario.",
        "Incorrect. Although functional, this introduces a resource ADICIONAL (API Gateway HTTP API) with its own configuration, life cycle and cost — for the described need (endpoint) HTTPS simple to UMA function, no advanced routing/throttling features), a Function URL directly responds with MENOS overhead configuration.",
        "Incorrect. Use one ALB as integration Lambda is valid for certain scenarios, but requires PROVISIONAR and GERENCIAR one Application Load Balancer (with hourly cost and listener/target group/certified configuration) — for a need SIMPLES from webhook endpoint to a single function, this is disproportionately heavier than a Function URL.",
        "Incorrect. AppSync introduces a CAMADA Fully unnecessary GraphQL for a webhook receiver waiting for an endpoint HTTPS direct and simple — Function URLs provide exactly this endpoint HTTPS direct without any layer of API additional."
      ]
    },
    "dva-avc-164": {
      "text": "A function Lambda is invoked in an ASSINCRONA way (activated by events of the Amazon S3). The team needs that, in case of SUCESSO of the invocation, PAYLOAD OF RESPOSTA of the function is automatically sent to a row of the Amazon SQS (for further processing); and that, in the case of FALHA (after Lambda exhaust the set retentatives), the original event along with error details is sent to a topic of the Amazon SNS (for alerts) — TUDO ISSO SEM write additional code DENTRO of the function to deal with this conditional routing of results. Lambda meets this requirement?",
      "options": [
        "Configure LAMBDA DESTINATIONS ('DestinationConfig' with the fields 'OnSuccess' and 'OnFailure') in function — for ASYNCRONA invocations, the Lambda ROTEIA AUTOMATICAMENTE the record of the invocation (including the PAYLOAD OF RESPOSTA of the function in case of success, or error details after exhausting the retentions in case of failure) for the configured destinations ('OnSuccess' → ARN row SQS; 'OnFailure' → ARN of topic SNS) — TUDO form NATIVA, without requiring any change in function code.",
        "Wrap function logic into a try/catch block: in the catch block, manually call the API 'Published by SNS; on the path of success, call manually API 'SendMessage's SQS.",
        "Configure a Dead Letter Queue (DLQ) pointing to the topic SNS to treat the failures, and add code DENTRO of the function to explicitly publish the success result in the row SQS before returning.",
        "Use Amazon EventBridge Pipes to route TODOS the invocation results (success and failure) for both destinations, filtering based on a custom field that the function itself should include in its response."
      ],
      "explanation": "Correct.. Lambda Destinations for asynchronous invocations automatically route the invocation result — successful payload for 'OnSuccess' and error details (after retaliatory exhaust) for 'OnFailure' — for configured destinations (SQS, SNS, Lambda or EventBridge), without requiring any additional code within the function.",
      "hint": "Think about which configuration of the Lambda, applicable to asynchronous invocations, automatically route the success result and failure details to configured destinations without requiring code in function.",
      "optionRationales": [
        "Correct.. Lambda Destinations for asynchronous invocations automatically route the invocation result — successful payload for 'OnSuccess' and error details (after retaliatory exhaust) for 'OnFailure' — for configured destinations (SQS, SNS, Lambda or EventBridge), without requiring any additional code within the function.",
        "Incorrect. DLQ treatment APENAS the case of FALHA (and contains less context of the invocation than the 'OnFailure' of Destinations), and require CODE DENTRO of the function to publish the success result in the row SQS contrary DIRETAMENTE o requirement to 'SEM write additional code DENTRO function' — Lambda Copper destinations AMBOS the cases (success and failure) natively, without additional code.",
        "Incorrect. EventBridge Pipes connects one ORIGEM to one DESTINO with optional filtering/enrichment, but not the mechanism NATIVO of the life cycle of asynchronous invocation of the Lambda for success/failure routing — in addition, requiring the function to include a 'custom field' in its filter response violates the requirement not to add code to the function; Lambda Destinations is the most simple and native mechanism for this exact case.",
        "Incorrect. This approach explicitly requires ESCREVER CODE DENTRO the function to perform the calls of publication/sending — directly contrary to the declared requirement to do this route 'SEM write additional code DENTRO function'; Lambda Destinations conducts this route in a fully declarative/configuratory manner."
      ]
    },
    "dva-avc-165": {
      "text": "A team needs to schedule the execution of a function Lambda of DUAS forms: (1) Implementation RECORRENTE daily 2h UTC for a job in a batch; and (2) UMA ONE-TIME execution at a specific future date/hour (e.g. within 45 The team wants a service MODERNO which I support AMBOS the types of scheduling (recurrent and punctual) natively, with resources such as JANELAS OF TEMPO FLEXIVES (to distribute the starting time of the invocations and reduce throttling) and policies of RETENTATIVA configurable — instead of relying only on the old 'Scheduled Rules' of CloudWatch Events. Which service meets this requirement?",
      "options": [
        "Use AMAZON EVENTBRIDGE SCHEDULER — a capacity DISTINTA and most recent EventBridge (separate from traditional 'rules'), which supports both scheduling RECORRENTES (cron/rate expressions) QUANTO SINGLE ('at', for a specific date/time future) agendas, with RETENTATIVA integrated and JANELAS OF TEMPO FLEXIVES (flexible time windows) to distribute the start of the invocations — can directly invoke a function Lambda (among other targets) for AMBOS the cases of use in a single service.",
        "Use a Scheduled Rule EventBridge (CloudWatch Events) with cron expression for the daily recurring job, and a separate state machine from Step Functions with a 'Wait' state for the future single execution.",
        "Use scheduled jobs of AWS Batch for daily recurring execution, and a function Lambda with the timeout set to the maximum (15 minutes) waiting ('sleep') until the future date/hour for the single execution.",
        "Use 'Wait' states only AWS Step Functions both for the recurring daily job (making the state machine FAZER LOOP indefinitely, waiting 24 hours between iterations) for the future single execution."
      ],
      "explanation": "Correct. Amazon EventBridge Scheduler is the modern service that natively supports both recurring (cron/rate) and unique ('at') schedules, with retentive policies and flexible configurable time windows — meeting both scenario requirements in a single service, as recommended substitute for legacy Scheduled Rules for these cases.",
      "hint": "Think of what service the AWS, distinct from traditional event rules, it supports natively both recurring schedules (cron/rate) and unique schedules for a future date, with retactatives and flexible time windows.",
      "optionRationales": [
        "Correct. Amazon EventBridge Scheduler is the modern service that natively supports both recurring (cron/rate) and unique ('at') schedules, with retentive policies and flexible configurable time windows — meeting both scenario requirements in a single service, as recommended substitute for legacy Scheduled Rules for these cases.",
        "Incorrect. FUNCIONAL, but demands DOIS MECANISMOS DIFERENTES (rules + Step Functions) to cover both requirements — the EventBridge Scheduler responds to AMBOS (recurrent and unique) NATIVAMENTE in a single service, including flexible time windows and retentive policies, without requiring an additional status machine only for punctual scheduling.",
        "Incorrect. AWS Batch doesn't have a cron schedule NATIVO without depending on the rules of the EventBridge behind the cloths; and do a function Lambda 'wait (sleep) for until 45 days is COMPLETAMENTE INVIABLE — the Maximum Timeout of a Function Lambda is of 15 MINUTOS, making this approach technically impossible for a wait of 45 days.",
        "Incorrect. Making a state machine of the Step Functions FAZER LOOP INDEFINIDAMENTE with expectations of 24 hours to implement an appointment RECORRENTE is a ANTI-FATHER (long-term execution, cumulative cost per state transition, monitoring complexity) — scheduling services NATIVOS as the EventBridge Scheduler are designed specifically for recurring schedules, being much simpler and more appropriate."
      ]
    },
    "dva-avc-166": {
      "text": "A developer is about to run 'cdk deploy' to update a stack of CloudFormation in PRODUCTION, managed via AWS CDK. Before applying the change, he wants PRE-VISUALIZAR exactly what changes (additional, modified, removed resources, and changes in policies of the IAM) would be made in the stack ATUALMENTE IMPLANTADA — similar to the 'terraform plan'' — SEM apply no real change. What command does the AWS CDK CLI meets this requirement?",
      "options": [
        "Use Change Sets CloudFormation directly: execute 'aws cloudformation create-change-set with the template synthesized by cdk synth, then 'aws cloudformation describe-change-set' to review the changes, and finally delete the change set.",
        "Run 'cdk synth' to generate the new template, and manually compare this file with the previous version of the template stored in the version control using a text diff tool (e.g. 'git diff')').",
        "Run 'cdk deploy --not-execute', which creates a change set of CloudFormation from the synthesized template, but does not execute it, allowing revision.",
        "Run 'cdk diff' — this command SINTETIZA the code CDK current in a template of the CloudFormation and the COMPARA with the stack template ATUALMENTE IMPLANTADA the AWS, displaying a readable diff of the changes that SERIAM applied (added/modified/removed resources, changes in properties, changes in policies of the IAM) — SEM modify the implanted stack."
      ],
      "explanation": "Correct. 'cdk diff' is the dedicated command to preview changes: it synthesizes the template from the code CDK current and compares it directly with what is REALMENTE implanted AWS, displaying the differences without applying any changes — exactly the preview behavior requested.",
      "hint": "Think of which command of the CDK CLI compare the synthesized template from the current code directly to what is REALMENTE implanted AWS, displaying the differences without applying anything.",
      "optionRationales": [
        "Correct. 'cdk diff' is the dedicated command to preview changes: it synthesizes the template from the code CDK current and compares it directly with what is REALMENTE implanted AWS, displaying the differences without applying any changes — exactly the preview behavior requested.",
        "Incorrect. There is no flag '--not executed' for 'cdk deploy' with this preview behavior — the dedicated and direct command to preview changes (without any effect on the implanted stack) is 'cdk diff', which does not create or leave any change set pending.",
        "Incorrect. 'cdk synth' only GERA o NOVO template from current code — compare manually with a version ANTERIOR stored in version control does not necessarily reflect what is REALMENTE implanted AWS (there may be DRIFT), in addition to requiring manual file management; 'cdk diff' compares directly against stack REALMENTE implanted, automated.",
        "Incorrect. FUNCIONALMENTE possible, this sequence requires MULTIPLES PASSOS MANUAIS (synth, upload, change set creation, description, cleaning exclusion) — 'cdk diff' performs this preview of changes like A ONE COMANDO convenient, without leaving change sets pending for further cleaning."
      ]
    },
    "dva-avc-167": {
      "text": "A project of the CodeBuild perform integration tests that need to connect to an instance Amazon RDS located in SUBNETS PRIVADAS of a VPC (without public access to the Internet/RDS). By default, the CodeBuild executes builds in a network managed by AWS, FORA Of any VPC of the customer — as a result, the build fails to connect to the instance RDS, with 'connection timed out' error. What design configuration CodeBuild solves this network connectivity problem?",
      "options": [
        "Configure the project's 'VpcConfig' property CodeBuild, specifying the ID of the VPC, the IDs of SUBNETS PRIVADAS where the instance RDS is located, and a SECURITY GROUP appropriate — this makes the build environment run with ENIs (Elastic Network Interfaces) attached to VPC/specified subnets, giving the built environment ACESSO OF REDE resources in private subnets (such as the RDS), as long as the rules of the security group allow connection to the database door.",
        "Create a VPC Endpoint (AWS PrivateLink) for the CodeBuild within the VPC, allowing the instance RDS start connections with the build environment of CodeBuild.",
        "Implement integration tests with the database in a function Lambda separate implemented DENTRO of VPC, and do the CodeBuild invoke this function Lambda to perform the tests, passing the results back via the payload response of the Lambda.",
        "Returning the instance RDS PUBLICAMENTE ACCESSIBLE and add service IP intervals CodeBuild to security group of the RDS to allow connection."
      ],
      "explanation": "Correct. 'VpcConfig' in the project CodeBuild connects the build environment to VPC specified through ENIs on the supplied subnets, granting network access to resources on private subnets (as a RDS) — solving directly the connection timeout error, provided that the security group allow traffic at the correct door.",
      "hint": "Think about which project configuration CodeBuild connects the build's own running environment to a VPC specific, allowing network access to resources in private subnets.",
      "optionRationales": [
        "Correct. 'VpcConfig' in the project CodeBuild connects the build environment to VPC specified through ENIs on the supplied subnets, granting network access to resources on private subnets (as a RDS) — solving directly the connection timeout error, provided that the security group allow traffic at the correct door.",
        "Incorrect. RDS publicly accessible is a RISCO OF SIGNIFICANT SECURITY (exhibition of a database on the Internet), and the CodeBuild is a service GERENCIADO whose output IP addresses are NOT fixed/dedicated (varie between executions), making this approach both INSEGURA how much POUCO TRUST; 'VpcConfig' resolves connectivity without exposing the RDS Publicly.",
        "Incorrect. VPC (PrivateLink) for the CodeBuild allow the AMBIENTE OF BUILD access APIs of services of the AWS Privately (e.g., call the API of the S3/CodeBuild without leaving the VPC) — DO NOT put INTERFACE OF REDE of the built environment itself DENTRO of VPC to reach resources as an instance RDS; 'VpcConfig' in the project CodeBuild is what effectively connects the build environment to the network of VPC.",
        "This approach adds a FUNCTION LAMBDA INTERMEDIATELY only as a network 'bridge' — unnecessary architectural complexity when the native 'VpcConfig' property of the project itself CodeBuild directly resolves the need for network connectivity for private subnets."
      ]
    },
    "dva-avc-168": {
      "text": "A team wants to launch a new functionality (a new checkout stream) in an application based on Lambda form GRADUAL — starting with 10% of the invocations using the new flow, increasing progressively — with the ability to make ROLLBACK INSTANTANEOUS for 0% if the error rate increases, SEM need to reimplant function code Lambda The function should consult the current value of the feature flag efficiently (low latency, with local cache) in each invocation. AWS meets this requirement?",
      "options": [
        "Create aliases Lambda Separated with weighted rotation (90%/10%) pointing to DUAS different versions of the function code (one with old functionality, one with new checkout flow)).",
        "Store the feature flag as a VARIABLE OF AMBIENTE function Lambda, and update it incrementally via 'UpdateFunctionConfiguration' to implement the gradual rollout by percentage.",
        "Store the value of the feature flag in a table DynamoDB, and do the function call 'GetItem' in CADA Invocation to check the current value of the flag.",
        "Use AWS APPCONFIG — define a 'Configuration Profile' representing the feature flag, with a 'Deployment Strategy' specifying a ROLLOUT GRADUAL POR PORCENTAGEM over time (e.g., increasing 10% at each interval); integrate the function Lambda EXTENSION (LAYER) of the AWS AppConfig for Lambda, What do you do? CACHE LOCAL of the configuration and updates it periodically, allowing the function to read the current value of the flag with minimum latency; rollback is done by implementing a new version of configuration with 0%, and AppConfig can integrate with alarms from CloudWatch for PAUSAR/REVERTER automatically a deployment if error metrics exceed a threshold."
      ],
      "explanation": "Correct. AWS AppConfig was designed specifically for feature flags with gradual rollout by percentage, instant rollback (new configuration version), integration with alarms of CloudWatch for automatic stop/rollback, and an extension Lambda which makes local cache of the configuration for low latency reading — attending to the TODOS scenario requirements without code re-implantation.",
      "hint": "Think of what service the AWS it was designed specifically for feature flags with gradual rollout percentage, instant rollback and an extension of Lambda which makes local cache configuration.",
      "optionRationales": [
        "Correct. AWS AppConfig was designed specifically for feature flags with gradual rollout by percentage, instant rollback (new configuration version), integration with alarms of CloudWatch for automatic stop/rollback, and an extension Lambda which makes local cache of the configuration for low latency reading — attending to the TODOS scenario requirements without code re-implantation.",
        "Incorrect. Environment variables are SINGLE values applied to TODA the function (does not allow X% of individual INVOCATIONS to use one value and the rest to use another) — in addition, each call to 'UpdateFunctionConfiguration' changes the function configuration to TODAS the instances, not offering a gradual percentage rollout POR INVOLVEMENT as AppConfig offers natively.",
        "Incorrect. This approach requires MANTER And IMPLANTAR DUAS VERSIONS CODE function (one for each flag state), mixing the concept of DEPLOYMENT (code versions) with FEATURE FLAGGING (alter behavior within the MESMO code) — is more complex than necessary for a simple flag, and does not offer the automatic rollback capabilities based on alarms that AppConfig offers.",
        "Incorrect. This adds LATINITY (a call to the DynamoDB) in TODA invocation, and does not offer gradual rollout strategies per percentage, automatic rollback based on alarms of CloudWatch, or CACHE LOCAL low latency that the AppConfig extension provides natively."
      ]
    }
  }
}
