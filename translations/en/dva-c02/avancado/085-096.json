{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "dva-c02",
    "level": "avancado",
    "sourcePath": "data/exams/dva-c02/avancado.json",
    "range": "085-096",
    "generator": "offline-marian-faithful-v2"
  },
  "questions": {
    "dva-avc-085": {
      "text": "A company wants passwordless authentication using a Amazon Cognito User Pool: user informs e-mail, receives a unique usage code (OTP) by SMS/e-mail and enter the code to log in — NENHUMA password is defined at no time. The team needs to implement a fully customized challenge/response flow using Lambda triggers of the Cognito. What set of Lambda triggers implements this flow CUSTOM_AUTH?",
      "options": [
        "Implement 'Pre Sign-up' trigger to automatically confirm users and 'Pre Token Generation' trigger to issue tokens without password validation.",
        "Implement 'Pre Authentication' and 'Post Authentication' triggers, setting User Pool to 'ALLOW_USER_PASSWORD_AUTH' but with the empty password field.",
        "Configure the MFA User Pool as 'OPTIONAL' with SMS as a second factor, and make users authenticate with 'USER_PASSWORD_AUTH' using a fixed password known to the customer.",
        "Implement the triggers 'Define Auth Challenge', 'Create Auth Challenge' and 'Verify Auth Challenge Response', with the client initiating authentication via flow 'CUSTOM_AUTH' ('Start Auth with 'AuthFlow': CUSTOM_AUTH') — 'Define Auth Challenge' orchestra what challenge to present below, 'Create Auth Challenge' generates and sends OTP (ex: way SNS/SES), e 'Verify Auth Challenge Response' validates the code sent by the user."
      ],
      "explanation": "Right. CUSTOM_AUTH of the Cognito, orchestrated by these three Lambda triggers, allows to implement any fully customized challenge/response mechanism, including OTP by SMS/'Define Auth Challenge' decides the sequence of challenges, 'Create Auth Challenge' generates/sends the code, and 'Verify Auth Challenge Response' validates the user's response — exactly the passwordless flow described.",
      "hint": "Think of which Lambda triggers of the Cognito allow you to define, generate and verify a fully customized challenge during login, replacing default password validation.",
      "optionRationales": [
        "Right. CUSTOM_AUTH of the Cognito, orchestrated by these three Lambda triggers, allows to implement any fully customized challenge/response mechanism, including OTP by SMS/'Define Auth Challenge' decides the sequence of challenges, 'Create Auth Challenge' generates/sends the code, and 'Verify Auth Challenge Response' validates the user's response — exactly the passwordless flow described.",
        "Incorrect. 'Pre Authentication' and 'Post Authentication' execute ANTES/DEPOIS of an attempt of standard authentication validate credentials — they cannot SUBSTITUIR the validation stage of credentials for a challenge OTP customised; moreover, 'USER_PASSWORD_AUTH' still requires ALGUM password value, contradicting the 'no password requirement is defined'.",
        "Incorrect. 'Pre Sign-up' runs during or CADASTRO of the user (not in login), and 'Pre Token Generation' executes after a successful authentication to modify token claims — neither implements a flow of DESAFIO/RESPOSTA during login; they do not provide a mechanism to send/validate a OTP as part of your own login attempt.",
        "Incorrect. Use a fixed password to TODOS users is a serious security failure (anyone who knows the fixed password and someone's e-mail/user would pass through the first factor), and the MFA standard is a factor ADICIONAL on password authentication, not a replacement that implements a fully passwordless flow based only on OTP."
      ]
    },
    "dva-avc-086": {
      "text": "An IC/CD pipeline executed at GitHub Actions needs to deploy resources in AWS (ex: way CloudFormation/CDK). The team wants EVITAR store access keys from AWS long-term as secrets of GitHub, allowing the pipeline to obtain temporary credentials from AWS dynamically in each execution of the workflow, restricted to a roll of the IAM specific and limited to GitHub specific repositories/branches. What configuration of the AWS is recommended?",
      "options": [
        "Create a user IAM dedicated to the IC/CD pipeline, generate long-term access keys and store them as encrypted secrets of GitHub Actions, rotating them manually to each 90 days.",
        "Create a IAM OIDC Identity Provider for 'token.actions.githubusercontent.com', and create a role of IAM with a confidence policy that allows 'sts:AssumeRoleWithWebIdentity' from this provider OIDC, with a 'Condition' restricting the claim 'token.actions.githubusercontent.com:sub' to specific repositories/branches (e.g. 'repo:my-org/my-repo:ref:refs/heads/main'); the GitHub Actions workflow uses the action 'aws-actions/configures-aws-credentials' to change the token OIDC of GitHub for temporary credentials of AWS ♪ I saw that roll ♪.",
        "Configure a Amazon Cognito Identity Pool with an unauthenticated access role (guest) that has the necessary deployment permissions, and make the workflow call 'GetCredentialsForIdentity' without any login token.",
        "Use AWS Secrets Manager to store a user's access keys IAM, and make the GitHub Actions workflow call 'GetSecretValue' (using a separate set of bootstrap credentials) to recover them at the beginning of each execution."
      ],
      "explanation": "Right, the federation. OIDC between GitHub Actions and the IAM allows each workflow execution to obtain temporary credentials via 'AssumeRoleWithWebIdentity', without any long-term credentials stored in GitHub. The condition in the token 'sub' claim OIDC restricts which repositories/branches can assume the role, meeting exactly the scope and safety requirement described.",
      "hint": "Think of what resource the IAM allows an external identity provider (such as GitHub Actions) to exchange a token signed by temporary credentials of the AWS, without storing long-term secrets.",
      "optionRationales": [
        "Right, the federation. OIDC between GitHub Actions and the IAM allows each workflow execution to obtain temporary credentials via 'AssumeRoleWithWebIdentity', without any long-term credentials stored in GitHub. The condition in the token 'sub' claim OIDC restricts which repositories/branches can assume the role, meeting exactly the scope and safety requirement described.",
        "Incorrect. This is exactly the long-term credentials approach the team wants EVITAR; the federation OIDC provides dynamically generated temporary credentials without storing any long-term secrets in GitHub.",
        "Incorrect. This approach still depends on user access keys IAM OF LONGA DURATION (only relocated to Secrets Manager) and still requires a separate set of bootstrap credentials to call 'GetSecretValue' first — does not eliminate long-term credentials, just adds a layer of address.",
        "Incorrect. Unauthenticated access/guest of Cognito Identity Pools is intended for anonymous users of applications with minimum permissions (e.g. public content reading) — grant deployment permissions to a guest role NO AUTENTICADA would allow QUALQUER PESSOA who discovered the ID of Identity Pool assumed these permissions, a serious security risk, and does not restrict access to GitHub specific repositories/branches."
      ]
    },
    "dva-avc-087": {
      "text": "Account A has a pocket of the Amazon S3 with encrypted objects using a Customer Managed Key (CMK) of the KMS, also belonging to Account A. An application running in Account B (via a role of IAM) You need to call 's3:GetObject' on these objects and successfully describe them using the CMK A. The team has already set up the S3 (Account A) to allow the roll of Account B to call 's3:GetObject', and politics IAM of the role of Account B already includes 'kms:Decrypt no ARN of CMK. Still, the description fails with access error denied from the KMS. What's missing?",
      "options": [
        "Make the role of Account B assume a role of Account A with permissions 'kms:Decrypt' via 'sts:AssumeRole', and use the credentials of this role for the so-called 'GetObject' of S3.",
        "A KEY POLICY of CMK (in Account A) you also need to explicitly grant to the role of Account B (or Account B as principal, combined with the policy of IAM of roll) permission to use 'kms:Decrypt' — for access between accounts to a CMK of the KMS, TANTO the key policy (resource-based policy attached to the CMK, acting as the final gate for all key access) QUANTO policy IAM the main caller need to grant permission; the key policy update is what is missing here.",
        "Add to Bucket Policy S3 of the Account A declaration granting 'kms:Decrypt' to the role of Account B.",
        "Enable 'BucketKeyEnabled' (S3 Bucket Keys) in the standard Bucket encryption configuration, which reduces the number of calls to API of the KMS and could solve problems of access between accounts."
      ],
      "explanation": "Correct. Access between accounts to one CMK of the KMS requires a concession in DUAS layers: the key policy of CMK (which acts as the primary/final access control for the key) should allow the main account B, and the policy IAM of this main should allow the action KMS. Politics IAM of the role of Account B is already correct; the missing piece is the key policy of CMK in the Account Do not grant access between accounts.",
      "hint": "Think of how many layers of policy control access to a key to KMS when the main caller belongs to another account of the AWS.",
      "optionRationales": [
        "Correct. Access between accounts to one CMK of the KMS requires a concession in DUAS layers: the key policy of CMK (which acts as the primary/final access control for the key) should allow the main account B, and the policy IAM of this main should allow the action KMS. Politics IAM of the role of Account B is already correct; the missing piece is the key policy of CMK in the Account Do not grant access between accounts.",
        "Incorrect. Bucket police grant permissions for DO ACTIONS S3 (ex.: 's3:GetObject') on the Bucket/Objects resource — they cannot grant permissions for shares of the KMS on a key from the KMS, which is a resource SEPARADO governed by its own key policy.",
        "Incorrect. Although the assumption of role between accounts is a valid standard in general, the scenario already states that the role of Account B has 's3:GetObject' (via bucket policy) and 'kms:Decrypt' (via its own policy IAM) — the missing piece is specifically the key policy of CMK give access between accounts, which 'AssumeRole' alone does not resolve (the assumed role would face the MESMO key policy gate).",
        "Incorrect. S3 Bucket Keys is an optimization of CUSTO/DESEMPENHO (reducing the volume/cost of calls to API of the KMS for SSE-KMS) — does not alter or circumvent the requirements of CONTROLE OF ACESSO of the key policy of the CMK for principals of other accounts."
      ]
    },
    "dva-avc-088": {
      "text": "Account A has a secret in AWS Secrets Manager containing database credentials. An application in Account B (via a role of IAM whose policy IAM already includes 'secretsmanager:GetSecretValue', plus 'kms:Decrypt's key KMS of secrecy) needs to recover this secret, but calls to 'GetSecretValue' from Account B fail with denied access. What additional configuration in Account A is required?",
      "options": [
        "Enable replication between secret accounts to Account B using the multi-region replication of secrets feature of Secrets Manager.",
        "Enable automatic secret rotation, as the rotation updates the secrecy permissions for all accounts that access it.",
        "Attaching a POLICY BASEADA EM RECURSO to secrecy (in Account A) granting the role (or Account B as principal) permission to call 'secretsmanager:GetSecretValue' — as well as the key policies of KMS, secrets of Secrets Manager support resource-based policies, which are necessary (in addition to IAM of the main caller) for access ENTRE CONTAS to secrecy.",
        "Share the key KMS of the secret with Account B through key policy key, since the secrets are encrypted with KMS."
      ],
      "explanation": "Right, for access between accounts to a secret of the Secrets Manager, a resource-based policy attached to the secret itself is needed, granting access to the main account of the other account — in a similar way to key policies of the KMS. Politics IAM of the role of Account B is already correct (according to the scenario); the missing piece is the policy of recourse of secrecy in Account A.",
      "hint": "Think of what kind of policy, attached directly to the resource (and not to the main caller), is needed to grant access to resources of the Secrets Manager between different accounts of AWS.",
      "optionRationales": [
        "Right, for access between accounts to a secret of the Secrets Manager, a resource-based policy attached to the secret itself is needed, granting access to the main account of the other account — in a similar way to key policies of the KMS. Politics IAM of the role of Account B is already correct (according to the scenario); the missing piece is the policy of recourse of secrecy in Account A.",
        "Incorrect. Although the key KMS of the secret really needs to allow 'kms:Decrypt' for the role of Account B (which the scenario claims to be already configured), this in itself does not grant permission on the RECURSO of the Secrets Manager ('secretsmanager:GetSecretValue') — the policy of recourse to secrecy itself is the missing piece to control access to the recourse to secrecy.",
        "Incorrect. Multi-region replication creates a REPLIES of secrecy in a different region DENTRO DA MESMA CONTA, for the purpose of disaster recovery/multi-region access — is not a mechanism for granting a CONTA DA AWS DIFERENTE access to a secret.",
        "Incorrect. Automatic rotation only updates the stored value of the secret and credentials in the destination database — it does not manage, in any way, access permissions between accounts to secrecy."
      ]
    },
    "dva-avc-089": {
      "text": "A function AWS Lambda has environment variables containing sensitive configuration values. By default, the Lambda encrypts environment variables at rest using a key managed by AWS (AWS The security team requires the use of a CUSTOMER MANAGED KEY (CMK) of the KMS for centralized key management and audit via CloudTrail. After setting up the function to use one CMK in the encryption of the environment variables, the function fails at the time of INVOCATION with a denied access error related to the KMS (the handler code is never reached). What configuration is probably missing?",
      "options": [
        "The reserved competition function Lambda is defined as 0, preventing the function from being invoked while sensitive environment variables are configured.",
        "A EXECUTION ROLE function Lambda is without permission 'kms:Decrypt CMK — when one CMK (instead of the key managed by AWS) is used to encrypt environment variables, the service itself Lambda You need to call 'kms:Decrypt' (using the execution roll permissions of the function) to describe the environment variables before invoking the handler; without this permission, the invocation fails even before the handler code is executed.",
        "The implementation package of the function exceeds the size limit when encrypted with a custom managed key, since encrypted packages with CMK are larger than those encrypted with the key managed by AWS.",
        "The key spec of CMK is incompatible with the encryption of environmental variables of the Lambda — the encryption of environmental variables of the Lambda requires an asymmetric key (RSA) of the KMS, but a symmetric key has been configured."
      ],
      "explanation": "Correct. CMK to encrypt environment variables, the service Lambda you need to describe them before each invocation, using 'kms:Decrypt' with the permissions of execution roll of the function on that CMK. If this permission has not been added to the execution roll (something that is NOT necessary when using the key managed by the AWS, then the error only appears after changing to the CMK), the invocation fails immediately with denied access error of the KMS, before the handler execute.",
      "hint": "Think of who (what service/identity) needs to describe the environment variables before the handler is executed, and what permissions this identity has over the key of the KMS configured.",
      "optionRationales": [
        "Correct. CMK to encrypt environment variables, the service Lambda you need to describe them before each invocation, using 'kms:Decrypt' with the permissions of execution roll of the function on that CMK. If this permission has not been added to the execution roll (something that is NOT necessary when using the key managed by the AWS, then the error only appears after changing to the CMK), the invocation fails immediately with denied access error of the KMS, before the handler execute.",
        "Incorrect. TIPO of the key of the KMS (managed by AWS vs. managed by the customer) does not significantly alter the TAMANHO of the encrypted values of the environment variables to the point of triggering size limits of the implantation package — this is not a real restriction related to the described failure.",
        "Incorrect. Reserved competition defined as 0 prevent TODAS the invocations with an error of THROTTLING, not a denied access error related to KMS — this does not match the failure mode described.",
        "Incorrect. Encryption of environmental variables of the Lambda uses a SIMETRIC key from KMS (the standard/usual key type of the KMS) — That's the guy. CORRETO of key, not a problem; asymmetric keys are not used for this purpose, and switch to an incorrect would."
      ]
    },
    "dva-avc-090": {
      "text": "A company wants to allow developers to create their own roles in IAM for your duties Lambda (self-service, via Terraform/CDK applied with permissions IAM of the developers themselves), but it needs a safeguard by ensuring that NENHUMA role created by a developer can JAMAIS have wider permissions than a predefined policy 'MaxDeveloperPermissions' (e.g. no role created by a developer should be able to execute 'iam:*', 'organizations:*' or access resources from other teams), regardless of which permission policies the developer attaches to the roles he creates. IAM apply this restriction?",
      "options": [
        "Require developers to use AWS CloudFormation StackSets to Create Roles IAM, since StackSets apply a maximum permission template to all stacks created.",
        "Use PERMISSIONS BOUNDARIES of the IAM — require that all of the IAM created by developers have a boundary permissions defined as the managed policy 'MaxDeveloperPermissions' (applied via a condition in the policy IAM of the developers themselves in the actions 'iam:CreateRole'/'iam:PutRole PermissionsBoundary', demanding that 'iam:PermissionsBoundary' be equal to ARN a boundary permit defines the maximum permit that a roll may have, regardless of the permit policies attached to it — the permit EFETIVAS are the INTERSECT between the policies of the roll and its permissions.",
        "Attach the 'MaxDeveloper Permissions' policy directly as the Permission Policy (not as a boundary) of any role created by developers, via a Terraform module pattern.",
        "Use Service Control Policies (SCPs) of the AWS Organizations attached to the OR of the developers, denying actions 'iam:*' and 'organizations:*' for all the principals of this OR."
      ],
      "explanation": "Right. Boundary permits are the native mechanism of IAM designed exactly for this scenario: limit the maximum of permissions that roles/users CRIADOS by a less privileged main may have, regardless of the permission policies that this main annex to them. Requiring that every roller created by developers has the boundary 'MaxDeveloper Permissions', the effective permissions of any roller created can never exceed that boundary.",
      "hint": "Think of what resource the IAM defines the maximum number of permissions that an identity may have, regardless of the permission policies attached to it.",
      "optionRationales": [
        "Right. Boundary permits are the native mechanism of IAM designed exactly for this scenario: limit the maximum of permissions that roles/users CRIADOS by a less privileged main may have, regardless of the permission policies that this main annex to them. Requiring that every roller created by developers has the boundary 'MaxDeveloper Permissions', the effective permissions of any roller created can never exceed that boundary.",
        "Incorrect. SCPs apply to TODOS the mains of an account/OR (including the roles that developers CRIAM, since the permissions of these roles are also limited by the SCPs of the OU); but, SCPs are a safeguard at the level of CONTA/OR defined by an administrator with access to the Organizations, separate from the specific mechanism to limit what roles INDIVIDUAIS created by a developer may have in relation to 'MaxDeveloperPermissions' — permissions boundaries are the native mechanism of IAM specifically designed to limit roles permissions created by a less privileged principal.",
        "Incorrect. CloudFormation StackSets manage stack deployment in multiple accounts/regions — they do not in themselves apply a limit of 'maximum permissions' to the roles of IAM defined DENTRO of the implanted templates; the limit on which permissions a created role can have is what permissions boundaries provide.",
        "Incorrect. Attaching a policy as a policy of permission REGULAR grants these permissions, but no LIMITA which OUTRAS permission policies (which the developer could also attach) the role may have — a boundary permission, on the other hand, limits the maximum EFETIVO of roller permits regardless of which other policies are annexed, which is the safeguard described."
      ]
    },
    "dva-avc-091": {
      "text": "A multi-tenant SaaS application uses a Amazon Cognito User Pool for authentication. Each user belongs to a specific 'tenant' (stored as a custom user attribute, 'custom:tenant_id'). The application's backend APIs (behind the API Gateway with an Authorizer COGNITO_USER_POOLS) need to receive the user's tenant_id as a CLAIM directly in the token JWT (access/ID token), for which functions Lambda can extract it from event.requestContext.authorizer.claims' SEM an additional query to the database by request. How the User Pool can be configured to include this customised claim in the tokens issued?",
      "options": [
        "Mark custom:tenant_id as a writeable attribute when creating the User Pool client app, since saveable custom attributes are automatically included as user's claims. JWT.",
        "Implement one Lambda trigger 'Post Authentication' calling the API 'AdminUpdateUserAttributes Cognito to update the 'custom:tenant_id' attribute immediately before tokens are issued.",
        "Implement one Lambda trigger 'Pre Token Generation' in User Pool — this trigger runs after authentication succeeds, but ANTES of tokens to be issued, and may ADICIONAR, SOBRESCREVER or SUPRIMIR claims in tokens ID and/or accesses (e.g. by reading the user's custom:tenant_id attribute and adding it as a tenant_id claim) — allowing downstream services to read tenant_id directly from the claims of JWT without further consultation.",
        "Configure a Lambda Authorizer of API Gateway (in addition to the Authorizer) COGNITO_USER_POOLS (c) which consults or DynamoDB to get the user's tenant_id and inject it in the context of the request."
      ],
      "explanation": "Correct. The trigger 'Pre Token Generation' is executed immediately prior to the issue of the tokens and was designed specifically to allow adding/modifying claims in the tokens ID and access based on any custom logic (e.g. read user attributes and add them as claims). This allows 'tenant_id' to be available directly in the JWT, no additional queries required by request.",
      "hint": "Think of which Lambda trigger of Cognito executes immediately before the issue of tokens and has the ability to add custom claims to them.",
      "optionRationales": [
        "Correct. The trigger 'Pre Token Generation' is executed immediately prior to the issue of the tokens and was designed specifically to allow adding/modifying claims in the tokens ID and access based on any custom logic (e.g. read user attributes and add them as claims). This allows 'tenant_id' to be available directly in the JWT, no additional queries required by request.",
        "Incorrect. The fact that a custom attribute is 'recordable' by the customer controls whether the APPLICATION CLIENTE can update the attribute value via 'UpdateUserAttributes' — this does NOT determine for itself whether/how the attribute appears as a CLAIM in the emitted tokens; the trigger 'Pre Token Generation' is the mechanism to shape the claims of the tokens.",
        "Incorrect. 'Post Authentication' executes DEPOIS that authentication is completed, but does not have the ability to MODIFICAR AS CLAIMS of the tokens that are being issued — updating the user's stored attribute does not retroactively alter the claims already in preparation for the issue ATUAL of token; 'Pre Token Generation' is specifically designed to shape claims before issue.",
        "Incorrect. This approach reintroduces the query to the database by request that the team wants EVITAR, and use DOIS Authorizers (COGNITO_USER_POOLS one more Lambda Custom Authorizer) for the same method as API Gateway is not how the authors compose — 'Pre Token Generation' already incorporates the claim directly into JWT only once, at the time of issue of token, without need of consultation by request."
      ]
    },
    "dva-avc-092": {
      "text": "A company wants to ensure that a sensitive Amazon S3 can only be accessed from DENTRO of VPC of the company (e.g. by bodies) EC2 and functions Lambda in private subnets), and that requests originated from the public internet (even with credentials of the AWS valid) are NEGADAS. How this should be configured?",
      "options": [
        "Remove public access settings from the pocket ('Block all public access') and ensure that no bucket policy grants 'Main': *'.",
        "Configure the Bucket Policy S3 to allow access only from the range CIDR of VPC, using a 'Condition' with 'IpAddress' on the key 'aws:SourceIp'.",
        "Create a Gateway VPC Endpoint for the Amazon S3 the VPC, and add to the sensitive pocket a statement in the pocket policy with 'Effect: Deny' for all the main when the request does NOT originate through the VPC specific endpoint, using a 'Condition' with 'StringNotEquals' in the context key 'aws:SourceVpce' (or 'aws:sourceVpc') defined as the Gateway Endpoint ID (or VPC) — this denies access to S3 for any requisition that does not deal with the VPC configured endpoint, regardless of main caller credentials.",
        "Put the pocket of S3 on a private subnet of VPC, removing your endpoint DNS public."
      ],
      "explanation": "Right. 'aws:Source'/'aws:sourceVpc' allow a bucket policy to identify if a request to S3 came through a VPC Specific endpoint (or VPC A Deny statement based on StringNotEquals in that key denies access to any request that does not pass through the configured endpoint, regardless of how valid the main's credentials are — exactly meeting the requirement to restrict access to requests originating from within the VPC.",
      "hint": "Think of what condition keys to a bucket policy of the S3 identify if a request arrived through an endpoint VPC specific, regardless of the credentials used.",
      "optionRationales": [
        "Right. 'aws:Source'/'aws:sourceVpc' allow a bucket policy to identify if a request to S3 came through a VPC Specific endpoint (or VPC A Deny statement based on StringNotEquals in that key denies access to any request that does not pass through the configured endpoint, regardless of how valid the main's credentials are — exactly meeting the requirement to restrict access to requests originating from within the VPC.",
        "Incorrect. 'aws:SourceIp' reflects the public IP of the request as seen by AWS; for traffic routed through a Gateway VPC Endpoint, this value does NOT match the range CIDR private VPC (Gateway Endpoints have no IP addresses in the traditional sense, and 'aws:SourceIp' is not a reliable way to identify traffic routed by VPC endpoint) — 'aws:Source'/'aws:sourceVpc' are the correct condition keys for this purpose.",
        "Incorrect. Although it is an important general good practice (preventing unintended public/anonymous access), this DOES NOT prevent a major AUTENTICADO with credentials IAM valid and permissions no S3 access the pocket on the public internet (e.g. from outside the VPC) — the scenario requires NEGAR access even with valid credentials, unless the request originates from the VPC, which requires the VPC endpoint combined with condition 'aws:Source'.",
        "Incorrect. Amazon S3 NON are resources of VPC and cannot be 'placed' within a subnet — the S3 is a global/regional service accessed through its endpoints of API public (or VPC endpoints for private connectivity) — this option describes an architecturally impossible configuration."
      ]
    },
    "dva-avc-093": {
      "text": "An application uses one Amazon Cognito User Pool with two groups: 'PremiumUsers' and 'StandardUsers'. After login, users get credentials from AWS see one Cognito Identity Pool to access a pocket of S3. Users of the 'PremiumUsers' group should receive a role with access to the 'premium-content' prefix, while users of 'StandardUsers' should receive a role restricted to the 'standard-content' prefix. As Identity Pool should be configured to assign different roles to the IAM based on the group Cognito User Pool to which the user belongs?",
      "options": [
        "Use 'Use default roll' for the authenticated Role of Identity Pool, and make the application call different prefixes from S3 based on a verification on the side of the CLIENTE of belonging to the user group, using MESMA role of IAM for all authenticated users.",
        "Configure Identity Pool Role Mapping for the User Pool Provider as 'Choose roll from token', setting rules that map belonging to the 'PremiumUsers' group (present in claim) 'cognito:token groups) for the premium role of IAM, e 'StandardUsers' for standard roll — Identity Pool inspects claim 'cognito:user pool token groups during 'GetCredentialsForIdentity' to select the appropriate role.",
        "Attach policies of the IAM directly to each user in Cognito User Pool, granting access to S3 not appropriate prefix.",
        "Create Two Cognito Separate Identity Pools (one for PremiumUsers, one for StandardUsers), and make the application choose which Identity Pool to call based on the user group, read from the ID token on the client."
      ],
      "explanation": "Right. The Identity Pool 'Choose role from token' option allows you to set rules that examine identity token claims (such as 'cognito:groups') to dynamically select, on the server side, which role of IAM assign during 'GetCredentialsForIdentity'. This ensures that PremiumUsers and StandardUsers receive credentials from AWS with different actual permissions, based on their membership in groups at the User Pool.",
      "hint": "Think about which role mapping configuration of the Cognito Identity Pool allows you to examine authentication token claims (such as belonging to groups) to dynamically choose which role of IAM assign.",
      "optionRationales": [
        "Right. The Identity Pool 'Choose role from token' option allows you to set rules that examine identity token claims (such as 'cognito:groups') to dynamically select, on the server side, which role of IAM assign during 'GetCredentialsForIdentity'. This ensures that PremiumUsers and StandardUsers receive credentials from AWS with different actual permissions, based on their membership in groups at the User Pool.",
        "Incorrect. Use a single default roll for TODOS authenticated users means that credentials IAM returned are IDENTICAL to Premium and Standard; depend on logic on the side of CLIENTE to restrict access is NOT a real safety limit — role mapping based on token claims is necessary to assign roles to the IAM DIFERENTES on the server side.",
        "Incorrect. Although technically possible, this requires logic on the side of the CLIENTE to choose between Identity Pools (which a malicious client could manipulate to choose Identity Pool 'premium' regardless of actual group membership) — a single Identity Pool with token-based role mapping rules is the standard and most robust approach.",
        "Incorrect. Users of a Cognito User Pool are NOT major of IAM and cannot have policies of the IAM directly annexed — policies/roles of the IAM are relevant only to the credentials of the AWS obtained via an Identity Pool, not for User Pool user objects themselves."
      ]
    },
    "dva-avc-094": {
      "text": "A company owns a Customer Managed Key (CMK) of the KMS used to encrypt data via envelope encryption ('GenerateDataKey'/'Decrypt') on many objects of the S3 accumulated in the last 3 years. The security team enables the AUTOMATICAL ROTATION OF CHAVE ((annual) for this CMK. After rotation occurs, encrypted objects ANTES rotation (using key material) ANTIGO) can still be described? Something needs to change in the application?",
      "options": [
        "No — automatic key rotation changes ID/ARN of CMK, then all references to the old key ID stored next to the historical ciphertexts become invalid.",
        "Yes, but the application needs to update its code to call 'Decrypt' with an additional parameter 'KeyVersion' specifying which rotation version originally encrypted the data.",
        "Yes — pre-rotation encrypted objects remain descriptable SEM no change in the application. When automatic key rotation occurs, the KMS generate NOVO cryptographic material for CMK, But it keeps TODAS the previous versions of the key material under the MESMO ID/ARN of CMK; calls to 'Decrypt' automatically use the version of the key material that originally encrypted the data (the KMS internally track this via metadata embedded in ciphertext), so existing data keys/ciphertexts continue to be correctly described without code changes.",
        "No — all encrypted data with the CMK before rotation become permanently indecipherable after rotation, requiring the application to reencrypt all historical data with the new key material before rotation is completed."
      ],
      "explanation": "Right. The automatic key rotation of the KMS DO NOT replace or discard the old key material — the KMS maintains all previous versions associated with the same CMK, and automatically uses the correct version to describe previously encrypted data, based on metadata in the ciphertext itself. This makes rotation completely transparent for the application.",
      "hint": "Think about what exactly is replaced when automatic key rotation of the KMS occurs: key identifier or only internal cryptographic material.",
      "optionRationales": [
        "Right. The automatic key rotation of the KMS DO NOT replace or discard the old key material — the KMS maintains all previous versions associated with the same CMK, and automatically uses the correct version to describe previously encrypted data, based on metadata in the ciphertext itself. This makes rotation completely transparent for the application.",
        "Incorrect. KMS maintains older versions of key material specifically for previously encrypted data to remain decryptable — a forced reencryption of all historical data is NOT like automatic key rotation of KMS works (this description approaches more than one strategy MANUAL rotation using a NOVA CMK with exchange of aliases, which would actually require reencryption planning, but it is a different approach from the 'automatic key rotation' of a single CMK).",
        "Incorrect. API 'Decryption KMS DO NOT require (not accepted) a keyVersion parameter — the KMS automatically determines the correct version of the key material from metadata embedded in the ciphertext itself, without the application having to track or specify key versions.",
        "Incorrect. Automatic key rotation DOES NOT change ID or ARN of CMK — the identity of the CMK remains the same; only the underlying key material is rotated periodically, preserving the old versions internally."
      ]
    },
    "dva-avc-095": {
      "text": "A function AWS Lambda recovers a database credentials from AWS Secrets Manager calling 'GetSecretValue' in TODA invocation, adding latency and contributing to the costs of requests to API of the Secrets Manager in high volume of invocations. The team wants to cache the secret value LOCALMENTE within the environment of implementation of the Lambda by one TTL configurable (ex.: 5 minutes), reducing redundant calls to 'GetSecretValue'', SEM implement custom cache logic in function code. What solution is provided by AWS You'll have to work it out.?",
      "options": [
        "Use AWS Parameters and Secrets Lambda Extension — a layer of Lambda running a server HTTP location within the running environment, storing in cache values recovered from Secrets Manager (and Parameter Store) by one TTL configurable; function code calls an endpoint HTTP local (ex.: 'http://localhost:2773/secretsmanager/get?secretId=...') instead of GetSecretValue of SDK of AWS directly, and the extension treats the cache and update in a transparent way.",
        "Store the value of the secret in an environment variable of Lambda, and configure the function to update your own environment variables to each 5 minutes via 'UpdateFunctionConfiguration''.",
        "Configure the function Lambda with Provided Competition, which automatically stores in cache values of Secrets Manager for pre-initiated run environments.",
        "Move the call 'GetSecretValue' to the scope GLOBAL of the function code (out of the handler), so that it executes only once during the cold start and the value is reused in warm invocations."
      ],
      "explanation": "Correct. AWS Parameters and Secrets Lambda Extension was designed exactly for this case: a layer provided by AWS adding a server HTTP cached place, with TTL configurable, for values of Secrets Manager and Parameter Store. The function calls the local endpoint instead of the SDK directly, eliminating redundant calls to 'GetSecretValue' without requiring custom cache logic.",
      "hint": "Think of which resource provided by AWS adds a local cache server within the running environment of Lambda, with TTL configurable, without requiring cache logic in function code.",
      "optionRationales": [
        "Correct. AWS Parameters and Secrets Lambda Extension was designed exactly for this case: a layer provided by AWS adding a server HTTP cached place, with TTL configurable, for values of Secrets Manager and Parameter Store. The function calls the local endpoint instead of the SDK directly, eliminating redundant calls to 'GetSecretValue' without requiring custom cache logic.",
        "Wrong. Move calls from SDK for the global scope takes advantage of the reuse of the execution context (avoiding repeated calls during the life of the MESMO container warm), but store the secret in cache throughout the VIDA of that execution environment (which may be hours)), SEM TTL configurable/updated — and each NOVO Cold start container still makes a new call. Lambda Extension provides one TTL configurable with automatic update, which the global scope cache itself does not offer.",
        "Incorrect. Provided Competition keeps running environments warm/initialized to reduce cold startups; does not have NENHUMA relationship with caching responses from API of the Secrets Manager — does not resolve repeated calls to 'GetSecretValue' in any way'.",
        "Incorrect. Update function configuration ('UpdateFunctionConfiguration') DISPARA UMA NOVA UPDATE function (potentially causing cold start for new invocations during update, and requiring the function to be allowed to modify its own configuration) — a complex and anti-standard approach compared to the extension made specifically for this cache need."
      ]
    },
    "dva-avc-096": {
      "text": "An application in Account B needs to invoke directly (via 'lambda:InvokeFunction') a function Lambda belonging to Account A, using a roll of IAM in Account B (Not seen API Gateway or any other intermediary — a direct 'Invoke' via SDK). What configuration is needed to allow such invocation between accounts?",
      "options": [
        "Adding a POLICY BASEADA EM RECURSO (Lambda permission) to the function of Account A, granting the role of Account B (or Account B as principal) permission 'lambda:InvokeFunction' (e.g. via 'aws lambda add-permission with '--main' defined as Account ID B or ARN the role); policy; IAM of the role of Account B also needs to allow 'lambda:InvokeFunction' no ARN of the function of Account A — both recourse-based policy (A account) and identity-based policy (B account) are necessary for access between accounts, in a manner similar to the KMS/Secrets Manager between accounts.",
        "Configure the execution roll of the Account A function to allow 'sts:AssumeRole' from the ARN of the role of Account B.",
        "Create a custom bus event from Amazon EventBridge in Account A, grant Account B permission to publish events in it via a policy of recourse of the EventBridge, and do the function Lambda From Account Processing bus events.",
        "Make Account B assume a NA roll CONTA The one who has permission 'lambda:InvokeFunction in function, via 'sts:AssumeRole', and use the temporary credentials of this role to invoke function."
      ],
      "explanation": "Right, just like it happens with KMS and Secrets Manager, access between accounts to a function Lambda requires DUAS layers: the function-based policy (in Account A) must allow the main account B, and the policy IAM of that principal (in Account B) must enable 'lambda:InvokeFunction' no ARN without the policy based on recourse in the function of Account A, the so-called 'Invoke' between accounts will fail regardless of permissions IAM Account B.",
      "hint": "Think of what kind of policy, attached to its own function Lambda (and not to the execution role of the function), controls which external mains can invoke it.",
      "optionRationales": [
        "Right, just like it happens with KMS and Secrets Manager, access between accounts to a function Lambda requires DUAS layers: the function-based policy (in Account A) must allow the main account B, and the policy IAM of that principal (in Account B) must enable 'lambda:InvokeFunction' no ARN without the policy based on recourse in the function of Account A, the so-called 'Invoke' between accounts will fail regardless of permissions IAM Account B.",
        "Incorrect. EXECUTION ROLE of the function controls what the FIRST FUNCTION can do while performing (its exit permissions) — has no relation to which main EXTERNOS are allowed to INVOCAR the function; the permission to invoke between accounts is controlled by the policy based on recourse of the function..",
        "Incorrect. This introduces a completely different architecture, ASSIGNRONA and event oriented (EventBridge), when the requirement is a direct SYNCRON 'lambda:InvokeFunction' via SDK — a much larger architectural change than simply adding a policy based on the use of the function Lambda.",
        "Incorrect. Although the assumption of a role between accounts is a general standard valid for access between accounts, it requires that Account A define and maintain a dedicated and affordable role and a policy of trust for Account B — grant direct permission to the existing role of Account B 'lambda:InvokeFunction' via function-based policy (plus the identity policy of Account B itself) is the simplest and most direct mechanism, specifically designed for this case."
      ]
    }
  }
}
