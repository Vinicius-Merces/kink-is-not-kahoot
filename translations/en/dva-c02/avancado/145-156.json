{
  "_batch": {
    "locale": "en",
    "sourceLocale": "pt-BR",
    "certId": "dva-c02",
    "level": "avancado",
    "sourcePath": "data/exams/dva-c02/avancado.json",
    "range": "145-156",
    "generator": "offline-marian-faithful-v2"
  },
  "questions": {
    "dva-avc-145": {
      "text": "A function Lambda in Python depends on heavy machine learning libraries (e.g.: PyTorch, transforms), totaling a package of implementation of 800MB — MUITO beyond the limit of 250MB (decompressed, including Layers) of traditional packaging in ZIP. The team wants to pack this function as a IMAGEM OF CONTAINER, with image build and push to the image repository happening automatically within an IC/CD pipeline using the CodeBuild. What design configuration CodeBuild is NEEDED for the docker build and docker push commands to work within the built environment?",
      "options": [
        "Instead of a container image, divide ML libraries into multiples Lambda Layers of up 250MB each, since this limit applies to CADA Layer individually, allowing you to add several Layers to reach the 800Total MB.",
        "Enable 'privilegedMode: true' in the project environment configuration CodeBuild — this gives the built access environment to run a DAEMON DOCKER inside the build container, needed for commands like 'docker build'; the buildsp.yml then includes: pre_build phase running 'aws ecr get-login-password ☆ docker login' (authenticating no Amazon ECR), 'build' phase running 'docker build -t <repo-uri>:<tag> .', and 'post_build' phase running 'docker push <repo-uri>:<tag> — after push, the template SAM/CloudFormation reference URI image of the ECR as function code Lambda (with 'PackageType: Image'), supporting up 10GB Image size.",
        "No special configuration is needed besides increasing the 'computerType' of the project CodeBuild for 'BUILD_GENERAL1_2XLARGE' — larger build instances include access to daemon Docker automatically, different from smaller instances.",
        "Upload the resulting container image to a private repository in AWS CodeArtifact, since CodeArtifact is the artefact storage service of AWS for all types of packages including Docker container images."
      ],
      "explanation": "Correct. 'privilegedMode: true' is mandatory in the project CodeBuild to allow the execution of a daemon Docker within the built environment (necessary for 'docker build'/'docker push'); combined with authentication in ECR and image push, this flow enables the packing of functions Lambda like container images of up to 10GB, exceeding the limit of 250MB of ZIP.",
      "hint": "Think about what configuration of the environment CodeBuild grants access to a daemon Docker inside the build container, needed to execute docker build/'docker push commands'.",
      "optionRationales": [
        "Correct. 'privilegedMode: true' is mandatory in the project CodeBuild to allow the execution of a daemon Docker within the built environment (necessary for 'docker build'/'docker push'); combined with authentication in ECR and image push, this flow enables the packing of functions Lambda like container images of up to 10GB, exceeding the limit of 250MB of ZIP.",
        "Incorrect. Access to the daemon Docker within the built environment of CodeBuild is controlled by the flag 'privilegedMode' (independently of the chosen 'computerType') — without 'privilegedMode: true', docker build/'docker push commands fail with connection errors to daemon Docker, even in larger computer instances.",
        "Incorrect. CodeArtifact is a repository of PACOTES (npm, PyPI, Maven, NuGet, etc.), DO NOT support Docker container images — o Amazon Elastic Container Registry (ECR) is the correct service to store container images, being the default destination for images used as function code Lambda of the type 'Image'.",
        "Incorrect. The limit of 250MB decompressed is the TOTAL COMBINADO function code MAIS TODAS the attached Layers — divide 800MB in multiple Layers of 250MB each does NOT circumvent this aggregate limit; for packages exceeding 250MB in total, packaging as a container image (up to 10GB) is the supported approach."
      ]
    },
    "dva-avc-146": {
      "text": "The platform team needs that a CloudFormation, when creating/updating/excluding a stack, also execute a call from API for a third-party SaaS service (e.g. register a webhook on an external observability tool) — an operation that does NOT match any resource type 'AWS::*' native to CloudFormation. They want this call from API be part of the CICLO OF VIDA of stack (executed in the creation, updating and deletion of stack, with the CloudFormation the results before proceeding). CloudFormation meets this requirement?",
      "options": [
        "Create a MACRO of the CloudFormation (registered via 'AWS::CloudFormation::Macro', a custom Transform) that intercepts the template and executes the call from API to SaaS before the implementation of the resources.",
        "Define a CUSTOM RESOURCE in the template (type 'AWS::CloudFormation::CustomResource' or 'Custom::NameAny') with the 'ServiceToken' property pointing to the ARN of a function Lambda — o CloudFormation INVOCA this function Lambda automatically in the 'Create', 'Update' and 'Delete' events of the stack, passing event details and feature properties; the function Lambda executes the custom logic (e.g. call the API from external SaaS) and sends a status response 'SUCCESS' or 'FAILED' (via the 'cfn-response' module or a PUT for one URL pre-signed S3) — o CloudFormation AGUARDA this response before considering the operation of the stack completed for this resource.",
        "Add a CreationPolicy with a WaitCondition to the template, setting the CloudFormation to pause the creation of the stack until receiving a signal via 'cfn-signal' of an external process that performs the call of API.",
        "Use 'AWS::CloudFormation::Init (cfn-init) in the section 'Metadata' of an instance EC2 of stack, setting up a configuration block that executes the call of API to external SaaS during the instance boot."
      ],
      "explanation": "Right. Custom Resources with 'ServiceToken' pointing to a function Lambda allow to execute ARBITRARY provisioning logic (including calls to external APIs) as part of the Create/Update/Delete life cycle of the stack, with the CloudFormation waiting for the success/failure status reported by the function before proceeding.",
      "hint": "Think about what kind of resource the CloudFormation invokes a function Lambda in the Create/Update/Delete events of the stack and waits for success/fail status before continuing.",
      "optionRationales": [
        "Right. Custom Resources with 'ServiceToken' pointing to a function Lambda allow to execute ARBITRARY provisioning logic (including calls to external APIs) as part of the Create/Update/Delete life cycle of the stack, with the CloudFormation waiting for the success/failure status reported by the function before proceeding.",
        "Incorrect. 'cfn-init'/'AWS::CloudFormation::Init' is a CONFIGURATION OF INSTITUTIONS EC2 (install packages, write files, start services via UserData/cfn-init helper script) — is not a life-cycle mechanism of STACK which applies to Create/Update/Delete in general, and would require provision of an instance EC2 only for this purpose, which is unnecessary and is not the standard for calls from API arbitrary during stack operations.",
        "Incorrect. CloudFormation TRANSFORMAM o TEMPLATE (process and modify the definition JSON/YAML before implementation) — they are not the mechanism for executing calls from API ARBITRARYS linked to the life cycle Create/Update/Delete a stack resource; Custom Resources (Lambda-backed) are the appropriate mechanism for this.",
        "Incorrect. 'CreationPolicy'/'WaitCondition' do the CloudFormation PAUSAR and wait for one SINAL external (via 'cfn-signal') before marking a resource as created — this requires that ALGUM PROCESSO EXTERNO is already running to send the signal and make the call from API, no FORNECE by itself the mechanism of invoking custom logic; moreover, it does not cover the 'Update'/'Delete' events in the integrated way that a Custom Resource Lambda-backed copper."
      ]
    },
    "dva-avc-147": {
      "text": "A template of the CloudFormation is implanted in multiple regions of the AWS (us-east-1, me-west-1, ap-subtheast-1). An appeal 'AWS::EC2::Forum needs an 'ImageId'' (AMI) DIFERENTE in each region (as well as AMI are specific by region), but these values are CONHECIDOS and STAFF at the time the template is written (does not require user entry in time of deployment). EMBUTIR this consultation table region→AMI directly in the template, selecting the correct value automatically based on the region where the stack is being implemented (via the pseudo-variable 'AWS::Region'). CloudFormation it was specifically designed for this type of static consultation table?",
      "options": [
        "Set a 'Parameter' called 'ImageId' with 'AllowedValues' listing all MIAs from all regions, requiring the user to manually select the correct value for the target region at the time of deployment.",
        "Store IDs from AMI of each region as SecureString parameters in Systems Manager Parameter Store (one per region), and refer them in the template using dynamic references '{{resolve:ssm:...}}' with the name of the parameter dynamically built from 'AWS::Region'.",
        "Set a separate 'Condition' for each region (e.g. 'IsUsEast1', 'IsEuWest1', 'IsApSoutheast1') and use 'Fn::If' nested within the resource's 'ImageId' property EC2 to select the value based on what condition is true.",
        "Set a section 'Mappings' in the template (e.g. 'Mappings: RegionAMIMAp: us-east-1: { AMI: friend-xxx}, eu-west-1: { AMI: ami-yyyy }, ...') and reference the correct value using 'Fn::FindInMap' as 'AWS::Region' as the first level search key (e.g. '!FindInMap [RegionAMIMAp, !Ref \"AWS::Region\", AMI]') — 'Mappings' is the section designed for query tables CHAVE-VALOR STATISTICS embedded in the template, solved in time of creation/updating of the stack based on values such as the region."
      ],
      "explanation": "Correct. 'Mappings' is exactly the section of CloudFormation for static and multidimensional query tables (e.g. region → value type → value), solved via 'Fn::FindInMap' using a known key in deployment time (as 'AWS::Region') — taking into account precisely the case of stable and known values at the time of writing of the template, without requiring user entry.",
      "hint": "Think of which section of the CloudFormation works as a multi-key static query table embedded in the template, solved via an intrinsic function that uses a pseudo-variable as a key.",
      "optionRationales": [
        "Correct. 'Mappings' is exactly the section of CloudFormation for static and multidimensional query tables (e.g. region → value type → value), solved via 'Fn::FindInMap' using a known key in deployment time (as 'AWS::Region') — taking into account precisely the case of stable and known values at the time of writing of the template, without requiring user entry.",
        "Incorrect. Using a Parameter would require the USER to know and manually select the AMI correct for CADA region CADA Deployment — this is prone to errors and runs counter to the region-based AUTOMATIC SELECTION requirement; 'Mappings' resolves this selection automatically via 'Fn::FindInMap' with 'AWS::Region', without requiring manual entry.",
        "Incorrect. Although technically possible, use 'Conditions' + 'Fn::If' nested for CADA region/value combination becomes extremely verbose and difficult to maintain as the number of regions/values grows — 'Mappings' with 'Fn::FindInMap' was designed specifically for multi-key query tables like this, being the cleaner and idiomatic approach.",
        "Incorrect. This introduces a DEPENDENCE EXTERNA (resources of Parameter Store which need to be created/maintained separately) and additional complexity (dynamic references with dynamically built parameter names have limitations) to store values that are STATISTICAL and CONHECIDOS in template writing time — 'Mappings' embeds this table directly into the template, without additional external resources, being simpler for this case."
      ]
    },
    "dva-avc-148": {
      "text": "A function Lambda in Python download a file CSV up to 1.5GB of the S3, decompresses and processes this file by saving temporary files in the directory '/tmp' before uploading the result to another pocket S3. The function is intermittently failing with 'In space left on device' errors when processing the larger files. Lambda is of 512MB. The team wants to increase the capacity of the '/tmp' to accommodate larger temporary files, the way MAIS DIRETA possible, SEM rewrite the function processing logic. What you need to set up?",
      "options": [
        "Configure the function 'EphemeralStorage' property Lambda (no SAM: 'EphemeralStorage: Size: 2048', or via console/CLI 'EphemeralStorageSize') for a value between 512MB and 10.240MB (10GB) — this parameter controls DIRETAMENTE the capacity of the directory '/tmp' available for the function, so INDEPENDENTE memory assigned to the function, without requiring any change in the processing code.",
        "Rewrite function to process file CSV in streaming, reading and recording in chunks via 'multipart upload' of S3 directly, eliminating completely the need to save files to '/tmp'.",
        "Mount a file system Amazon EFS in function Lambda and redirect all temporary recordings to the mounting point of the EFS, which offers virtually unlimited storage.",
        "Increase the function's assigned memory Lambda for 10.240MB, since the size of the '/tmp' scale PROPORCIONALMENTE to the configured memory, in the same way as the CPU allocated scale with memory."
      ],
      "explanation": "Correct. 'EphemeralStorage.Size' is the dedicated configuration parameter to control the capacity of '/tmp' (from 512MB up 10.240MB), regardless of function memory — increasing this value directly resolves the insufficient space error without requiring changes in processing logic.",
      "hint": "Think about what parameter configuration of the Lambda directly controls the capacity of the directory '/tmp', regardless of the memory allocated to the function.",
      "optionRationales": [
        "Correct. 'EphemeralStorage.Size' is the dedicated configuration parameter to control the capacity of '/tmp' (from 512MB up 10.240MB), regardless of function memory — increasing this value directly resolves the insufficient space error without requiring changes in processing logic.",
        "Incorrect. CPU (which scale proportionally with memory), the size of the '/tmp' is NOT determined by function memory — is controlled by a parameter SEPARADO and INDEPENDENTE ('EphemeralStorage.Size'); increase memory to 10.240MB would not increase the '/tmp' (which would remain in the default of 512MB) and would significantly increase the cost of invocation without need.",
        "Incorrect. Although riding EFS is a VALID solution for much larger shared/persistent storage needs (above 10GB) or shared between invocations/functions, is a solution SIGNIFICATIVAMENTE MAIS PESADA (requires configuration of VPC, access point EFS, additional permissions) than simply increase 'EphemeralStorage.Size' to up 10GB — for the case described (file of up to 1.5GB, within the limit of 10GB of the '/tmp'), increasing 'EphemeralStorage' is the most direct and SEM rewrite logic.",
        "Incorrect. Although a rewrite for streaming PUDESSE eliminate the dependence of '/tmp' (and be good architectural practice for some cases), the scenario EXPLICITAMENTE asks for a solution 'SEM rewrite the processing logic' — increase 'EphemeralStorage.Size' resolves the immediate space problem in a configurational way, without requiring code rewriting effort."
      ]
    },
    "dva-avc-149": {
      "text": "A function Lambda used by a API critical has traffic patterns that VARIAM significantly throughout the day (peaks during business hours, low usage at night), but exact peak times are not perfectly predictable (various per day). The team configured Provisioned Competition with a value of FIXO of 50, But it causes THROTTLING during unexpected peaks (when demand exceeds 50) AND WASTE OF CUSTO during periods of low blood pressure ( 50 They want the provided competition to be AJUSTE AUTOMATICAMENTE on the basis of the REAL in real time, within set minimum/maximum limits. Which approach meets this requirement?",
      "options": [
        "Record function alias Lambda as a TARGET SCALABLE AWS APPLICATION AUTO SCALING (namespace 'lambda', scalable dimension 'lambda:function:ProvisionedCompetition, feature 'function:NameFunction:NameAlias'), and create a TARGET TRACKING based on the metric 'LambdaProvisionedCompetitionUtilization' (e.g. target value of 0.7) — o Application Auto Scaling AJUSTA AUTOMATICAMENTE the value of Provided Competition up or down, within the minimum/maximum set limits, based on actual usage observed, without manual intervention.",
        "Configure APENAS 'Scheduled Scaling to increase the Provisioned Competition to 100 during business hours (9h-18(h) and reduce it to 10 outside of that time.",
        "Continuously increase the Reserved Competition function to 200, ensuring that there is always sufficient capacity allocated to peaks, eliminating the need for Provisioned Competition.",
        "Create an alarm from CloudWatch in the 'ConcurrentExecutions' metric of the function that, when crossing a threshold, triggers a function Lambda separate calling 'UpdateFunctionConfiguration' to manually increase the value of 'ProvisionedCompetitionExecutions'."
      ],
      "explanation": "Correct. The Application Auto Scaling with a Target Tracking policy in the 'LambdaProvisionedCompetitionUtilization' metric dynamically adjusts the Provided Competition based on real-time use, respecting min/max limits — exactly the self-adjustment behavior based on described use, balancing cost and throttling.",
      "hint": "Think of what service the AWS allows you to register the Provided Competition of an alias Lambda as a scalable resource and adjust it automatically based on a use metric.",
      "optionRationales": [
        "Correct. The Application Auto Scaling with a Target Tracking policy in the 'LambdaProvisionedCompetitionUtilization' metric dynamically adjusts the Provided Competition based on real-time use, respecting min/max limits — exactly the self-adjustment behavior based on described use, balancing cost and throttling.",
        "Incorrect. CUSTOMIZADA manually reimplements a functionality that AWS Implementation Auto Scaling already provides NATIVAMENTE through Target Tracking policies — in addition to adding operational complexity (managing an extra function, scaling logic up and down, avoiding oscillations), it would be slower to react and more prone to errors than the managed solution.",
        "Incorrect. 'Scheduled Scaling' works well for patterns TOTALMENTE PREVIOUS at fixed times, but the scenario describes peaks that 'NO are perfectly predictable (variable per day)' — scheduled scaling alone does not react to peaks INESPERADOS inside or outside the scheduled window; Target Tracking based on use if ADAPTA dynamically to real variations of demand, better meeting the requirement.",
        "Incorrect. Reserved Competition defines a TETO Maximum number of competing executions (and reserve that capacity of the pool of competition of the account) — DOES NOT maintain PRE-executive environments-INICIALIZADOS ('heated'), therefore does NOT solve cold start; increase Reserved Competition permanently for 200 also does not solve the problem of CUSTO low-use does not provide dynamic adjustment based on requested use."
      ]
    },
    "dva-avc-150": {
      "text": "A function Lambda integrated synchronously into the Amazon API Gateway executes a report export that may legitimately lead to 45 The team set up the first two seconds to complete in some cases. TIMEOUT function Lambda for 60 (within the maximum limit of 15 minutes), but the client still receives an error HTTP 504 (Gateway Timeout) of API Gateway after approximately 29 seconds, MESMO when the function Lambda would continue to perform normally until the 45 What is the correct explanation and architectural solution??",
      "options": [
        "Enable CACHE of responses from API Gateway with a TTL long (ex.: 1 time) for this route, preventing subsequent requests of the same type from facing the timeout of 29 seconds again.",
        "The API Gateway has a TIMEOUT OF MAXIMUM INTEGRATION FIXO of 29 SEGUNDOS for integrations with Lambda (so much REST APIs HTTP APIs), which do NOT PODE be increased beyond this value, regardless of the timeout set in the function itself Lambda — for tasks that legitimately require more than 29 seconds, the solution is DESACOPLAR the call: the API Gateway invokes function Lambda ASSYNCRONA (or actuates a Step Functions State Machine) immediately returning a task identifier (ex.: 202 Accepted), and the customer CONSULTA a separate status endpoint (polling) or receives a notification (e.g. via WebSocket) when processing 45 seconds is completed.",
        "Put a Network Load Balancer (NLB) in front of the function Lambda, instead of the API Gateway, since NLBs do not require any requisition timeout and may have functions Lambda as TARGETS direct to synchronous calls with any duration.",
        "Increase the 'IntegrationTimeoutInMillis' parameter of integration of API Gateway to 60000 (60 seconds), aligning it with the function timeout Lambda of 60 seconds."
      ],
      "explanation": "Right. 29 seconds to the integration timeout of the API Gateway with Lambda is a limit HARD (not configurable beyond that value) — for operations that exceed this limit, the correct architectural standard is to decouple by asynchronous processing (Lambda asynchronous, Step Functions, SQS) with the client consulting the result later, instead of waiting for an immediate synchronous response.",
      "hint": "Think of what timeout limit of API Gateway for integrations Lambda It's a roof. HARD and cannot be increased, regardless of the timeout configured in the function.",
      "optionRationales": [
        "Right. 29 seconds to the integration timeout of the API Gateway with Lambda is a limit HARD (not configurable beyond that value) — for operations that exceed this limit, the correct architectural standard is to decouple by asynchronous processing (Lambda asynchronous, Step Functions, SQS) with the client consulting the result later, instead of waiting for an immediate synchronous response.",
        "Incorrect. Although 'IntegrationTimeoutInMillis' (or equivalent in REST APIs) is a configurable parameter, it has a VALOR Maximum HARD of 29.000ms (29 seconds) for integrations Lambda — try to set it up for 60000 will have no effect beyond that ceiling; the limit of 29 seconds is IMPOSTO PELA PLATAFORMA and cannot be bypassed by configuration.",
        "Incorrect. NLBs direct traffic to instance-type targets EC2, IP address or Application Load Balancer — functions Lambda NO direct targets supported by NLBs for this invocation pattern HTTP synchronous; this is not an architectural solution valid for the timeout problem of 29 seconds of API Gateway.",
        "Incorrect. Cache of answers does not solve the problem of PRIMEIRA (non-hidden) request, which would still suffer the 504 after 29 seconds — in addition, cache error responses 504 is not the desired behavior, and each combination of export parameters would probably generate a different cache key, making the cache ineffective for this case of export use of reports."
      ]
    },
    "dva-avc-151": {
      "text": "A team manages a set of functions Lambda shared. Recently, they have updated a LAMBDA LAYER (containing a common utility library) used by 5 different functions, publishing a NOVA LAYER VERSATION with major bug fixes. After the deployment, they realized that the 5 functions CONTINUAM exhibiting behavior ANTIGO (with the bug not fixed), even after subsequent invocations (without apparent related cold start). What is the explanation for this behavior, and what the team needs to do for the functions to use the corrected layer code?",
      "options": [
        "Functions should be configured to refer to the layer using the special identifier '$LATEST' (in the same way as versions of functions Lambda), which makes them always point to the most recently published layer version.",
        "The Layers are referenced by NOME (not by the version) in the function configuration, so all functions that use this layer automatically adopt the latest version published in the NEXT invocation with cold start.",
        "Delete Version ANTIGA layer after publishing the new version — this automatically forces all functions that reference the deleted version to use (fallback) to the latest available version.",
        "Each VERSATION PUBLICADA of a Lambda Layer is IMPUTABLE — publish an update on a layer creates a NOVA VERSATION of the layer (with its own incremental number and ARN exclusive, e.g.: ending in ':2', ':3'), while VERSÃO ANTERIOR remains INALTERADA and continues to be used by any function that still explicitly refers to it; the team needs ATUALIZAR the 'Layers' property of CADA one of 5 functions to reference the NOVO ARN of layer version (e.g.: '...:layer:utils:3' instead of '...:layer:utils:2') so that they use the corrected code."
      ],
      "explanation": "Correct. Versions of Lambda Layers are immutable — publishing an update creates a new version with ARN own, without affecting functions that refer to the previous version; each function must be explicitly reconfigured to point to the new ARN layer version to adopt the change, allowing rollouts controlled by function.",
      "hint": "Think of it as a published version of a Lambda Layer behaves in terms of immutability, and as a function Lambda reference which layer version to use.",
      "optionRationales": [
        "Correct. Versions of Lambda Layers are immutable — publishing an update creates a new version with ARN own, without affecting functions that refer to the previous version; each function must be explicitly reconfigured to point to the new ARN layer version to adopt the change, allowing rollouts controlled by function.",
        "Incorrect. The 'Layers' property of setting a function Lambda reference o ARN COMPLETO of a SPECIFIC VERSION of the layer (including the version number), not only the name — there is no automatic propagation to the latest version, nor to cold start; without explicitly updating the ARN referenced, the function will continue to use the old version indefinitely.",
        "Incorrect. Lambda Layers DO NOT have a concept of '$LATEST' or aliases as exists for VERSIONS OF FUNCTIONS Lambda — each layer version is a ARN distinct and unchangeable numbered, and should be explicitly referenced; there is no 'always newer' pointer for layers.",
        "Incorrect. Excluding a layer version that is still referenced by functions makes these functions go to APRESENTAR ERROS in subsequent invocations (the referenced version no longer exists) — There is NO automatic failback mechanism for another version; the correct form is to explicitly update the reference of each function to the new one. ARN of version, and only then (optionally) remove the old version if no longer required."
      ]
    },
    "dva-avc-152": {
      "text": "A team has a function Lambda in Node.js whose implementation package, including dependencies (native binary for image processing), totalizes 800MB — exceeding the limit of 250MB decompressed (code + all combined Layers) of packaging ZIP They decide to pack the function as a IMAGEM OF CONTAINER stored Amazon ECR ('PackageType: Image'). What statement describes CORRETAMENTE the behavior/trade-off of this approach compared to packaging ZIP?",
      "options": [
        "Container images Lambda support up to 10GB size (vs. 250MB of ZIP/Combined layers), and the function continues to be performed in MESMO managed execution environment of Lambda (the same invocation/duration collection model, the same automatic step-up model and the provided competition) — the difference is APENAS in COMO the code and dependencies are packaged and delivered to the Lambda (an image OCI compatible, stored in ECR, instead of a file ZIP), not in how the function is executed/collected.",
        "After migrating to container image, the function Lambda is invoked through a URL of the Amazon ECR directly by the customers of the API, eliminating the need for service Lambda as an execution layer.",
        "Functions packaged as a container image are charged by HORA of the container (model similar to the Fargate), instead of the invocation and duration charge model in milliseconds used for functions packaged in ZIP.",
        "Functions packed as a container image DO NOT support provided competition neither Lambda Layers, since these features depend on the packaging format ZIP."
      ],
      "explanation": "Correct. Packaging as a container image increases the size limit of the package 250MB to 10GB, but the execution continues within the standard model of Lambda (same runtime managed, scaling, provided competition and invocation/duration collection model) — the change is only in the format/mechanism of packing and distribution of the code.",
      "hint": "Think about what really changes (and what stays the same) when changing the packaging format of a function Lambda of ZIP for container image stored in ECR.",
      "optionRationales": [
        "Correct. Packaging as a container image increases the size limit of the package 250MB to 10GB, but the execution continues within the standard model of Lambda (same runtime managed, scaling, provided competition and invocation/duration collection model) — the change is only in the format/mechanism of packing and distribution of the code.",
        "Incorrect. MODELO COVERAGE OF THE Lambda (by number of invocations + duration in milliseconds × assigned memory) is the MESMO regardless of packaging format (ZIP or container image) — there is no 'per hour' style charge Fargate for Lambda based on container; the difference between ZIP and container image is only in the code delivery format, not in the collection model.",
        "Incorrect. Functions Lambda based on container image SUPORTAM Provided Competition usually (with the relief of potentially larger cold start for large, meagre images with SnapStart on some runs) — but it is true that container images do NOT use Lambda Layers of the traditional form (dependencies are included directly in the image via Dockerfile); however, the claim that Provided Competition 'is not supported' is incorrect.",
        "Incorrect. ECR It's APENAS the storage repository of the IMAGEM used as function source code — customers/API Gateway continues to invoke the function Lambda usually ( ARN/function name, Function URL, or integration with API Gateway); or ECR does not expose a direct invocation endpoint to customers, and the service Lambda remains the execution layer."
      ]
    },
    "dva-avc-153": {
      "text": "An application uses the Amazon ElastiCache for Redis (cluster mode disabled mode, a primary node) + 2 read replicas) to store a catalog of products in cache. The catalog grew to 50GB, EXCEDENDO the memory capacity of the MAIOR type of node available for the cluster — data need to be PARTICIONADOS (sharded) between MULTIPLES primary nos, each responsible for a subset of the key space, so that the total data set fits into the sum of the memory of several nos. The application already uses a cluster-compatible Redis client library. Which ElastiCache configuration meets this horizontal partitioning requirement?",
      "options": [
        "Enable one GLOBAL DATASTORE of ElastiCache for Redis, replicating the cluster to a secondary region — this effectively DOBRA the total memory capacity available when adding the memory of the two regions.",
        "Keep 'cluster mode disabled' but increase the number of read replicas from 2 for 5, distributing catalog readings among more replicas to reduce memory load in each individual node.",
        "Re-create the ElastiCache for Redis replication group with 'CLUSTER MODE ENABLED' (Redis Cluster) — in this way, keyspace is PARTICIONADO up to 16.384 hash slots distributed between MULTIPLES SHARDS (groups of us), each shard containing a primary node (and optionally replicas); the data is automatically distributed among the shards based on the key hash, allowing the total data set to exceed the memory capacity of a single node — the Redis Cluster-compatible client automatically routes the commands to the correct shard.",
        "Migratory to the largest type of knot available (e.g. 'cache.r6g.16xlarge') and keep 'cluster mode disabled', since larger node types always have enough memory for any cache workload."
      ],
      "explanation": "Correct. 'Cluster Mode Enabled' (Redis Cluster) partitions the keyspace between multiple shards (each with its own primary node), distributing the data horizontally — exactly the mechanism required when the data set exceeds the memory capacity of a single node, with cluster-aware clients automatically routing to the correct shard.",
      "hint": "Think about which ElastiCache for Redis partitioning (do sharding) keyspace configuration between multiple primary nos, instead of just replicating the same full data set for additional nos.",
      "optionRationales": [
        "Correct. 'Cluster Mode Enabled' (Redis Cluster) partitions the keyspace between multiple shards (each with its own primary node), distributing the data horizontally — exactly the mechanism required when the data set exceeds the memory capacity of a single node, with cluster-aware clients automatically routing to the correct shard.",
        "Incorrect. Em 'cluster mode disabled', CADA replica maintains a COPY COMPLETA of the MESMO primary data set (reading/HA replication, non-partitioning) — adding more replicas does NOT reduce the amount of data stored in each node nor solves the problem of the set of 50GB exceed the memory capacity of a single node; only 'cluster mode enabled' partitions (fragment) the DADOS among primary multiples.",
        "Incorrect. Even the largest type of knot has a memory limit FINITO — if the data set continues to grow (or already exceeds this limit), 'cluster mode disabled' does not offer a path of scaling HORIZONTAL (partitioning); 'cluster mode enabled' is the approach that allows scaling by adding MAIS shards as the data set grows, instead of depending only on the size of the node.",
        "Incorrect. 'Global Datastore' replicates or MESMO data set for a SECONDARY cluster in OUTRA REGION for the purpose of DISASTER RECOVERY/regional latency (local reading in multiple regions) — is a REPLICA COMPLETA of data, not partitioning that increases the effective storage capacity; does not solve the problem of a data set that exceeds the memory capacity of a node/cluster."
      ]
    },
    "dva-avc-154": {
      "text": "An application uses the Amazon ElastiCache for Redis (cluster mode disabled), 1 primary node + 2 replicates in different AZs) to store user sessions. During a recent interruption in an Availability Zone, the NON PRIMARY was unavailable, and the application presented errors of ESCRITA for approximately 15 minutes, until an operator promotes MANUALMENTE one of the replicas for primary through the console. The team wants that, in future incidents, this promotion occurs AUTOMATICAMENTE, without manual intervention, minimizing the time of unavailability of writing. What should be configured in the replication group of ElastiCache?",
      "options": [
        "Enable 'Multi-AZ' as FAILOVER AUTOMATICAL in the replication group ('AutomaticFailoverEnabled: true') — with this configuration, the ElastiCache MONITORA the health of the primary node and, when detecting a failure, PROMOVE AUTOMATICAMENTE one of the reading replicas (in a different AZ) to become the new primary node, updating the cluster endpoint to point to the new primary — without the need for manual operator intervention.",
        "Create an alarm from CloudWatch in the health metric of the primary node, which, when firing, activates a function Lambda which calls API appropriate ElastiCache to manually promote a replica for primary.",
        "Increase the number of read replicas from 2 for 5, distributed in more AZs, since a greater number of replicas available increases the probability of ElastiCache automatically promoting one of them in the event of primary failure.",
        "Enable one GLOBAL DATASTORE, replicating the replication group to a secondary region, so that the failure occurs automatically to the secondary region in case of primary node failure in the main region."
      ],
      "explanation": "Correct. 'AutomaticFailoverEnabled: true' (Multi-AZ) makes ElastiCache automatically detect the failure of the primary node and promote a healthy replica in another AZ for primary, updating the endpoint — eliminating the need for manual promotion and minimizing the time of unavailability of writing.",
      "hint": "Think about which specific ElastiCache replication group configuration enables primary node failure detection and automatic promotion of a replica without manual intervention.",
      "optionRationales": [
        "Correct. 'AutomaticFailoverEnabled: true' (Multi-AZ) makes ElastiCache automatically detect the failure of the primary node and promote a healthy replica in another AZ for primary, updating the endpoint — eliminating the need for manual promotion and minimizing the time of unavailability of writing.",
        "Incorrect. CUSTOMIZADA reimplements, with additional latency (alarm evaluation time + invocation of Lambda), a functionality that ElastiCache already offers NATIVAMENTE and much faster through 'AutomaticFailoverEnabled' (Multi-AZ) — there is no need to build this mechanism manually.",
        "Incorrect. 'Global Datastore' is designed for DISASTER RECOVERY ENTRE REGIONS (complete regional failure scenarios), typically with failover involving additional steps and greater latency — for a failure of NO PRIMARY within a single region (AZ failure scenario), 'AutomaticFailoverEnabled' (Multi-AZ) within the MESMA region is the appropriate mechanism, faster and less scope for this type of incident.",
        "Incorrect. No AutomaticFailoverEnabled enabled, NENHUMA automatic promotion occurs, regardless of QUANTAS replicas exist — the number of replicas affects the ability to read and the availability of candidates for promotion, but the AUTOMATICAL PROMOTION itself depends exclusively on the Multi-AZ/Automatic Failover configuration being enabled."
      ]
    },
    "dva-avc-155": {
      "text": "A team is designing a new cache layer for a game application with the following requirements: (1) support to ESTRUTURAS OF DADOS complex, as SORTED SETS to keep a leaderboard of players ranked by score in real time; (2) PERSISTENCE of disk data (snapshots) for recovery after reboot; (3) PUB/SUB native to notify players in real time about changes in ranking; and (4) REPLICATION with automatic failover for high availability. What mechanism (engine) of Amazon ElestiCache responds to TODOS these requirements?",
      "options": [
        "Amazon ElastiCache for MEMCACHED — being multi-threaded, scale better horizontally for simple key-value cache charges than the Redis (which is single-threaded), making it the choice of better performance for this scenario.",
        "Amazon ElastiCache for MEMCACHED, supplemented by a topic of the Amazon SNS to implement the pub/sub functionality that Memcached does not have native.",
        "Amazon DynamoDB Accelerator (DAX) configured as a cache layer in front of a table DynamoDB which stores the players' ranking.",
        "Amazon ElastiCache for REDIS — natively supports rich data structures like random sets (ideal for leaders with 'ZADD'/'ZRANGE'), offers PERSISTENCE via snapshots (RDB) and/or append-only logs (AOF), supports PUB/SUB native to real-time notifications, and supports Multi-AZ REPLECTION and automatic failover — taking into account TODOS the four listed requirements."
      ],
      "explanation": "Right. Redis (and therefore ElastiCache for Redis) is the ONLY engine among the options it offers, so that NATIVA and integrated, sorted sets for leadersboards, persistence via snapshots/AOF, pub/sub and replication with automatic failover — meeting all the requirements of the scenario simultaneously.",
      "hint": "Think about which ElastiCache engine natively offers data structures like sorted sets, disk persistence, pub/sub and failed replication — all in one single product.",
      "optionRationales": [
        "Right. Redis (and therefore ElastiCache for Redis) is the ONLY engine among the options it offers, so that NATIVA and integrated, sorted sets for leadersboards, persistence via snapshots/AOF, pub/sub and replication with automatic failover — meeting all the requirements of the scenario simultaneously.",
        "Incorrect. Although Memcached is multi-threaded and suitable for CACHE SIMPLES of high transfer rate value key, it does NOT support data structures such as sorted sets, does not offer disk persistence, and does NOT have native pub/sub nor replication with automatic failover — not attending to NENHUM additional requirements for leaderboard, persistence, pub/sub or replication described in the scenario.",
        "Incorrect. Even if adding SNS to fill the pub/sub gap, the Memcached AINDA DO NOT support random sets (needs for ordered leaderboard), disk persistence, or auto failed replication — multiple gaps remain unresolved; Redis meets all native requirements without additional services required.",
        "Incorrect. DAX is a specific cache and EXCLUSIVO for the DynamoDB (compatible only with the API of the DynamoDB), is not an engine of general purpose of ElastiCache, and does not directly offer structures such as sorted sets with redis semantics or pub/sub — it is not the correct option for the described requirements, which point directly to the native redis capabilities."
      ]
    },
    "dva-avc-156": {
      "text": "A static site is hosted in a pocket of the Amazon S3 and distributed through the Amazon CloudFront. The 'app.js' file has a 'Cache-Control: max-age'=86400' (24 hours) configured in the cache behavior of the CloudFront — a configuration INTENCIONAL which benefits most of the static files of the site (CSS, images, sources), significantly reducing the load at source S3. After uploading a NOVA VERSATION of 'app.js' (same file name, updated content with a critical bug fix) to the S3, users continue to receive the version ANTIGA from file to 24 hours (served from the cache of CloudFront). The team needs this critical correction reflected IMEDIATAMENTE for all users, SEM change the strategy of TTL of 24 hours for the other static files on the site (which benefit from the long cache). What should you do?",
      "options": [
        "Reduce 'Default TTL' of the cache behavior of the CloudFront for 0 seconds to TODA the distribution, forcing the CloudFront to always validate with origin before serving any object.",
        "Configure 'Cache-Control: no-cache' in TODOS the objects of the pocket S3, ensuring that no file is cached by CloudFront from that moment.",
        "Issue an INVALIDATION OF CACHE of the CloudFront specifically for the path '/app.js' (ex.: 'aws cloudfront create-invalidation --distribution-id <ID> --paths \"/app.js\"') immediately after uploading the new version to the S3 — it forces him to CloudFront to treat the subsequent requests for '/app.js' as a cache failure (cache miss), looking for the updated version directly from the source S3, SEM change the configuration of TTL/Cache-Control of Cache Behavior for Other Files.",
        "Create a NOVA distribution of CloudFront for each application department, and update the record DNS to point to the new distribution after each deployment."
      ],
      "explanation": "Correct. A cache invalidation of the CloudFront directed to the specific path ('/app.js') removes only this object from the cache edge locations, forcing the search for the updated version at source, without the need to change the configuration of TTL/Cache-Control for the rest of the site — solving the ad hoc problem without affecting the general cache strategy.",
      "hint": "Think of what operation the CloudFront forces the search for an updated version of a specific file at source, without changing the configuration of TTL/cache of all distribution.",
      "optionRationales": [
        "Correct. A cache invalidation of the CloudFront directed to the specific path ('/app.js') removes only this object from the cache edge locations, forcing the search for the updated version at source, without the need to change the configuration of TTL/Cache-Control for the rest of the site — solving the ad hoc problem without affecting the general cache strategy.",
        "Incorrect. Apply no-cache to TODOS objects eliminates cache benefits (load reduction at source, lower latency) to the TODOS the static files of the site, not only for 'app.js' — the explicit requirement is to solve the problem of 'app.js'' SEM change the strategy of TTL for other files; a directed invalidation resolves this without this broad side effect.",
        "Incorrect. Create a new distribution CloudFront per employee is an approach EXTREMAMENTE costly and operationally complex (each distribution takes minutes to spread globally, requires updating of DNS, and multiple distributions need to be managed/cleaned) — an invalidation directed to the specific file is the standard and much simpler practice for this scenario.",
        "Incorrect. TTL for 0 in TODA the distribution eliminates the benefits of cache to TODOS the files (not just 'app.js'), increasing the load at source S3 and latency for all users and files — the requirement is to solve the problem only for the updated file, SEM change the strategy of TTL for the others; a punctual invalidation is the appropriate surgical solution."
      ]
    }
  }
}
